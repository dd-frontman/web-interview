## ⚙ Основные встроенные утилитарные типы

### `Partial<Type>`

Делаeт **все свойства объекта необязательными**:

```
type PartialUser = Partial<User>
// все поля optional
```

### `Required<Type>`

Противоположность `Partial`. Все поля становятся **обязательными**:

```
type RequiredUser = Required<User>
```

Возвращает исходный тип с гарантированным заполнением свойств

### `Readonly<Type>`

Делает все свойства **только для чтения**:

```
type ReadonlyUser = Readonly<User>
readonlyUser.name = "new" // Ошибка
```

### `Record<Keys, Type>`

Создаёт объект с ключами из `Keys` и значениями заданного типа:

```
type StatusMap = Record<'active'|'inactive', string>
```

Альтернатива `{ [K in Keys]: Type }`

### `Pick<Type, Keys>`

Выбирает из типа только указанные свойства:

```
type Contact = Pick<User, 'name'|'email'>
```

Полезен для компонент или API с ограниченным набором данных.

### `Omit<Type, Keys>`

Удаляет из типа указанные свойства:

```
type NoAge = Omit<User, 'age'>
```

Обратный `Pick`. Удаляет ненужное.

### `Exclude<UnionType, ExcludedMembers>`

Убирает из объединения все типы, совпадающие с `ExcludedMembers`:

```
type WithoutString = Exclude<string|number, string> // number
```

Для фильтрации union-типа.

### `Extract<Type, Union>`

Оставляет из `Type` только те члены, которые есть в `Union`:

```
type OnlyString = Extract<'a'|'b'|1, 'b'|'a'> // 'a'|'b'
```

Сортировка типов из разных объединений.

### `NonNullable<Type>`

Убирает `null` и `undefined`:

```
type NoNull = NonNullable<string|undefined|null> // string
```

Полезно при строгих API.

### `ReturnType<FunctionType>`

Извлекает тип возвращаемого значения функции:

```
type R = ReturnType<() => number> // number
```

### `Parameters<FunctionType>`

Возвращает tuple с типами параметров функции:

```
type P = Parameters<(a: string, b: number) => void> // [string, number]
```

### `Awaited<Type>`

(added в TS 4.5) Разворачивает промис, возвращая совокупный тип результата:

```ts
type A = Awaited<Promise<Promise<string>>> // string
```

---

## 📋 Таблица: список утилитарных типов

| Utility Type       | Что делает                                 |
|--------------------|---------------------------------------------|
| `Partial<T>`       | Все свойства опциональны                   |
| `Required<T>`      | Все свойства обязательны                   |
| `Readonly<T>`      | Все свойства только для чтения             |
| `Pick<T, K>`       | Выбирает указанные свойства из типа        |
| `Omit<T, K>`       | Убирает указанные свойства из типа          |
| `Record<K, T>`     | Тип объекта с ключами `K` и значениями `T` |
| `Exclude<T, U>`    | Удаляет члены типа, входящие в `U`         |
| `Extract<T, U>`    | Оставляет только члены `T` из `U`           |
| `NonNullable<T>`   | Убирает `null` и `undefined`                |
| `ReturnType<F>`    | Тип результата функции                      |
| `Parameters<F>`    | Типы параметров функции как tuple           |
| `Awaited<T>`       | Разворачивает промис типа `T`               |

---

## 🎯 Когда применять?

- **При рефакторинге** типов и интерфейсов.
- **В React/Redux/React Query** — для обновления частей объекта (`Partial`) и передачи props (`Pick`).
- **Для API-моделей** — с `Omit` ограничить поля, `Required` для обязательных.
- **Для безопасных асинхронных функций** — `Awaited<Type>` помогает корректно типизировать результат `await`.

---

Практика: комбинируйте стандартные utility types с собственными generic-типами, чтобы снижать дублирование и риск несогласованных интерфейсов.
