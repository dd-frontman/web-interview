# 🧮 Алгоритмы

> [!tip] Связанные темы
>
> - JavaScript
> - Структуры данных
> - Эффективное обучение
> - Подготовка к собеседованию

## 🎯 Что такое алгоритм

**Алгоритм** — это пошаговая инструкция для решения задачи. Это как рецепт: берешь ингредиенты, выполняешь действия в определенном порядке и получаешь результат.

## ⏱️ Сложность алгоритмов

### **Big O нотация**

Показывает, как растет время выполнения с ростом входных данных:

- **O(1)** — константное время (всегда одинаково)
- **O(n)** — линейное время (пропорционально размеру)
- **O(n²)** — квадратичное время (растет быстро)
- **O(log n)** — логарифмическое время (растет медленно)

### **Примеры сложности**

```javascript
// O(1) - константное время
function getFirst(arr) {
	return arr[0];
}

// O(n) - линейное время
function findElement(arr, target) {
	for (let i = 0; i < arr.length; i++) {
		if (arr[i] === target) return i;
	}
	return -1;
}

// O(n²) - квадратичное время
function findDuplicates(arr) {
	let duplicates = [];
	for (let i = 0; i < arr.length; i++) {
		for (let j = i + 1; j < arr.length; j++) {
			if (arr[i] === arr[j]) duplicates.push(arr[i]);
		}
	}
	return duplicates;
}
```

## 🔍 Алгоритмы поиска

### **Линейный поиск**

Проходит по всем элементам по порядку:

```javascript
function linearSearch(arr, target) {
	for (let i = 0; i < arr.length; i++) {
		if (arr[i] === target) return i;
	}
	return -1;
}

// Сложность: O(n)
// Используй когда: массив не отсортирован или маленький
```

### **Бинарный поиск**

Работает только с отсортированными массивами:

```javascript
function binarySearch(arr, target) {
	let left = 0;
	let right = arr.length - 1;

	while (left <= right) {
		let mid = Math.floor((left + right) / 2);

		if (arr[mid] === target) return mid;
		if (arr[mid] < target) left = mid + 1;
		else right = mid - 1;
	}

	return -1;
}

// Сложность: O(log n)
// Используй когда: массив отсортирован
```

## 🔄 Алгоритмы сортировки

### **Пузырьковая сортировка**

Сравнивает соседние элементы и меняет их местами:

```javascript
function bubbleSort(arr) {
	let n = arr.length;

	for (let i = 0; i < n; i++) {
		for (let j = 0; j < n - i - 1; j++) {
			if (arr[j] > arr[j + 1]) {
				[arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];
			}
		}
	}

	return arr;
}

// Сложность: O(n²)
// Используй когда: массив маленький или для обучения
```

### **Быстрая сортировка**

Разделяет массив на части и сортирует их рекурсивно:

```javascript
function quickSort(arr) {
	if (arr.length <= 1) return arr;

	let pivot = arr[Math.floor(arr.length / 2)];
	let left = arr.filter((x) => x < pivot);
	let middle = arr.filter((x) => x === pivot);
	let right = arr.filter((x) => x > pivot);

	return [...quickSort(left), ...middle, ...quickSort(right)];
}

// Сложность: O(n log n) в среднем, O(n²) в худшем случае
// Используй когда: нужна эффективная сортировка
```

## 🔁 Рекурсия

### **Факториал**

```javascript
function factorial(n) {
	if (n <= 1) return 1;
	return n * factorial(n - 1);
}

console.log(factorial(5)); // 120
```

### **Числа Фибоначчи**

```javascript
function fibonacci(n) {
	if (n <= 1) return n;
	return fibonacci(n - 1) + fibonacci(n - 2);
}

// Оптимизированная версия с мемоизацией
function fibonacciMemo(n, memo = {}) {
	if (n in memo) return memo[n];
	if (n <= 1) return n;

	memo[n] = fibonacciMemo(n - 1, memo) + fibonacciMemo(n - 2, memo);
	return memo[n];
}

console.log(fibonacciMemo(10)); // 55
```

## 🧩 Стратегии решения задач

### **1. Разбивай на части**

- Раздели большую задачу на маленькие
- Решай каждую часть отдельно
- Объединяй решения

### **2. Ищи паттерны**

- Анализируй примеры
- Ищи повторяющиеся элементы
- Используй известные алгоритмы

### **3. Начинай с простого**

- Реши задачу для простых случаев
- Постепенно усложняй
- Обобщай решение

### **4. Тестируй**

- Проверяй на разных входных данных
- Ищи граничные случаи
- Отлаживай пошагово

## 💡 Практические советы

- **Изучай базовые алгоритмы** — они часто встречаются в задачах
- **Практикуйся регулярно** — LeetCode, HackerRank, Codewars
- **Анализируй решения** — понимай, почему алгоритм работает
- **Оптимизируй постепенно** — сначала работающий код, потом быстрый

## 📚 Ресурсы для изучения

- **Книги**: "Грокаем алгоритмы", "Алгоритмы: построение и анализ"
- **Курсы**: Coursera, edX, YouTube
- **Практика**: LeetCode, HackerRank, Codewars
- **Визуализация**: VisuAlgo, Algorithm Visualizer

> [!note] См. также
>
> - JavaScript
> - Структуры данных
> - Подготовка к собеседованию

#алгоритмы #структуры-данных #сложность #оптимизация #программирование
