## 🧱 Рендер-функции (`h`, `resolveComponent`)

Вместо шаблонов (`<template>`) ты можешь описать **структуру компонента через JavaScript**.
- `h` — функция для создания виртуального DOM-элемента.
- `resolveComponent` — позволяет получить компонент по имени (как `import`, но внутри рендера).
```js
import { h, resolveComponent } from 'vue'

export default {
  render() {
    const MyButton = resolveComponent('MyButton')
    return h('div', [
      h('p', 'Привет из render()'),
      h(MyButton, { onClick: () => alert('Нажал') }, 'Кнопка')
    ])
  }
}
```
🔹 Используется редко, но полезно для динамических UIs, markdown-парсеров и т.п.