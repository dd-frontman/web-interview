## 1. Краткое объяснение
Vue выглядит простым фреймворком для создания интерфейсов, но под капотом у него есть сложный механизм: реактивность, рендеринг, оптимизации, управление жизненным циклом. Эти темы важно изучить, чтобы перейти с уровня "умею писать компоненты" на уровень "понимаю, как работает фреймворк".

---
## 2. Подробное объяснение

### 🔹 Основные подкапотные механизмы Vue:
1. **Реактивность (Reactivity system)**  
   - Vue 2: `Object.defineProperty`.  
   - Vue 3: Proxy (`reactive`, `ref`).  
   - Отслеживание зависимостей (dependency tracking) → эффект (`effect`) → триггеры обновлений.  
   - Подводный камень: работа с `shallowRef`, `shallowReactive`, заморозка объектов, утечки реактивности.

2. **Virtual DOM и оптимизации рендера**  
   - Vue использует Virtual DOM, но в отличие от React он **более оптимизирован**: компилятор генерирует *Patch Flags* (подсказки для рендера).  
   - Patch Flags помогают обновлять только изменившиеся узлы.  

3. **Компилятор шаблонов**  
   - `<template>` компилируется в **render-функцию**.  
   - Vue заранее решает, какие части статичны, а какие динамичны.  
   - Отличие: Vue-компилятор может *tree-shake* неиспользуемые фичи.

4. **Scheduler (планировщик обновлений)**  
   - Vue группирует несколько изменений состояния и выполняет их батчингом в **nextTick**.
   - Это предотвращает лишние перерисовки.  
   - Ошибка новичков: ожидание синхронного обновления `ref`/`reactive`.

5. **Система жизненного цикла**  
   - Хуки (`onMounted`, `onUpdated`, `onUnmounted`) строятся поверх внутренних фазы монтирования, апдейта и размонтирования.  
   - Важно понимать, что хуки вызываются в определённой последовательности → это влияет на асинхронный код.

6. **Композиционный API (Composition API)**  
   - Поверх реактивности построены `setup()`, `provide/inject`, `watch`, `computed`.  
   - Под капотом `watch` — это реактивный эффект с cleanup.  
   - `computed` реализуется как *lazy effect* (ленивая реакция).

7. **Suspense и асинхронные компоненты**  
   - Асинхронные компоненты под капотом используют Promise + Suspense.  
   - Vue ждёт `setup()` или компонент, пока они зарезолвятся.  

8. **Teleport и фрагменты**  
   - `Teleport` работает как портал: DOM узлы рендерятся в другом месте, но реактивность сохраняется.  
   - Фрагменты позволяют компоненту возвращать несколько корневых узлов.

9. **Оптимизации при сборке**  
   - Tree-shaking API (`ref`, `reactive` импортируются отдельно).  
   - `<script setup>` компилируется напрямую в `setup()` для ускорения.

---

## 3. Примеры кода

### Реактивность
```js
import { reactive, effect } from 'vue'

// объект становится реактивным
const state = reactive({ count: 0 })

effect(() => {
  console.log(`Count changed: ${state.count}`)
})

state.count++ // триггерит effect
