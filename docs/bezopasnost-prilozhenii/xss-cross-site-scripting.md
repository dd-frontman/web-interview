---
title: "XSS XSS"
description: "Это не ошибка браузера — это ошибка приложения, которое не защищает вывод данных, принимаемых из внешних источников, например из ввода пользователя."
tags:
  - "bezopasnost-prilozhenii"
  - "xss-cross-site-scripting"
updatedAt: "2026-02-16"
---
## 🧠 Что такое XSS

**XSS (Cross-Site Scripting)** — это **атака на веб-приложение**, при которой злоумышленник внедряет вредоносный скрипт в страницу сайта так, что браузер пользователя выполняет этот код, как будто он является частью сайта.

Это не ошибка браузера — это ошибка приложения, которое **не защищает вывод данных**, принимаемых из внешних источников, например из ввода пользователя.

Последствия XSS могут быть серьёзными:

- кража сессионных куки и захват пользовательской сессии
- выполнение нежелательных действий от имени пользователя
- фишинг или подмена интерфейса
- распространение вредоносного кода на посетителей сайта

---

## 🔍 Основные виды XSS атак

Существует три основных вида XSS атаки в зависимости от того, **как злоумышленник внедряет вредоносный код**:

---

### 1) **Reflected XSS (отражённый)**

При таком типе атаки вредоносный скрипт включается **в ответ сервера**, например в URL-параметр или форму. Если сервер **отражает** часть запроса в ответ без фильтрации, браузер жертвы выполнит этот скрипт.

Пример: злоумышленник делает ссылку с кодом вроде:

`https://example.com/search?q=<script>alert(1)</script>`

Если приложение вставляет `q` прямо в HTML без экранирования, браузер выполнит `alert(1)` как код.

---

### 2) **Stored XSS (хранимый или постоянный)**

Здесь вредоносный код **сохраняется на сервере**, например в комментариях, профиле пользователя или БД, а затем показывается другим пользователям.

Если уязвимость есть, злоумышленник может вставить `<script>...</script>` в комментарий, и **каждый посетитель страницы получит и выполнит этот скрипт**.

Этот тип особенно опасен, потому что он затрагивает **много пользователей**, а не одного.

---

### 3) **DOM-Based XSS**

Этот XSS возникает **уже на стороне клиента**, когда уязвимый JavaScript **обрабатывает данные из URL/DOM** и вставляет их в страницу без фильтрации. Сервер при этом даже может не участвовать.

Это распространено в современных SPA (React, Vue, Angular) при неправильной работе с данными из `location.hash`, `innerHTML`, `dangerouslySetInnerHTML` и т. п.

---

### Дополнительный тип — **Self-XSS**

Это когда жертву **обманывают, чтобы она сама выполнила вредоносный код** (например, через консоль разработчика). Сам по себе Self-XSS не всегда опасен, но может использоваться в сочетании с другими атаками.

---

## 🧠 Почему XSS опасен

В XSS злоумышленник запускает скрипт так, как будто это _родной_ код сайта, что даёт ему привилегии этого сайта в браузере пользователя. Это означает, что он может:

✔ читать cookie и sessionStorage  
✔ перемещать пользователя  
✔ показывать фальшивый UI  
✔ логировать нажатия клавиш  
✔ отправлять запросы от имени пользователя

---

## 💡 Примеры уязвимого и безопасного кода

### ❌ Небезопасный код (открытый для XSS)

HTML:

```html
<!-- Отображаем ввод пользователя без фильтрации -->
<div id="result">{ { searchQuery } }</div>
```

JavaScript (например в Vue):

```js
data() {
  return {
    searchQuery: this.$route.query.q
  }
}

```

Если `q` из URL содержит `"<script>alert('XSS')</script>"`, и Vue вставляет его напрямую в DOM как HTML, это запускается как скрипт — уязвимость XSS.

---

### ✅ Безопасный подход (Vue по умолчанию)

Vue **экранирует** содержимое при обычном использовании привязки, поэтому даже если вход содержит теги, они будут показаны как текст, а не выполнены:

`<div>{ { userInput } }</div>`

Даже при `userInput = "<script>alert('XSS')</script>"` браузер покажет его как текст, а не выполнит. Это встроенная защита Vue от XSS.

---

## ⚠️ Когда XSS может всё ещё проломать Vue

### 📍 Использование `v-html`

Команда `v-html` в Vue позволяет вставлять _сырое HTML_ в DOM:

```html
<div v-html="commentHtml"></div>
```

Если `commentHtml` содержит пользовательские данные без предварительной очистки (санитизации), это **явная XSS уязвимость** — браузер выполнит весь HTML и JS внутри.

---

### 📍 Непроверенные шаблоны

Иногда разработчики вставляют HTML через `innerHTML`, шаблоны, или сторонние библиотеки, которые не делают экранирование — это прямой путь к DOM-основным XSS.

---

## 🛡 Как защититься от XSS

Вот лучшие практики защиты:

### 📌 1) **Экранирование вывода**

Показывать данные из ввода пользователя как текст, а **не** как HTML или код. Современные фреймворки делают это по умолчанию (Vue, React).

---

### 📌 2) **Санитизация HTML**

Если нужно вставлять HTML (например, комментарии с форматированием), нужно предварительно **очистить его от опасных тегов** (например, `<script>`, `on*` атрибуты) с помощью библиотек вроде DOMPurify.

---

### 📌 3) **Content Security Policy (CSP)**

CSP позволяет браузеру блокировать выполнение скриптов, которые не разрешены политикой, даже если они внедрены. Это очень эффективный дополнительный слой защиты.

---

### 📌 4) **Валидация входных данных**

Проверка всех пользовательских данных на сервере и фронтенде: допустимые форматы, длина, типы. Это уменьшает риск внедрения опасного кода.

---

### 📌 5) **Не использовать `v-html` без необходимости**

Если возможно, используйте обычные привязки данных (`{ { ... } }`) вместо `v-html`. Это минимизирует риск XSS.

| Привязка                  | Риск XSS                   | Защита                               |
| ------------------------- | -------------------------- | ------------------------------------ |
| `{ { text } }`            | ❌ низкий                  | Vue автоматически экранирует         |
| `v-html="rawHtml"`        | ⚠️ высокий без санитизации | Нужно применять DOMPurify/санитайзер |
| Серверная реализация HTML | ↗ зависит от бекэнда       | Санитизация на сервере               |

---

## Чит-щит таблица

| Угроза / Тип XSS             | Описание                                                | Пример / Особенности                        |
| ---------------------------- | ------------------------------------------------------- | ------------------------------------------- |
| Reflected (отражённая)       | Скрипт в параметре запроса, сразу выполняется           | `?q=<script>alert()</script>`               |
| Stored (хранимая)            | Скрипт сохраняется на сервере, выполняется при загрузке | Комментарии, профили                        |
| DOM-based (на клиенте)       | JS меняет DOM напрямую с вводом от пользователя         | `document.write(location.href)`             |
| Self-XSS                     | Пользователь сам запускает скрипт под обманом           | Не является уязвимостью, но опасно          |
| double encoding              | Двойное кодирование bypass-атаков                       | `%253Cscript%253E...`                       |
| Mutated XSS (mXSS)           | Браузер изменяет код и создаёт XSS-вхождения            | Незакрытые теги, неправильно экранированные |
| Vue: unsafe `v-html`         | Вставка HTML от пользователя без очистки                | Использовать санитайзер + CSP               |
| Vue: монтирование на HTML    | Монтируется на серверный HTML с пользовоч. вводом       | Vue может интерпретировать как шаблон       |
| Защита: escaping/encoding    | Экранирование символов перед вставкой                   | HTML entity, JS-escape, CSS-escape          |
| Санитизация HTML (DOMPurify) | Очистка HTML от скриптов                                | Использовать при `v-html`                   |
| CSP                          | Политика доверенных источников                          | Запрет inline-скриптов, только безопасные   |
| HttpOnly-куки                | Куки недоступны из JS                                   | Снижение риска кражи сессии                 |
| Фреймворки                   | Автоматически экранируют вывод                          | React, Vue, Angular                         |

---

<RelatedTopics
    :items="[
        { title: 'Безопасность приложений', href: '/bezopasnost-prilozhenii/bezopasnost-prilozhenii' },
        { title: 'CSP — Content Security Policy', href: '/bezopasnost-prilozhenii/csp-content-security-policy' },
        { title: 'CSRF (Cross-Site Request Forgery)', href: '/bezopasnost-prilozhenii/csrf-cross-site-request-forgery' },
    ]"
/>
