# ⚡ Nuxt vs Vue — в чём отличия (подробно)

Nuxt — это **мета‑фреймворк** поверх Vue, который превращает Vue‑приложение в «полный стек»: SSR/SSG/ISR, файловый роутинг, серверные эндпоинты, модули, авто‑импорты, готовая структура и DevOps‑фишки.  
Vue сам по себе — это **UI‑фреймворк** (рендер компонентов + реактивность). Всё остальное вы дособираете вручную.

---

## 1) Уровень абстракции и область задач

- **Vue**
  - Отвечает за UI и реактивность: компоненты (`.vue`), `ref/reactive`, жизненный цикл.
  - Роутинг, состояние, сборка, SSR — **по вашему выбору и настройке** (Vue Router, Pinia, Vite/Webpack, собственный Node‑сервер и т. п.).

- **Nuxt**
  - Даёт «скелет» приложения и **готовые решения**: SSR/SSG/ISR, роутинг из файлов, серверные API, статическая генерация, авто‑импорты, модули (i18n, image, auth и т. д.).
  - Включает **Nitro** — кроссплатформенный серверный рантайм (Node, Serverless, Edge).

---

## 2) Роутинг: руками vs из файлов

- **Vue:** вы создаёте `router.ts`, настраиваете пути вручную:

  ```ts
  // vue + vue-router
  const routes = [{ path: "/users/:id", component: UserPage }];
  const router = createRouter({ history: createWebHistory(), routes });
  ```

- **Nuxt:** достаточно положить файл в `pages/`:
  ```
  pages/
    index.vue        -> '/'
    users/[id].vue   -> '/users/:id'
  ```
  Роутинг собирается автоматически, поддерживаются `layouts/`, `middleware/`, nested‑routes «из коробки».

---

## 3) SSR/SSG/ISR и рендеринг

- **Vue:** CSR по умолчанию. SSR/SSG — это отдельная история: нужно поднять сервер, настроить рендер, гидрацию, кеширование.

- **Nuxt:**
  - **SSR**: сервер рендерит HTML, клиент гидрируется.
  - **SSG**: статическая генерация страниц на билде.
  - **ISR**: инкрементальная регенерация отдельных страниц по правилам.
  - Готовые хуки и API для **data‑fetching на сервере** (`useAsyncData`, `useFetch`) и для **клиента**.

Пример в Nuxt (серверный prefetch + кеширование кэшем маршрута):

```ts
<script setup lang="ts">
const { data: user } = await useAsyncData('user', () => $fetch('/api/user'), {
  server: true, // загрузить на сервере, отдать готовый HTML
  dedupe: 'defer', // не дублировать параллельные запросы
});
</script>
```

---

## 4) Серверный код внутри проекта (Nitro)

- **Vue:** сервер и API вы делаете отдельно (Express/Koa/Fastify и т. п.), отдельный репозиторий/папка, свой деплой.

- **Nuxt:**
  - Папка `server/api/**` для **эндпоинтов**:
    ```ts
    // server/api/hello.get.ts
    export default defineEventHandler(() => ({ msg: "hi" }));
    ```
  - Папка `server/middleware/**` для серверных мидлвар.
  - Один репозиторий → и UI, и API → единые типы/DTO, общий деплой.
  - Адаптеры: Node, Vercel, Netlify, Cloudflare Workers (Edge) и др.

---

## 5) Структура проекта и конвенции

- **Vue:** свободная организационная структура.
- **Nuxt:** конвенции, ускоряющие разработку:
  ```
  app.vue         // корневой layout
  pages/          // маршруты
  layouts/        // переиспользуемые раскладки
  components/     // авто‑регистрация компонентов
  composables/    // авто‑импорт composables (useX)
  plugins/        // инициализация библиотек
  server/api/     // серверные эндпоинты (Nitro)
  middleware/     // роут‑мидлвары (клиент/сервер)
  assets/, public/ // статика
  nuxt.config.ts  // конфигурация и модули
  ```

---

## 6) Data‑fetching и управление данными

- **Vue:** вы выбираете: `fetch/axios`, SWR/TanStack, собственные провайдеры, SSR‑prefetch — всё на вашей совести.

- **Nuxt:** готовые инструменты:
  - `useFetch` / `useAsyncData` (сервер/клиент, дедупликация, сериализация в payload).
  - `$fetch` с типами и базовым URL из `runtimeConfig`.
  - Интеграция с кешем роутов/ISR.
  - Можно добавить **@tanstack/vue-query** — Nuxt хорошо дружит с ним (hydration, persistent‑cache).

---

## 7) Авто‑импорты, дев‑оптимизации, DX

- **Vue:** типичные импорты вручную:

  ```ts
  import { ref, computed } from "vue";
  ```

- **Nuxt:** авто‑импорты из `#imports`:
  ```ts
  // можно писать без импорта
  const count = ref(0);
  const doubled = computed(() => count.value * 2);
  ```
  Плюс авто‑регистрация компонентов из `components/`, авто‑импорт composables из `composables/`.

---

## 8) Конфигурация, модули и экосистема

- **Vue:** вы сами собираете конфиг Vite/Webpack, настраиваете плагины, окружение, i18n, PWA.

- **Nuxt:** единая точка `nuxt.config.ts` + модули:
  - `@nuxt/image`, `@nuxt/i18n`, PWA, SEO‑модули, Font‑оптимизация, SSG‑helpers и т. д.
  - **runtimeConfig** (публичные/приватные переменные), **routeRules** (пер‑маршрутные правила SSR/ISR/cache), **nitro** опции.

---

## 9) SEO, мета‑теги, head

- **Vue:** обычно вручную или через сторонние плагины.

- **Nuxt:** встроенный `useHead()` / `app.config.ts` / маршрутизируемые мета‑данные; SSR отдаёт готовый HTML с метатегами — это плюс для SEO и шаринга.

---

## 10) Деплой и адаптеры

- **Vue:** деплой — статический (SPA) или свой Node‑сервер для SSR.

- **Nuxt:** один билд можно выкатить **как SSR‑приложение**, **как статический сайт**, **как serverless**, **как edge‑функцию** — без переписывания кода (спасибо Nitro).

---

## 11) Когда что выбрать

- **Выбрать Vue**, если:
  - Нужен **легковесный SPA/виджет** без SSR и серверной части.
  - Вы хотите максимальную гибкость и минимальную «магическую» обвязку.

- **Выбрать Nuxt**, если:
  - Нужны **SSR/SSG/ISR**, SEO и быстрый TTFB.
  - Нужны **серверные эндпоинты** рядом с UI и единая типобезопасная модель.
  - Проект крупный, и важны конвенции, модули, авто‑импорты и единый DX.

---

## 12) Ключевые примеры «было/стало»

**Фетч данных:**

```ts
// Vue (CSR, вручную)
const user = ref<User | null>(null);
onMounted(async () => {
	user.value = await api.getUser();
});

// Nuxt (SSR/CSR унифицировано)
const { data: user } = await useAsyncData("user", () => $fetch<User>("/api/user"));
```

**API внутри проекта:**

```ts
// Vue: отдельный сервер (например, Express)
app.get('/api/user', ...);

// Nuxt: serverless/edge эндпоинт в монорепо
// server/api/user.get.ts
export default defineEventHandler(() => ({ id: 1, name: 'Alice' }));
```

**Роутинг:**

```ts
// Vue
createRouter({ routes: [{ path: "/about", component: About }] });

// Nuxt
// pages/about.vue -> '/about'
```

---

# 📑 Шпаргалка (коротко)

| Критерий      | Vue                           | Nuxt                                           |
| ------------- | ----------------------------- | ---------------------------------------------- |
| Роль          | UI‑фреймворк                  | Мета‑фреймворк над Vue                         |
| Роутинг       | Ручная настройка (Vue Router) | **Файловый** `pages/`                          |
| SSR/SSG/ISR   | Нужно собирать самому         | **Из коробки**                                 |
| Data‑fetching | Свободный выбор               | `useAsyncData`/`useFetch` + payload            |
| Серверные API | Отдельный сервер              | `server/api/**` (Nitro)                        |
| Авто‑импорты  | Вручную                       | **Авто** (компоненты, composables, `#imports`) |
| Конфиг/модули | Сборка по кусочкам            | `nuxt.config.ts` + **модули**                  |
| SEO/Head      | Плагины/вручную               | `useHead`, SSR‑meta из коробки                 |
| Деплой        | SPA/свой SSR                  | SSR/SSG/Edge/Serverless (адаптеры)             |
| Когда брать   | Лёгкий SPA, виджет            | SEO/SSR, крупный продукт, единый стек          |

---

<RelatedTopics
	:items="[
		{ title: 'Hydration', href: '/nuxt/hydration' },
		{ title: 'ISR - Incremental Static Regeneration', href: '/nuxt/isr-incremental-static-regeneration' },
		{ title: 'Nitro', href: '/nuxt/nitro' },
	]"
/>

