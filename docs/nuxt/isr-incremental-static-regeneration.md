## 1. Краткое объяснение

**ISR (Incremental Static Regeneration)** — это режим, в котором страница генерируется **как в SSG** (во время билда), но при этом может **обновляться на сервере через заданный интервал времени**.  
👉 То есть это «гибрид» между **SSG (быстро, но статично)** и **SSR (всегда свежо, но медленно)**.

---

## 2. Подробное объяснение

### 🔹 Как работает:

1. На этапе билда создаётся статический HTML (как в SSG).
2. Когда пользователь заходит на страницу:
   - Если кеш **ещё актуален** → отдаем готовый HTML.
   - Если кеш **устарел** → первый запрос всё равно получает старый HTML, а параллельно на сервере запускается **перегенерация страницы**.
3. Следующий пользователь уже получает обновлённую страницу.

### 🔹 Где применять:

- Когда данные меняются **периодически** (например, раз в час, день, неделю).
- Если нужен **баланс** между скоростью и свежестью данных.
- Отлично подходит для: новостных сайтов, интернет-магазинов, блогов с обновлениями.

---

## 3. Примеры кода

### 🔸 React + Next.js (ISR)

```tsx
// pages/posts/[id].tsx
import { GetStaticProps, GetStaticPaths } from "next";

type Post = { id: string; title: string; body: string };

export const getStaticPaths: GetStaticPaths = async () => {
	const posts = await fetch("https://jsonplaceholder.typicode.com/posts").then((res) => res.json());

	return {
		paths: posts.slice(0, 5).map((p: Post) => ({ params: { id: p.id.toString() } })),
		fallback: "blocking",
	};
};

export const getStaticProps: GetStaticProps = async ({ params }) => {
	const post = await fetch(`https://jsonplaceholder.typicode.com/posts/${params?.id}`).then((res) =>
		res.json()
	);

	return {
		props: { post },
		revalidate: 60, // обновляем страницу каждые 60 секунд
	};
};

export default function PostPage({ post }: { post: Post }) {
	return (
		<>
			<h1>{post.title}</h1>
			<p>{post.body}</p>
		</>
	);
}
```

➡️ Здесь страница обновляется раз в минуту, даже если сайт не пересобирали.

---

### 🔸 Vue + Nuxt (ISR)

В **Nuxt 3** встроенного ISR пока нет (как в Next.js), но его можно имитировать:

1. Использовать **on-demand revalidation** (ручной сброс кеша).
2. Или использовать модули, например [`nuxt-isr`](https://github.com/nuxt-modules/isr).

Пример с модулем:

```ts
// nuxt.config.ts
export default defineNuxtConfig({
	modules: ["nuxt-isr"],
});
```

```vue
<script setup lang="ts">
// pages/posts/[id].vue
const route = useRoute();
const id = route.params.id;

const { data: post } = await useAsyncData(`post-${id}`, () => $fetch(`/api/posts/${id}`));
</script>

<template>
	<h1>{{ post.title }}</h1>
	<p>{{ post.body }}</p>
</template>
```

```ts
// server/api/posts/[id].ts
export default defineEventHandler(async (event) => {
	const id = event.context.params?.id;
	const data = await $fetch(`https://jsonplaceholder.typicode.com/posts/${id}`);

	// Кэшируем результат на 60 секунд
	setResponseHeader(event, "x-isr", "60");
	return data;
});
```

---

## 4. Сравнение ISR vs SSG vs SSR

|                  | **SSG**                     | **ISR**                                    | **SSR**                          |
| ---------------- | --------------------------- | ------------------------------------------ | -------------------------------- |
| Когда рендерится | Один раз на билде           | На билде + периодическая регенерация       | При каждом запросе               |
| Данные           | Могут устареть              | Обновляются через `revalidate`             | Всегда свежие                    |
| Скорость         | Максимальная (готовый HTML) | Почти как SSG (кеш), обновление асинхронно | Медленнее (рендер на запрос)     |
| Нагрузка         | Минимальная                 | Средняя (только при регенерации)           | Высокая (каждый запрос = рендер) |
| Где лучше        | Блог, стат. страницы        | Новости, магазины, каталоги                | ЛК, корзины, админки             |

---

## 5. Чит-щит

| Ключ                  | Объяснение                          | Пример                     |
| --------------------- | ----------------------------------- | -------------------------- |
| **ISR**               | Гибрид между SSG и SSR              | `revalidate: 60` в Next.js |
| **Когда обновляется** | По таймеру или ручному запросу      | каждые 60 сек              |
| **Плюсы**             | Быстро, SEO, свежие данные          | Новости, каталоги          |
| **Минусы**            | Нет в Nuxt из коробки, нужен модуль | `nuxt-isr`                 |

---

## 6. Подводные камни

1. **Сложность в Nuxt**
   - В Next ISR встроен, а в Nuxt нужно использовать сторонние модули.

2. **Первый пользователь после истечения времени**
   - Может увидеть старую версию, пока идёт перегенерация.

3. **Очень часто меняющиеся данные**
   - ISR не подходит → лучше SSR.

4. **SEO и кэш**
   - Иногда поисковик может получить устаревшую страницу, если она обновляется редко.

---

<RelatedTopics
	:items="[
		{ title: 'Hydration', href: '/nuxt/hydration' },
		{ title: 'Nitro', href: '/nuxt/nitro' },
		{ title: 'Nuxt vs Vue', href: '/nuxt/nuxt-vs-vue' },
	]"
/>

