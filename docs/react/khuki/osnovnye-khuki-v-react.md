React делит хуки на 3 группы: **базовые**, **дополнительные** и **хуки для оптимизации/производительности**.  
Я перечислю все стандартные хуки, которые чаще всего встречаются в работе.

---

## 1. Базовые хуки (основа)

| Хук            | Для чего нужен                                           | Пример                                   |
| -------------- | -------------------------------------------------------- | ---------------------------------------- |
| **useState**   | Локальное состояние компонента                           | `const [count, setCount] = useState(0)`  |
| **useEffect**  | Побочные эффекты: запросы к API, подписки, таймеры       | `useEffect(() => { fetchData(); }, [])`  |
| **useContext** | Доступ к контексту (глобальные данные без проп-дрилинга) | `const theme = useContext(ThemeContext)` |

---

## 2. Хуки для работы со ссылками и DOM

| Хук                    | Для чего нужен                                                                                             | Пример                                            |
| ---------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| **useRef**             | Сохранять «ссылку» между рендерами (не вызывает перерисовку), доступ к DOM-элементу                        | `const inputRef = useRef<HTMLInputElement>(null)` |
| **useLayoutEffect**    | Эффекты, которые выполняются **синхронно после рендера**, до отрисовки на экране (например, измерения DOM) | `useLayoutEffect(() => { ... })`                  |
| **useInsertionEffect** | Для библиотек стилей (styled-components, emotion) — вставка стилей до любых layout-эффектов                | `useInsertionEffect(() => { ... })`               |

---

## 3. Хуки для оптимизации

| Хук                              | Для чего нужен                                                           | Пример                                                     |
| -------------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------- |
| **useMemo**                      | Кэшировать вычисления, чтобы не пересчитывать при каждом рендере         | `const total = useMemo(() => heavyCalc(items), [items])`   |
| **useCallback**                  | Кэшировать функцию, чтобы не создавать новую на каждый рендер            | `const onClick = useCallback(() => doSomething(id), [id])` |
| **memo** (HOC, не хук, но рядом) | Оптимизировать компонент, чтобы он не ререндерился без изменения `props` | `export default memo(MyComp)`                              |

---

## 4. Хуки для сложного состояния

| Хук            | Для чего нужен                                                    | Пример                                                     |
| -------------- | ----------------------------------------------------------------- | ---------------------------------------------------------- |
| **useReducer** | Альтернатива `useState` для сложных состояний с логикой переходов | `const [state, dispatch] = useReducer(reducer, initState)` |

---

## 5. Хуки для concurrent features (React 18+)

| Хук                  | Для чего нужен                                                                  | Пример                                                 |     |
| -------------------- | ------------------------------------------------------------------------------- | ------------------------------------------------------ | --- |
| **useTransition**    | Позволяет разделять «быстрые» и «медленные» обновления (UI остаётся отзывчивым) | `const [isPending, startTransition] = useTransition()` |     |
| **useDeferredValue** | «Откладывает» обновление значения, чтобы не тормозить интерфейс                 | `const deferredQuery = useDeferredValue(query)`        |     |
| **useId**            | Генерация уникальных id, безопасных для SSR                                     | `const id = useId()`                                   |     |

---

## 6. Хуки для ошибок и нестандартных сценариев

| Хук                     | Для чего нужен                                         | Пример                                        |
| ----------------------- | ------------------------------------------------------ | --------------------------------------------- |
| **useImperativeHandle** | Управление тем, что выдаёт `ref` наружу в `forwardRef` | `useImperativeHandle(ref, () => ({ focus }))` |
| **useDebugValue**       | Отладка кастомных хуков (видно в React DevTools)       | `useDebugValue(value)`                        |

---

## 📝 Чит-щит: Все хуки React

| Категория              | Хуки                                              |
| ---------------------- | ------------------------------------------------- |
| Базовые                | `useState`, `useEffect`, `useContext`             |
| DOM и refs             | `useRef`, `useLayoutEffect`, `useInsertionEffect` |
| Оптимизация            | `useMemo`, `useCallback`, `memo`                  |
| Состояние              | `useReducer`                                      |
| Concurrent (React 18+) | `useTransition`, `useDeferredValue`, `useId`      |
| Спец                   | `useImperativeHandle`, `useDebugValue`            |

---

📌 Итог:

- Для **состояния**: `useState`, `useReducer`.
- Для **эффектов**: `useEffect`, `useLayoutEffect`, `useInsertionEffect`.
- Для **DOM и ссылок**: `useRef`, `useImperativeHandle`.
- Для **оптимизаций**: `useMemo`, `useCallback`, `memo`.
- Для **современных фич**: `useTransition`, `useDeferredValue`, `useId`.
- Для **отладки**: `useDebugValue`.

---
