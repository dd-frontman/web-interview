# 📊 React vs Vue — Полное сравнение хуков, директив и особенностей

---

## 1) Управление состоянием

| React                                                                                   | Vue                                                                              |
| --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| **useState** — локальное состояние компонента (`const [count, setCount] = useState(0)`) | **ref** — реактивная переменная (`const count = ref(0)`)                         |
| **useReducer** — управление сложным состоянием через редьюсер                           | **reactive** — реактивный объект/массив (`const state = reactive({ count: 0 })`) |

---

## 2) Эффекты и жизненный цикл

| React                                                                           | Vue                                                               |
| ------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| **useEffect** — побочные эффекты (запросы, подписки, таймеры)                   | **onMounted**, **onUnmounted**, **watch**, **watchEffect**        |
| **useLayoutEffect** — синхронные эффекты до отрисовки браузером                 | **onMounted + nextTick()** (для измерений DOM)                    |
| **useInsertionEffect** — вставка стилей до layout-эффектов (редко используется) | Vue не имеет аналога, библиотеки стилей напрямую манипулируют DOM |

---

## 3) Контекст и DI

| React                                                  | Vue                                                                      |
| ------------------------------------------------------ | ------------------------------------------------------------------------ |
| **useContext** — доступ к данным из `Context.Provider` | **provide / inject** — внедрение зависимостей между родителем и потомком |

---

## 4) Refs и доступ к DOM

| React                                                                              | Vue                                                                          |
| ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| **useRef** — хранение значения между рендерами, доступ к DOM (`ref.current`)       | **ref()** для переменных, и `ref="el"` в шаблоне для DOM (`el.value`)        |
| **useImperativeHandle** — контроль того, что доступно через `ref` при `forwardRef` | Vue не имеет прямого аналога (обычно достаточно `ref` к компоненту/элементу) |

---

## 5) Оптимизация

| React                                             | Vue                                                                    |
| ------------------------------------------------- | ---------------------------------------------------------------------- |
| **useMemo** — кэширование вычислений              | **computed** — реактивные вычисляемые значения                         |
| **useCallback** — мемоизация функций              | Обычно не нужно — Vue автоматически оптимизирует реактивные вычисления |
| **memo** (HOC) — предотвращение лишних ререндеров | **v-memo** (Vue 3.2+) или `<KeepAlive>` для компонентов                |

---

## 6) Списки и ключи

| React                                                        | Vue                                                              |
| ------------------------------------------------------------ | ---------------------------------------------------------------- |
| Рендер: `list.map(item => <li key={item.id}>item.name</li>)` | Рендер: `<li v-for="item in list" :key="item.id">item.name</li>` |
| **key обязателен** для стабильности                          | **:key обязателен** для оптимизации                              |

---

## 7) Условный рендеринг

| React                                       | Vue                                     |
| ------------------------------------------- | --------------------------------------- |
| `flag && <Comp />` или `flag ? <A/> : <B/>` | `v-if`, `v-else-if`, `v-else`, `v-show` |
| Логика — это обычный JS                     | Специальные директивы `v-if`, `v-show`  |

---

## 8) События

| React                                      | Vue                                     |
| ------------------------------------------ | --------------------------------------- |
| `onClick={handler}`, `onChange={e => ...}` | `@click="handler"`, `@input="e => ..."` |
| События — camelCase                        | События — через `@`                     |

---

## 9) Формы и двусторонняя привязка

| React                                                | Vue                                                       |
| ---------------------------------------------------- | --------------------------------------------------------- |
| Контролируемые компоненты через `value` и `onChange` | `v-model` — автоматический биндинг значения и события     |
| Нужно вручную связывать state ↔ input                | Из коробки, работает для input, select, custom components |

---

## 10) Ключевые особенности

| React                                                         | Vue                                                                    |
| ------------------------------------------------------------- | ---------------------------------------------------------------------- |
| JSX (JavaScript + XML), вся логика в JS                       | SFC (Single File Component): `<template> + <script> + <style>`         |
| Нет директив — всё делается через JS                          | Есть директивы: `v-if`, `v-for`, `v-show`, `v-model`, `v-bind`, `v-on` |
| Hooks API для управления жизненным циклом и состоянием        | Composition API + Options API (выбор подхода)                          |
| `children` для композиции (слоты через пропсы)                | Нативные слоты `<slot>`, именованные и с пропсами                      |
| Контекст через `createContext` / `useContext`                 | DI через `provide/inject`                                              |
| Хранение глобального состояния — Redux, Zustand, Jotai и т.п. | Pinia (официальная альтернатива Vuex), простая интеграция              |

---

## 11) Современные возможности (React 18 vs Vue 3)

| React                                                          | Vue                                                                  |
| -------------------------------------------------------------- | -------------------------------------------------------------------- |
| **useTransition**, **useDeferredValue** — concurrent rendering | Vue 3 использует компилятор + реактивность, конкурентного режима нет |
| **useId** — генерация уникальных ID (SSR-friendly)             | Vue автоматически обрабатывает ключи/идентификаторы                  |
| SSR — Next.js (или Remix)                                      | SSR — Nuxt.js                                                        |

---

## 📝 Чит-щит (сводная таблица)

| Категория   | React                                                | Vue                                                |
| ----------- | ---------------------------------------------------- | -------------------------------------------------- |
| Состояние   | `useState`, `useReducer`                             | `ref`, `reactive`                                  |
| Эффекты     | `useEffect`, `useLayoutEffect`, `useInsertionEffect` | `onMounted`, `onUnmounted`, `watch`, `watchEffect` |
| Контекст/DI | `useContext`                                         | `provide/inject`                                   |
| DOM         | `useRef`, `useImperativeHandle`                      | `ref()` + `ref="el"`                               |
| Оптимизация | `useMemo`, `useCallback`, `memo`                     | `computed`, `watch`, `v-memo`                      |
| Списки      | `.map() + key`                                       | `v-for + :key`                                     |
| Условия     | `&&`, `?:`                                           | `v-if`, `v-else`, `v-show`                         |
| События     | `onClick={}`                                         | `@click=""`                                        |
| Формы       | контролируемые инпуты                                | `v-model`                                          |
| Композиция  | `children` (слоты через пропсы)                      | `<slot>` (встроенные слоты)                        |
| Архитектура | JSX + Hooks                                          | SFC + Composition API                              |
| SSR         | Next.js                                              | Nuxt.js                                            |

---

---

<RelatedTopics
	:items="[
		{ title: 'React', href: '/react/index' },
		{ title: 'JSX и его альтернативы', href: '/react/jsx-i-ego-alternativy' },
		{ title: 'Локальное состояние (реактивность)', href: '/react/lokalnoe-sostoyanie-reaktivnost' },
	]"
/>

