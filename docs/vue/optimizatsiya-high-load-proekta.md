## 🏗️ Типичный стек хайлоад проекта на Vue

- **Vue 3 + TypeScript** — высокая производительность + масштабируемость.
- **SSR с Nuxt 3** или SPA с Vite\*\* — в зависимости от требований к SEO и скорости загрузки.
- **Pinia** (вместо Vuex) — лёгкий и производительный стейт-менеджмент.
- **Code splitting + lazy loading** — уменьшение initial bundle.
- **Server-side pagination, фильтрация, сортировка** — всё тяжёлое на сервер.
- **SSR/ISR + кеш CDN** — быстрая первая загрузка.

## 🔧 Основные методы оптимизации

### 1. **Асинхронная подгрузка компонентов (lazy loading)**

```js
const LazyComponent = defineAsyncComponent(() => import("@/components/Heavy.vue"));
```

- ✅ Использовать для модальных окон, табов, админских форм и пр.
- ✅ Работает идеально с `defineAsyncComponent` или `<Suspense>`.

---

### 2. **Оптимизация рендеринга**

#### a. Использование `v-memo` (vue 3.2) и `shallowRef`

```vue
<div v-memo="[user.id]">{{ user.name }}</div>
```

- ✅ Избегает повторного рендера, если зависимости не поменялись.
- ✅ Альтернатива `shouldComponentUpdate` из React.

#### b. `shallowRef` / `markRaw`

## 🔹 `shallowRef` — «поверхностный» ref

Создаёт `ref`, который отслеживает только **саму ссылку** на объект, **без глубокой реактивности вложенных свойств**.

```js
import { shallowRef } from "vue";

const user = shallowRef({ name: "Anna", age: 30 });

user.value.age = 31; // ❌ Не вызовет обновление компонента!
user.value = { name: "Anna", age: 32 }; // ✅ Это вызовет обновление
```

### Когда использовать:

- Когда объект **часто изменяется внутри**, но тебе **не нужно следить за этими изменениями**.
- Когда используешь **большие сторонние объекты**: карты, графики, canvas, WebSocket и пр.
- Когда хочешь вручную управлять обновлением компонента.

## 🔹 `markRaw` — исключить объект из реактивности

### Что делает:

**Полностью исключает объект из реактивной системы Vue.** Все его свойства — **не реактивны**.

```js
import { markRaw, reactive } from "vue";

const socket = markRaw(new WebSocket("ws://..."));

const state = reactive({
	conn: socket,
});
```

### Когда использовать:

- Когда объект **не должен быть обёрнут Vue реактивностью** (например, `Map`, `Set`, `WebSocket`, `Chart.js`, `Pixi.Application` и т. п.).
- Когда Vue реактивность **ломает** стороннюю библиотеку или снижает производительность.

---

### 3. **Декомпозиция и виртуализация**

#### a. Разделение больших таблиц на компоненты по строкам

- Каждая строка — отдельный компонент.

#### b. Использование `vue-virtual-scroller`

```js
<RecycleScroller :items="items" :item-size="48" key-field="id">
  <template #default="{ item }">
    <RowItem :data="item" />
  </template>
</RecycleScroller>
```

`vue-virtual-scroller` — это библиотека для **виртуализации списков и таблиц** во Vue.js. Она позволяет **рендерить только видимые элементы**, а не весь массив данных, что **существенно ускоряет интерфейс** при работе с большими объёмами.

---

### 4. **Throttle / Debounce на инпуты и фильтры**

```js
import { debounce } from "lodash-es";

const search = debounce(async (q) => {
	await fetchData(q);
}, 400);
```

✅ Особенно важно при динамическом поиске, фильтрах, вводе текста.

---

### 5. **Серверная пагинация и кеширование**

- ❌ Никогда не тянуть все данные с бэка.
- ✅ Всегда работать с параметрами `limit`, `offset`, `filter`, `sort`.
- ✅ Кешировать страницы/фильтры в localStorage / IndexedDB (например, в админках).

---

### 6. **Трассировка производительности (devtools / браузер)**

#### Что мониторить:

- Components re-renders — какие часто перерисовываются и почему.
- Size of chunks — через `vite-plugin-visualizer`.
  - Чтобы понять, **какие модули и библиотеки** попадают в какой `chunk`.
  - Выявить гигантские зависимости (moment.js, lodash-es целиком, и т.д.)
  - Разделить код на более мелкие части (code splitting).

```bash
npm i -D rollup-plugin-visualizer
```

Использование с Vite:

```ts
// vite.config.ts
import { visualizer } from "rollup-plugin-visualizer";

export default defineConfig({
	plugins: [
		visualizer({ open: true }), // откроет html в браузере после сборки
	],
});
```

### Что ты увидишь:

- Интерактивная диаграмма (sunburst или treemap).
- Размеры `chunks` в gzip / brotli.
- Какие компоненты и библиотеки грузятся при начальной загрузке.

### Как оптимизировать:

- Раздели vendor chunk: `manualChunks` в `build.rollupOptions`.
- Убери тяжёлые либы (moment → dayjs, lodash → lodash-es по частям).
- Ленивая загрузка тяжёлых модулей (`import()`).

---

## 🎮 **FPS и main thread — Chrome Performance panel**

### Что отслеживать:

- **FPS (frames per second)** — ниже 50 FPS → лаги.
- **Main thread** — если он перегружен, UI тормозит.
- **Long tasks** — операции, которые блокируют поток > 50ms.

### Как смотреть:

1. Открой Chrome DevTools → вкладка `Performance`.
2. Нажми кнопку `Record`, взаимодействуй с интерфейсом.
3. Смотри:
   - График FPS (зелёный = хорошо).
   - Flame chart (таймлайны функций).
   - Long tasks (красные прямоугольники).

### Проблемы, которые можно найти:

| Проблема                 | Последствия                      |
| ------------------------ | -------------------------------- |
| Большой DOM diff         | Жесткие лаги при навигации       |
| Сложный layout / repaint | Лаг при скролле или hover        |
| Тяжёлые sync операции    | Всё "фризит", пока не завершится |
| Частые reflow            | Таблицы и формы тормозят         |

### Как исправлять:

- Разбей DOM → меньше элементов на странице.
- Используй `requestIdleCallback`, `setTimeout` для тяжёлых операций.
- Оптимизируй computed, watchers.
- Виртуализируй длинные списки (например, `vue-virtual-scroller`).

### 7. **Webpack/Vite оптимизации**

- ✅ Использовать `splitChunks`, чтобы выносить `node_modules` отдельно.
- ✅ Оптимизация `preload` и `prefetch`.
- ✅ Vite build с `build.rollupOptions.output.manualChunks`.

---

### 8. **Работа с websocket / SSE / polling**

- ✅ Использовать `shallowRef` для потоков.
- ✅ Делать батчинг обновлений в `requestIdleCallback` или `setTimeout`.

---

<RelatedTopics
	:items="[
		{ title: 'Асинхронные рендеры и батчинг', href: '/vue/asinkhronnye-rendery-i-batching' },
		{ title: 'defineExpose()', href: '/vue/defineexpose' },
		{ title: 'Директивы Vue', href: '/vue/direktivy-vue' },
	]"
/>

