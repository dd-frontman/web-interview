---
title: "provide и inject"
description: "Позволяют передавать данные глубоко вниз по дереву компонентов без props."
tags:
  - "vue"
  - "provide-i-inject"
updatedAt: "2026-02-16"
---
## 🧩 `provide` / `inject`

Позволяют **передавать данные глубоко вниз по дереву компонентов** без `props`.

- `provide()` — "даёт" значение в родительском компоненте
- `inject()` — "принимает" значение в любом потомке

```js
// Родитель
provide("user", ref("Alice"));

// Далеко в потомках
const user = inject("user");
```

---

<RelatedTopics
	:items="[
		{ title: 'Асинхронные рендеры и батчинг', href: '/vue/asinkhronnye-rendery-i-batching' },
		{ title: 'defineExpose()', href: '/vue/defineexpose' },
		{ title: 'Директивы Vue', href: '/vue/direktivy-vue' },
	]"
/>
