## 🔍 `watch` vs `watchEffect`

Обе функции следят за реактивными данными, но **по-разному**:

✅ Пример `watch`:

```ts
const count = ref(0);
watch(count, (newVal, oldVal) => {
	console.log(`count изменился с ${oldVal} на ${newVal}`);
});
```

### 👉 **`watch` легче**, потому что:

- Он **реагирует только на конкретные данные**, которые ты сам указал.
- Он **не запускается сразу** (если не задать `immediate: true`).
- Он **не следит за всем подряд**, а только за тем, что явно передали.

---

✅ Пример `watchEffect`:

```ts
const count = ref(0);
const double = ref(0);

watchEffect(() => {
	double.value = count.value * 2;
});
```

### `watchEffect` **тяжелее**, потому что:

- Запускается **сразу при монтировании**
- Vue **автоматически отслеживает все реактивные зависимости** внутри функции
- Любая реактивная переменная, даже случайно использованная, будет триггерить пересчёт

---

> [!tip] Связанные темы
>
> - [Асинхронные рендеры и батчинг](/vue/asinkhronnye-rendery-i-batching)
> - [defineExpose()](/vue/defineexpose)
> - [Директивы Vue](/vue/direktivy-vue)

