---
title: "Scheduler"
description: "👉 Scheduler — это «очередь задач», которая собирает изменения и выполняет их пачкой, а не сразу."
tags:
  - "vue"
  - "scheduler"
updatedAt: "2026-02-16"
---
**Scheduler (планировщик)** — это внутренний механизм **Vue.js**, который решает:

> **когда именно выполнять обновления реактивных данных и перерисовку DOM**,  
> **и в каком порядке**, чтобы сделать это **максимально эффективно**.

Если совсем просто:

👉 **Scheduler — это «очередь задач», которая собирает изменения и выполняет их пачкой, а не сразу.**

---

## Зачем вообще нужен scheduler?

Представь код:

```js
count.value++;
count.value++;
count.value++;
```

Без scheduler Vue бы:

- 3 раза пересчитал вычисления
- 3 раза обновил DOM

❌ медленно и неэффективно

С **scheduler**:

- Vue **замечает все изменения**
- **складывает их в очередь**
- **делает одно обновление**

✅ быстро и оптимально

## Что именно планирует scheduler в Vue 3

Scheduler управляет:

1. **effect’ами** (реактивные функции)
2. **обновлениями компонентов**
3. **watch / watchEffect**
4. **computed**
5. **DOM-патчингом**

Все они **не выполняются сразу**, а:

- попадают в **очередь**
- выполняются **в правильный момент**

---

## Как это работает по шагам

### 1️⃣ Изменяется реактивное значение

```js
state.count++;
```

### 2️⃣ Vue НЕ обновляет сразу

Вместо этого:

- помечает зависимости как “грязные”
- кладёт задачи в **scheduler queue**

---

### 3️⃣ Scheduler ждёт “подходящий момент”

Обычно это:

- **microtask** (Promise.then)
- один тик event loop

---

### 4️⃣ Scheduler запускает очередь (`flushJobs`)

- удаляет дубликаты
- сортирует задачи
- выполняет их **по приоритету**

---

### 5️⃣ Обновляется DOM ОДИН раз

---

## Важный момент: **асинхронность**

Scheduler делает обновления **асинхронными**.
Поэтому:

```js
state.count++;
console.log(el.textContent); // ❌ старое значение
```

## DOM ещё не обновился.

## Для этого существует `nextTick`

```js
state.count++;
await nextTick();
console.log(el.textContent); // ✅ новое значение
```

`nextTick` — это:

> «подожди, пока scheduler закончит все обновления»

### Scheduler и `watch`

```js
watch(source, callback, {
	flush: "pre" | "post" | "sync",
});
```

Это **прямое управление scheduler’ом**.

### `flush: 'pre'` (по умолчанию)

- вызывается **до обновления DOM**

### `flush: 'post'`

- вызывается **после обновления DOM**
- полезно для работы с DOM

### `flush: 'sync'`

- **без scheduler**
- выполняется сразу ❗
- использовать осторожно

## Scheduler и computed

`computed`:

- **не пересчитывается сразу**
- помечается как `dirty`
- пересчитывается **только когда реально нужен**

Это тоже работа scheduler’а.

## Scheduler и обновление компонентов

Каждый компонент:

- имеет **update job**
- job кладётся в очередь
- Vue гарантирует:
  - родитель обновляется **до** потомка
  - один компонент — **один апдейт за тик**

## Ключевая идея (очень важно)

> **Vue 3 = реактивность + scheduler**
> Без scheduler:

- реактивность была бы медленной
- DOM дёргался бы постоянно

Scheduler делает Vue:

- быстрым
- предсказуемым
- масштабируемым

---

## Короткое резюме

**Scheduler в Vue 3:**

- ⏳ откладывает обновления
- 📦 объединяет изменения
- 🔁 устраняет дубликаты
- 🧠 управляет порядком выполнения
- ⚡ ускоряет приложение

---

<RelatedTopics
	:items="[
		{ title: 'Асинхронные рендеры и батчинг', href: '/vue/asinkhronnye-rendery-i-batching' },
		{ title: 'defineExpose()', href: '/vue/defineexpose' },
		{ title: 'Директивы Vue', href: '/vue/direktivy-vue' },
	]"
/>
