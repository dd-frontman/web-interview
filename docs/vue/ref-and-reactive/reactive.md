---
title: "Reactive"
description: "reactive() — это функция API реактивности Vue 3, которая превращает JavaScript-объект в глубоко реактивный объект (reactive proxy)."
tags:
  - "vue"
  - "ref-and-reactive"
  - "reactive"
updatedAt: "2026-02-16"
---
## 🧠 Что такое `reactive()`

`reactive()` — это **функция API реактивности Vue 3**, которая превращает JavaScript-объект в **глубоко реактивный объект (reactive proxy)**.  
То есть Vue будет следить за **любым изменением в свойствах** этого объекта и автоматически **обновлять UI**, когда данные меняются. [Vue.js+1](https://vuejs.org/guide/essentials/reactivity-fundamentals.html)

📌 Главное:  
🟩 **“Глубокая” реактивность** — Vue отслеживает **все вложенные свойства, объекты и массивы внутри объекта**. [Vue.js](https://vuejs.org/guide/essentials/reactivity-fundamentals.html)

---

## 🧠 Почему `reactive()` нужен

Vue связывает состояние с UI, чтобы когда ты меняешь данные — компоненты обновлялись. `reactive()` позволяет:

✔ хранить объект состояния  
✔ автоматически отслеживать все читаемые/изменяемые свойства  
✔ обновлять интерфейс при изменении данных без ручного DOM-апдейта [Vue.js](https://vuejs.org/guide/essentials/reactivity-fundamentals.html)

---

## 🧠 Как это работает под капотом

Vue использует **ES 2015 Proxy** (вместо старого `Object.defineProperty` из Vue 2). Это означает:

🔹 Vue создаёт **Proxy-обёртку** вокруг объекта  
🔹 При чтении свойства Vue ловит `get` — и **регистрирует зависимость (track)**  
🔹 При записи свойства Vue ловит `set` — и **триггерит обновления (trigger)** [Medium](https://medium.com/%40ignatovich.dm/how-vue-3s-reactivity-works-under-the-hood-with-proxies-explained-simply-4593048bafdf)

И Vue делает это **глубоко для всех вложенных свойств** — не нужно вручную оборачивать вложенные структуры. [Vue.js](https://vuejs.org/guide/extras/reactivity-in-depth)

---

## 🧠 Пример использования

```js
import { reactive } from "vue";

const state = reactive({
	count: 0,
	user: {
		name: "Alice",
		age: 25,
	},
});

state.count++; // отслеживается
state.user.name = "Bob"; // тоже отслеживается
```

В этом примере:  
✔ изменения `count` — реактивны  
✔ изменения вложенного `user.name` также отслеживаются Vue — UI обновится автоматически. [Vue.js](https://vuejs.org/guide/essentials/reactivity-fundamentals.html)

---

## 🧠 Reactivity + массивы и вложенные объекты

Vue делает **глубокую реактивность**, поэтому:

```js
state.todos.push({ text: "Learn Vue 3" }); // работает
state.todos[0].done = true; // тоже работает
```

Vue следит как за самим массивом, так и за объектами внутри него. [Vue.js](https://vuejs.org/guide/essentials/reactivity-fundamentals.html)

---

## 🧠 Что `reactive()` делает неявно

### 📍 Автоматическое разворачивание ref внутри объекта

Если объект содержит поле, созданное через `ref`, Vue **автоматически разворачивает его**, когда его читают через proxy:

```js
const state = reactive({
	count: ref(0),
});
console.log(state.count); // Vue unwrap ref
```

📌 Это удобство: не нужно всегда писать `.value`. [vueframework.com](https://vueframework.com/docs/v3/ru/ru/api/basic-reactivity.html)

---

## 🧠 Важные ограничения

### ❗ Присвоение нового объекта

Если ты присваиваешь новый объект в переменную, которая ранее была реактивной через `reactive()`, то **реактивность потеряется**:

```js
let state = reactive({ a: 1 });
state = reactive({ a: 2 }); // старая связь с UI потеряна
```

👉 В таких случаях лучше использовать `ref`, если ты планируешь **полную замену объекта**. [Stack Overflow](https://stackoverflow.com/questions/65732144/vue-js-3-replace-update-reactive-object-without-losing-reactivity)

---

## 🧠 Когда использовать `reactive()` vs `ref()`

| Сценарий                                                     | Что выбрать                |
| ------------------------------------------------------------ | -------------------------- |
| Объект/сложное состояние с несколькими полями                | `reactive()`               |
| Простое примитивное значение (`number`, `string`, `boolean`) | `ref()`                    |
| Объект, который будет часто полностью заменяться             | `ref(object)`              |
| Нужно отслеживать массив                                     | `reactive()` или `ref([])` |
| Нужно реактивно заменять весь объект                         | `ref(object)`              |

---

## 🧠 Мини-пример в компоненте

```js
<script setup>
import { reactive } from 'vue';

const state = reactive({
  count: 0,
  profile: {
    name: "Alice",
    online: true
  }
});

function increment() {
  state.count++;
}
</script>

<template>
  <div>{{ state.count }}</div>
  <div>{{ state.profile.name }}</div>
  <button @click="increment">+1</button>
</template>

```

Этот код автоматически реагирует на изменения `state.count` и `state.profile.name` без дополнительной логики. [Vue.js](https://vuejs.org/guide/essentials/reactivity-fundamentals.html)

---

## 🧠 Короткое резюме

**`reactive()` — это основной способ создать глубокую реактивность в Vue 3:**  
🔸 превращает обычный JS-объект в реактивный прокси  
🔸 отслеживает все вложенные свойства  
🔸 обновляет UI автоматически при изменении данных  
🔸 работает на основе Proxy → track/trigger механизма [Vue.js+1](https://vuejs.org/guide/essentials/reactivity-fundamentals.html)

---

## 📌 Майндсет Senior

✔ Для сложных объектных состояний `reactive()` — отличное решение.  
✔ Если нужна именно реактивность изменений **на уровне свойств**, выбирай `reactive()`.  
✔ Если объект планируется **полностью заменять** часто, предпочти `ref(object)`.

---

<RelatedTopics
	:items="[
		{ title: 'Реактивность во Vue3', href: '/vue/ref-and-reactive/reaktivnost-vo-vue3' },
		{ title: 'ref vs reactive', href: '/vue/ref-and-reactive/ref-vs-reactive' },
		{ title: 'shallowReactive', href: '/vue/ref-and-reactive/shallowreactive' },
	]"
/>
