**`ref` — это способ создать _реактивное значение_.**  
Он нужен, когда ты хочешь, чтобы простые данные (число, строка, булево) или даже объект стали отслеживаемыми Vue и обновляли UI при изменении. [Vue.js](https://vuejs.org/guide/essentials/reactivity-fundamentals.html)

---

## 🛠️ Схема «под капотом» — шаги и компоненты

Вот **структура и схема** того, что происходит под капотом при создании `ref(value)`:

---

```PERL
 ┌───────────────────────────────────────┐
 │               ref(value)              │
 └───────────────────────────────────────┘
                    │
                    ▼
  ┌───────────────────────────────────────┐
  │     создаёт объект-обёртку (refObj)   │
  │   ┌───────────────────────────────┐   │
  │   │ { value: ... }                │   │
  │   │  ─ геттер/сеттер для .value   │   │
  │   └───────────────────────────────┘   │
  │                 │                     │
  │                 ▼                     │
  │   специальный механизм трекинга и     │
  │   уведомлений (track/trigger)         │
  └───────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        ▼                       ▼
┌──────────────────┐   ┌──────────────────────────┐
│ отслеживание     │   │ уведомление подписчиков  │
│ чтения `.value`  │   │ при записи `.value = ...`│
│ (track)          │   │ (trigger)                │
└──────────────────┘   └──────────────────────────┘

```

📌 Если `ref` содержит объект, то **внутри Vue вызывает `reactive()` на этом объекте**, чтобы сделать его глубоким и реактивным тоже (auto-unwrap). [GitHub](https://github.com/vuejs/vue-next/issues/2018)

---

## 🔍 Что делает геттер/сеттер

Когда ты пишешь:

`const count = ref(0); console.log(count.value);`

Vue фактически вызывает:

- **геттер `.value`** → вызывает `track(refObj, 'value')` — система **отслеживает зависимость**
- Затем, когда ты меняешь:

`count.value++;`

Сработает **сеттер `.value`** → Vue вызывает `trigger(refObj, 'value')` — **оповещает всю систему**, что это значение изменилось. [Vue.js](https://vuejs.org/api/reactivity-core)

---

## 📦 Как это работает внутри с объектами

Если ты передаёшь объект в `ref`:

`const user = ref({ name: 'Alice' });`

То под капотом Vue делает примерно так:

`ref({ … })   ↓ value → reactive({ … })`

То есть объект внутри `.value` **оборачивается в Proxy**, такой же как у `reactive()`, и **вложенные поля тоже становятся реактивными**. [GitHub](https://github.com/vuejs/vue-next/issues/2018)

---

## 📌 Почему `.value` обязателен

Потому что JavaScript **Proxy работает только с объектами** — нельзя напрямую сделать реактивным примитив (число/строку/булево). Vue **оборачивает этот примитив в объект с `.value`** и делает геттер/сеттер через референцию. [Vue.js](https://vuejs.org/api/reactivity-core)

---

## 🧠 Как `ref` взаимодействует с реактивной системой

Vue поддерживает следующие механизмы под капотом:

### 🧩 Track / Trigger

Это главный внутренний механизм реактивности (доступен не только для ref, но и для reactive):

- **track(target, key)** — когда читается значение
- **trigger(target, key)** — когда значение изменяется

Vue хранит зависимые _эффекты_ (рендер, computed, watch) в карте зависимостей (`targetMap`), ассоциируя их с объектом и ключом. Когда срабатывает trigger — Vue знает _какие эффекты перезапустить_. [This Dot Labs](https://www.thisdot.co/blog/understanding-vues-reactive-data)

---

## 📋 Схема зависимостей Vue 3

```JS
 refObj     reactiveObj
   │             │
   ▼             ▼
track()    ---- track() ----
   │             │
store in targetMap (WeakMap of deps)
   │             │
trigger()  ---- trigger() ----
   │             │
re-run effects / перерендеры

```

## 🤓 Примеры под капотом (упрощённые)

### Упрощённый псевдокод `ref`

```JS
function ref(initialValue) {
  let value = initialValue

  return {
    get value() {
      track(this, 'value') // регистрирует зависимость
      return value
    },
    set value(newVal) {
      value = newVal
      trigger(this, 'value') // оповещает
    }
  }
}

```

Это **не полный исходник Vue**, а упрощённая схема, но она отражает суть:  
↪ геттер/сеттер — это **точки входа в систему отслеживания**. [Michael Hoffmann](https://mokkapps.de/blog/ref-vs-reactive-what-to-choose-using-vue-3-composition-api)

---

## 📌 Что именно делает Vue

📍 Если `.value` — примитив → используется только геттер/сеттер для отслеживания.  
📍 Если `.value` — объект → автоматически использует `reactive()` → чистый Proxy с глубоким трекингом изменений. [GitHub](https://github.com/vuejs/vue-next/issues/2018)  
📍 Когда шаблон/эффект читает `ref.value`, Vue _где-то хранит активный эффект_, чтобы знать, что нужно перезапустить при изменении. [This Dot Labs](https://www.thisdot.co/blog/understanding-vues-reactive-data)  
📍 Vue автоматически ⚡ _unwrap’ит_ (`.value`) в шаблоне, чтобы тебе не писать `.value` в шаблоне. [PurpleSchool](https://purpleschool.ru/knowledge-base/article/ref)

---

## 🧠 Почему всё это важно

✔ `ref` даёт **реактивность единичных значений**  
✔ `.value` — интерфейс, который позволяет Vue отслеживать изменения  
✔ Объекты в `ref` становятся глубокими автоматически  
✔ Vue умеет **оптимально перестраивать UI**, триггеры зависят только от того, что реально используется

---

## 📌 Итогная схема (краткая)

```PERL
ref(value)
  ├── обёртка с .value
  ├── геттер → track
  ├── сеттер → trigger
  └── если значение — объект → вызывается reactive()
                     └── Proxy для глубокого трекинга

```

---

> [!tip] Связанные темы
>
> - [Vue](/vue)
> - [Реактивность во Vue3](/vue/ref-and-reactive/reaktivnost-vo-vue3)
> - [ref vs reactive](/vue/ref-and-reactive/ref-vs-reactive)

