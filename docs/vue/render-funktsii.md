## 🧠 Что такое **рендер-функция**

**Рендер-функция** — это **JavaScript-функция, которая описывает, как должен выглядеть UI компонента**.  
Вместо написания разметки в `<template>` ты прямо в коде возвращаешь **виртуальные DOM-узлы (VNode)** — структуры, которые Vue потом превращает в реальный DOM. [ru.vuejs.org+1](https://ru.vuejs.org/guide/extras/render-function)

📌 В Vue компоненты в конечном итоге всегда рендерятся через **render-функции**, даже если ты используешь шаблон — компилятор Vue сначала **трансформирует шаблон в render-функцию**. [ru.vuejs.org](https://ru.vuejs.org/guide/extras/rendering-mechanism)

---

## 🧠 Почему это важно

Обычно ты пишешь Vue-компоненты через `<template>` — это выглядит как HTML.  
Но под капотом Vue компилирует шаблон в **рендер-функцию**, которая создаёт дерево VNode для **виртуального DOM**. При изменении реактивных данных Vue вызывает эту функцию снова, сравнивает старое и новое дерево виртуального DOM и обновляет реальный DOM минимально и эффективно. [ru.vuejs.org](https://ru.vuejs.org/guide/extras/rendering-mechanism)

---

## 🧠 Как выглядит render-функция

### 📌 Простой пример

```js
import { h } from "vue";

export default {
	render() {
		return h("div", "Hello from render function!");
	},
};
```

Это эквивалентно:

```html
<template>
	<div>Hello from render function!</div>
</template>
```

Здесь:

- `h` — это функция, которая создаёт **виртуальный DOM-узел (VNode)**
- строки/узлы, которые ты возвращаешь, служат описанием того, как должен выглядеть UI  
   📌 Vue затем превращает это описание в реальный DOM. [vuejs.org](https://vuejs.org/guide/extras/render-function)

---

## 🧠 Где использовать render-функции

💡 Редко нужны в обычных компонентах — шаблоны хватает в 99% случаев.  
Но **рендер-функции дают больше контроля**, когда:  
✔ нужно создать UI программно (динамически)  
✔ шаблон слишком сложен для шаблонной синтаксической системы  
✔ ты строишь сильно динамические компоненты или UI-утилиты  
✔ используешь JSX вместо шаблонов

Они позволяют описывать DOM-структуру прямо в JavaScript, а не в шаблоне. [DEV Community](https://dev.to/jacobandrewsky/render-functions-in-vue-4a2g)

---

## 🧠 Что делает render-функция Vue

1. Возвращает **VNode (виртуальные DOM-узлы)** вместо строковых шаблонов.
2. Vue затем использует это дерево для рендеринга **виртуального DOM**.
3. При изменении реактивных данных функция вызывается снова, создаётся новое виртуальное дерево.
4. Vue сравнивает его со старым (diffing) и обновляет реальный DOM минимально (patching). [ru.vuejs.org](https://ru.vuejs.org/guide/extras/rendering-mechanism)

---

## 🧠 Примеры

### 📌 Нечто динамическое

```js
import { h } from "vue";

export default {
	props: ["tag"],
	render() {
		// можно динамически выбрать, какой HTML создать
		return h(this.tag, {}, "Dynamic tag!");
	},
};
```

Здесь компонент сможет рендерить разные теги в зависимости от пропса `tag`. Это сложнее сделать в обычном шаблоне. [vuejs.org](https://vuejs.org/guide/extras/render-function)

---

## 🧠 Варианты записи

### 📍 В `setup()` (Composition API)

```js
import { h } from "vue";

export default {
	setup(props) {
		return () => h("div", props.msg);
	},
};
```

Здесь ты возвращаешь **функцию**, а не объект — это и есть render-функция. Она будет вызываться несколько раз при реактивных изменениях. [ru.vuejs.org](https://ru.vuejs.org/guide/extras/render-function)

---

## 🧠 Почему Vue использует `h()`

Функция `h()` (иногда называется **hyperscript**) создаёт **VNode-объекты** — легковесные структуры, представляющие узлы виртуального DOM:

`h('div', { class: 'box' }, 'text')`

→ это объект, похожий на:

`{ type: 'div', props: { class: 'box' }, children: 'text' }`

Все Vue-рендеры потом работают с такими объектами. [vuejs.org](https://vuejs.org/guide/extras/render-function)

---

## 🧠 JSX как альтернатива

Render-функции также можно писать с помощью JSX:

```js
export default {
	render() {
		return <div>Hello JSX!</div>;
	},
};
```

JSX компилируется в вызовы `h()` за кулисами. [vuejs.org](https://vuejs.org/guide/extras/render-function)

---

## 🧠 Короткое резюме

🔹 **Render-функция** — это функция, которая описывает, как должен выглядеть UI на уровне **виртуального DOM**, и возвращает VNode-объекты. [vuejs.org](https://vuejs.org/guide/extras/render-function)  
🔹 Vue обычно генерирует их из `<template>` автоматически, но ты можешь писать их вручную для динамики и гибкости. [ru.vuejs.org](https://ru.vuejs.org/guide/extras/rendering-mechanism)  
🔹 Они дают полный контроль над рендерингом UI, так как работают на уровне JavaScript и виртуального DOM. [DEV Community](https://dev.to/jacobandrewsky/render-functions-in-vue-4a2g)

---

## 📌 Когда не нужно использовать

❌ Когда логика отображения простая  
❌ Когда шаблоны читаемее и понятнее  
❌ Когда нет необходимости в динамике, сложной логике рендера
