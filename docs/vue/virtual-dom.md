### 📌 Главная проблема реального DOM

Когда ты меняешь реальный DOM (например, изменяешь текст, классы, структуру), браузеру приходится:

- перерасчитывать layout,
- перестраивать склады,
- перерисовывать элементы.

Эти операции **дорогостоящие по производительности**, особенно если их много. [Википедия](https://en.wikipedia.org/wiki/Virtual_DOM)

### 📌 Virtual DOM помогает решить это

Vue создаёт **виртуальную копию DOM**, сравнивает её с предыдущей версией и **вычисляет только те изменения**, которые на самом деле нужны. Затем Vue обновляет **только соответствующие части реального DOM**, избегая ненужных операций. [ru.vuejs.org](https://ru.vuejs.org/guide/extras/rendering-mechanism)

Это называется:

- **diffing** — сравнение двух деревьев,
- **patching / reconciliation** — применение минимальных изменений к реальному DOM. [vuejs.org](https://vuejs.org/guide/extras/rendering-mechanism)

---

## 🧠 Как это работает по шагам (простые слова)

1️⃣ **Компиляция шаблона**  
Vue берет твой шаблон (`<template>…</template>`) и компилирует его в **функцию рендера** — функцию, которая создаёт виртуальный DOM. [vuejs.org](https://vuejs.org/guide/extras/rendering-mechanism)

2️⃣ **Монтирование (mount)**  
При первом рендере Vue вызывает эту функцию, получает **Virtual DOM tree** и строит **реальный DOM** на его основе. [vuejs.org](https://vuejs.org/guide/extras/rendering-mechanism)

3️⃣ **Реактивные изменения**  
Когда данные компонента меняются (reactivity), Vue заново запускает функцию рендера, чтобы создать **новый Virtual DOM**. [vuejs.org](https://vuejs.org/guide/extras/rendering-mechanism)

4️⃣ **Сравнение (diffing)**  
Vue сравнивает **старое** дерево Virtual DOM и **новое**. Он определяет **минимальный набор изменений**, которые нужно применить. [vuejs.org](https://vuejs.org/guide/extras/rendering-mechanism)

5️⃣ **Обновление реального DOM**  
Vue применяет (patch) только те изменения, которые действительно нужны — например, обновляет один текст или меняет атрибут. [ru.vuejs.org](https://ru.vuejs.org/guide/extras/rendering-mechanism)

---

## 🧠 Почему Vue использует Virtual DOM

✔️ **Производительность**  
Меняя не весь DOM, а только изменённые части, Vue снижает нагрузку на браузер и делает UI быстрее. [ru.vuejs.org](https://ru.vuejs.org/guide/extras/rendering-mechanism)

✔️ **Декларативный стиль**  
Ты описываешь, _что_ должно быть, а не _как_ это обновлять вручную. Vue сам решает, как эффективно обновить интерфейс. [vuejs.org](https://vuejs.org/guide/extras/rendering-mechanism)

✔️ **Универсальность**  
Virtual DOM помогает работать не только в браузере, но и в других рендерах (SSR, мобильные платформы). [DEV Community](https://dev.to/jacobandrewsky/understanding-vues-virtual-dom-108p)

---

## 🧠 Простой пример (на уровне логики)

`<p>{{ count }}</p> <button @click="count++">Increment</button>`

Что происходит под капотом?

1. Vue создаёт Virtual DOM на основе шаблона.
2. Нажимаешь кнопку — `count` увеличивается.
3. Vue создаёт **новое** Virtual DOM дерево.
4. Vue сравнивает старое и новое дерево → видит, что изменился только текст `{{ count }}`.
5. Vue обновляет **только текст в `<p>`**, а не весь DOM. [Hack Frontend](https://www.hackfrontend.com/docs/vue/vue-dom)

---

## 🧠 Что такое VNode

Virtual DOM строится из **VNode (виртуальных узлов)** — объектов JavaScript, которые описывают, что должно быть в DOM:

`{   type: "div",   props: { id: "app" },   children: [...] }`

Это просто объект — поэтому сравнивать и изменять его **гораздо быстрее**, чем реальный DOM. [vuejs.org](https://vuejs.org/guide/extras/rendering-mechanism)

---

## 🧠 Заметные преимущества

| Преимущество                       | Что это даёт                                   |
| ---------------------------------- | ---------------------------------------------- |
| **Меньше операций в реальном DOM** | Улучшенная производительность                  |
| **Автоматическое сравнение**       | Посредством diff/patch минимальные изменения   |
| **Декларативность**                | Ты описываешь UI, Vue сам решает как обновлять |
| **Более предсказуемый UI**         | Меньше багов из-за прямых DOM-манипуляций      |

---

## 🧠 Короткое резюме (простыми словами)

🧾 **Virtual DOM** — это **внутренняя структура**, которую Vue держит в памяти и которая описывает UI.  
🧾 Когда что-то меняется, Vue создаёт новый виртуальный DOM, сравнивает его со старым и применяет _только нужные изменения_ к реальному DOM.  
🧾 Это делает Vue быстрее и позволяет писать чистый декларативный код, не думая о прямых манипуляциях с DOM. [vuejs.org](https://vuejs.org/guide/extras/rendering-mechanism)

---

## 📌 Важно запомнить

💡 **Virtual DOM — это не стандарт браузера.**  
Он — **концепция**, реализуемая Vue (и другими фреймворками) для оптимизации работы с реальным DOM. [ru.vuejs.org](https://ru.vuejs.org/guide/extras/rendering-mechanism)

💡 Vue часто скрывает детали Virtual DOM от разработчика — ты обычно не смотришь на него напрямую, но пользуешься его преимуществами через шаблоны и реактивность. [vuejs.org](https://vuejs.org/guide/extras/rendering-mechanism)

---

> [!tip] Связанные темы
>
> - [Асинхронные рендеры и батчинг](/vue/asinkhronnye-rendery-i-batching)
> - [defineExpose()](/vue/defineexpose)
> - [Директивы Vue](/vue/direktivy-vue)

