---
title: "v-memo Vue 3"
description: "v-memo говорит Vue: «не пересчитывай и не перерисовывай этот кусок шаблона, если набор указанных зависимостей не изменился»."
tags:
  - "vue"
  - "direktivy-vue"
updatedAt: "2026-02-16"
---
## 🔹 Основные директивы

| Директива             | Назначение                                             |
| --------------------- | ------------------------------------------------------ |
| `v-if`                | Условный рендеринг — добавляет/удаляет элемент из DOM  |
| `v-else-if`, `v-else` | Продолжения логики `v-if`                              |
| `v-show`              | Показывает/скрывает элемент через `display: none`      |
| `v-for`               | Перебирает список и рендерит блок для каждого элемента |
| `v-bind` / `:`        | Привязывает значение к атрибуту / prop                 |
| `v-on` / `@`          | Обработка событий                                      |
| `v-model`             | Двусторонняя привязка данных                           |
| `v-slot`              | Управление слотами (в т.ч. именованными)               |
| `v-pre`               | Пропускает компиляцию шаблона                          |
| `v-once`              | Рендерит один раз и больше не обновляет                |
| `v-cloak`             | Скрывает DOM до инициализации Vue                      |
| `v-memo`              | (Vue 3.4+) Кэширует рендер элемента по зависимостям    |

## 🧩 `v-slot` (включая именованные)

### Использование:

````vue
<MyCard>
  <template v-slot:header>
    Заголовок
  </template>
  <template v-slot:default>
    Контент
  </template>
</MyCard>

## 🎯 `v-slot` — управление слотами Позволяет **вставлять контент** в слот дочернего компонента. В
Vue 3 используется как `v-slot`, чтобы быть более гибким. ```vue
<!-- Родитель -->
<MyCard>
  <template v-slot:header>
    <h1>Заголовок</h1>
  </template>
</MyCard>

<!-- MyCard.vue -->
<template>
    <div>
        <slot name="header" />
        <slot />
    </div>
</template>
````

---

## ✋ `v-pre` — отключает реактивность и компиляцию

**Пропускает компиляцию** шаблона внутри элемента. Vue не обрабатывает {{ переменные }} внутри.

- Для отображения "сырых" шаблонов
- Для оптимизации (если блок точно не изменится)

```vue
<div v-pre>Контент, который не должен обновляться</div>
// или так
<div v-pre>{{ username }}</div>
// отобразится как
<div v-pre>{{ username }}</div>
```

---

# v-memo Vue 3

---

## 1) Простое объяснение

`v-memo` говорит Vue: «не пересчитывай и не перерисовывай этот кусок шаблона, **если набор указанных зависимостей не изменился**».  
Под куском понимается поддерево DOM (элемент и его дети).

Ключевые факты:

- Работает **с Vue 3.2+**.
- Синтаксис: `v-memo="[dep1, dep2]"`. Длина массива должна быть **фиксированной**.
- `v-memo="[]"` ≈ `v-once` (никогда не обновляется).
- Полезно для **очень длинных списков** (например, `v-for` > 1000 элементов).
- При использовании с `v-for` — **на том же элементе**, где стоит `v-for`.

---

## 2) Пример из практики

### Сценарий: длинный список, подсветка выбранного элемента

```vue
<template>
    <ul>
        <li
            v-for="item in list"
            :key="item.id"
            v-memo="[item.id === selected]"
            :class="{ selected: item.id === selected }"
            @click="selected = item.id"
        >
            <strong>{{ item.title }}</strong>
            <p>{{ item.description }}</p>
        </li>
    </ul>
</template>

<script setup lang="ts">
import { ref } from "vue";

const selected = ref<number | null>(null);
const list = ref(
    Array.from({ length: 3000 }, (_, i) => ({
        id: i + 1,
        title: `Row #${i + 1}`,
        description: "…много контента и вложенных узлов…",
    }))
);
</script>

<style scoped>
.selected {
    background: #222;
    color: #fff;
}
</style>
```

👉 Здесь элемент будет перерисовываться только тогда, когда меняется **его состояние выбранности**.

---

## 3) Типичные ошибки

1. **Ставить `v-memo` не на элемент с `v-for`**  
   ❌

   ```vue
   <li v-for="item in list" :key="item.id">
     <div v-memo="[item.id === selected]">…</div>
   </li>
   ```

   ✅

   ```vue
   <li v-for="item in list" :key="item.id" v-memo="[item.id === selected]">…</li>
   ```

2. **Динамическая длина массива**  
   Должна быть постоянной.  
   ❌ `[a, b].slice(0, n)`  
   ✅ `[a, b]`

3. **Новые объекты/функции в зависимостях**  
   ❌ `v-memo="[{}]"` или `v-memo="[() => 1]"`  
   ✅ `v-memo="[somePrimitive]"`

4. **Пустой массив без понимания**  
   `v-memo="[]"` = `v-once` (узел никогда не обновится).

5. **Использование везде подряд**  
   Это микрооптимизация — нужен только в списках или тяжёлых поддеревьях.

---

## 5) Краткое резюме

- `v-memo` **мемоизирует поддерево** и пропускает обновления, если зависимости не изменились.
- `v-memo="[]"` = `v-once`.
- С `v-for` — **ставь на тот же элемент**.
- Длина массива фиксированная, зависимости должны быть простыми (числа, строки, булевы).
- Использовать точечно, чаще всего в **длинных списках**.

---

## ⏳ `v-cloak` — скрытие DOM до монтирования

Vue добавляет атрибут `v-cloak`, пока не инициализируется приложение.

```vue
<style>
[v-cloak] {
    display: none;
}
</style>

<div v-cloak>
  {{ data }}
</div>
```

🔹 Полезно при **SSR** или долгой инициализации — не показывать "сырые" шаблоны.

---

## 🎯 `v-model:arg` — передача параметра в `v-model`

Позволяет **связывать кастомное поле компонента**, не только `modelValue`.

```vue
<!-- Родитель -->
<MyInput v-model:title="bookTitle" />

<!-- MyInput.vue -->
<script setup>
defineProps(["title"]);
defineEmits(["update:title"]);
</script>

<input :value="title" @input="$emit('update:title', $event.target.value)" />
```

🔹 Так можно связывать любое свойство, а не только `modelValue`.

## 🔁 `v-bind:attr.sync` — двусторонняя передача props (устарело)

> ⚠️ `.sync` — **устаревший синтаксис** (в Vue 3 лучше использовать `v-model:custom`)

### Пример (Vue 2 стиль):

```vue
<ChildComponent :some-prop.sync="parentValue" />
```

Vue трансформирует это в:

```js
:some-prop="parentValue" @update:some-prop="val => parentValue = val"
```

---

## 🖱️ `.capture`, `.passive` — модификаторы событий

`@scroll.passive` — говорит браузеру: "не мешаю прокрутке"

```vue
<div @scroll.passive="onScroll" />
```

🔹 Улучшает производительность на мобильных

---

### `@click.capture` — обработка **на фазе перехвата**

```vue
<div @click.capture="onClick">...</div>
```

---

<RelatedTopics
    :items="[
        { title: 'Асинхронные рендеры и батчинг', href: '/vue/asinkhronnye-rendery-i-batching' },
        { title: 'defineExpose()', href: '/vue/defineexpose' },
        { title: 'Оптимизация High Load проекта', href: '/vue/optimizatsiya-high-load-proekta' },
    ]"
/>
