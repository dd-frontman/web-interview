---
title: "Циклические зависимости избежать"
description: "Симптомы: при выполнении fromA() в момент импорта b может быть undefined."
tags:
  - "arkhitektura"
  - "tsiklicheskie-zavisimosti"
updatedAt: "2026-02-16"
---
# Циклические зависимости избежать

---

## 1) Простое объяснение

**Циклическая зависимость** — это когда два (или больше) модуля **зависят друг от друга по кругу**.  
Примеры «круга»:

- `A → B → A` (две вершины)
- `A → B → C → A` (три и более)

Почему плохо:

- Ломает порядок инициализации модулей (получите `undefined`, частично инициализированные объекты).
- Усложняет сборку, tree-shaking и кэширование.
- Делает код хрупким: небольшие изменения с одной стороны ломают другую.
- В SSR/Node циклы проявляются _сильнее_, чем в браузере.

---

## 2) Как выглядит на практике (JS/TS, Vue, React)

### 2.1. JS/TS-модули

```ts
// a.ts
import { b } from "./b";
export const a = "a";
export function fromA() {
    return "A → " + b();
}

// b.ts
import { fromA } from "./a";
export function b() {
    return "B (uses " + fromA() + ")";
}
// A -> B и B -> A: цикл.
```

Симптомы: при выполнении `fromA()` в момент импорта `b` может быть `undefined`.

### 2.2. Vue (composables/feature)

```js
features/user/useUser.ts  -> импортирует features/auth/useAuth.ts
features/auth/useAuth.ts  -> импортирует features/user/useUser.ts   // цикл
```

Из-за ранних импортов один из composable получит **пустые ссылки** или «неподписанные» реактивные переменные.

### 2.3. React (hooks/contexts)

```js
contexts/UserContext.tsx -> импортирует hooks/useAuth.ts
hooks/useAuth.ts         -> импортирует contexts/UserContext.tsx   // цикл
```

При SSR порядок `require()` может привести к «полурассинхронизированному» контексту.

---

## 3) Как обнаружить циклы

- **ESLint**: `eslint-plugin-import` правило `import/no-cycle`.
- **TS/Бандлеры**: предупреждения от Vite/Webpack/Rollup про circular deps.
- **nx dep-graph / vite --debug / madge**: визуализация графа зависимостей.
- **Runtime признаки**: `undefined` вместо функции/объекта при импорте, странные ошибки инициализации.

Пример ESLint-конфига:

```js
// .eslintrc.cjs
module.exports = {
    plugins: ["import"],
    rules: {
        "import/no-cycle": ["error", { maxDepth: Infinity }],
    },
};
```

---

## 4) Стратегии разрыва циклов

### 4.1. Разделите «контракты» и «реализацию»

Вынесите общие типы/интерфейсы в **низкий слой** (shared/contracts), чтобы верхние слои не тянулись друг к другу.

```
entities/
  user/
    model.ts
    repo.ts
features/
  auth/
    service.ts      // зависит от contracts, а не от user.repo напрямую
shared/
  contracts/
    user.ts         // типы/интерфейсы UserDTO, IUserRepo
```

`auth/service` импортирует `contracts/user.ts`, а **реальную** имплементацию `IUserRepo` подсовываем через DI/фабрику — цикл исчез.

### 4.2. Внедрение зависимостей (DI) / Инверсия зависимостей

Зависимость передаём **снаружи**, а не импортируем напрямую.

```ts
// before
import { sendEmail } from "../notifications/email";
export function createUser(u) {
    /*...*/ sendEmail(u.email);
}

// after (DI)
export type Notifier = (email: string) => Promise<void>;
export function createUser(u, notify: Notifier) {
    /*...*/ return notify(u.email);
}
```

Теперь `createUser` не знает про `email`-модуль — круг порван.

### 4.3. Избегайте «barrel»-файлов, создающих круг

Barrel (`index.ts`) удобен, но может скрыть цикл.

- Либо импортируйте **конкретные** файлы (`from './ui/Button'`),
- либо держите **односторонний** barrel (только вверх по слоям), не позволяя нижним слоям импортировать его.

### 4.4. Соблюдайте **правила слоёв** (FSD/Layered)

Допускайте зависимости **только вниз**:

```
app → processes → pages → widgets → features → entities → shared
```

Если `features/cart` тянет `features/user` и наоборот — есть риск цикла.  
Выделите общую часть в `entities` или `shared`.

### 4.5. Вынесите «крест-логики» в посредника (Mediator / Pub-Sub)

Вместо прямых импортов A↔B используйте событийную шину.

```ts
// shared/bus.ts
type Events = { USER_CREATED: { id: string } };
export const bus = new EventTarget();
export const emitUserCreated = (p: Events["USER_CREATED"]) =>
    bus.dispatchEvent(new CustomEvent("USER_CREATED", { detail: p }));

// features/user -> emitUserCreated({ id })
//
// features/notifications -> подписка на bus.addEventListener('USER_CREATED', ...)
```

### 4.6. Динамический импорт (lazy) вместо «жёсткого» импорта

Если связь нужна только **во время действия**, отложите импорт:

```ts
async function openEditor() {
    const { Editor } = await import("./Editor"); // разрывает статический цикл
    new Editor().mount();
}
```

### 4.7. Выделите «третью точку»

Если `A` и `B` зависят друг от друга, возможно, вам нужен **C (service/facade)**, которая знает про оба, а `A` и `B` знают только про `C`.

```
A → Facade ← B      (A и B не знают друг о друге)
```

---

## 5) Пошаговый план рефакторинга, если цикл уже есть

1. **Найдите цикл** (ESLint/madge/dep-graph). Фиксируйте конкретные файлы.
2. **Определите направление слоёв** (где высокоуровневое, где низкоуровневое).
3. **Вынесите контракт/типы** в общий низкий слой (`shared/contracts`).
4. **Внедрите зависимость** через параметры функции/конструктора (DI), вместо прямого импорта.
5. **Уберите barrel** из «спорного» места или ограничьте его экспорт.
6. **При необходимости** — введите посредника (facade/pub-sub) или **lazy import**.
7. **Зафиксируйте результат**: включите `import/no-cycle`, добавьте регресс-тесты (e2e/unit), обновите ADR/доки.

---

## 6) Инструменты и настройки (быстрый старт)

- **ESLint**: `import/no-cycle`, `import/no-deprecated`, `import/no-extraneous-dependencies`.
- **TS path aliases**: следите, чтобы алиасы не позволяли импорт «вверх» по слою.
- **Nx/Turborepo**: `dep-graph` и `enforce-module-boundaries` (Nx) для FSD-правил.
- **Madge**: `madge src --circular` — найти циклы.
- **Architecture tests**: простые unit-проверки, запрещающие «обратные» импорты между слоями.

Пример `madge` в npm-скрипте:

```json
{
    "scripts": {
        "depcheck": "madge src --circular --extensions ts,tsx,vue"
    }
}
```

---

## 7) Частые причины циклов и как лечить

| Причина                                     | Пример                                                                          | Лечение                                                    |
| ------------------------------------------- | ------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| Взаимные импорты фич                        | `features/user` ⇄ `features/auth`                                               | Вынести общее в `entities/shared`, применить DI            |
| Barrel, который экспортирует «всё из везде» | `features/index.ts` тянет `entities/**`, а `entities/**` тянет `features/index` | Удалить/ограничить barrel, импортировать конкретные модули |
| Смешение слоёв                              | `pages` тянет `app` (вверх)                                                     | Соблюдать направление зависимостей                         |
| Единый «бог‑сервис»                         | `services/api.ts` знает и про auth, и про user, и они знают про него            | Разбить на мелкие адаптеры, вынести интерфейсы             |
| Инициализация при импорте                   | «горячие» побочные эффекты в `import`                                           | Отложить инициализацию (factory/init), lazy import         |

---

# 📑 Шпаргалка

| Что делать                                | Зачем                               |
| ----------------------------------------- | ----------------------------------- |
| Включить `import/no-cycle` и `madge`      | Автоматически ловить циклы          |
| Разделить **контракты** и **реализацию**  | Зависимости направлены вниз         |
| Применить **DI / инверсию зависимостей**  | Разорвать прямые импорты            |
| Избегать «тяжёлых» barrel‑файлов          | Баррели часто создают скрытые циклы |
| Соблюдать **правила слоёв (FSD/Layered)** | Только однонаправленные импорты     |
| Использовать mediator/pub-sub             | Убрать A↔B прямые связи             |
| Применять **dynamic import** точечно      | Разрывать статические циклы         |
| Писать ADR/правила импорта                | Чтобы команда не возвращала циклы   |

---

<RelatedTopics
    :items="[
        { title: 'Архитектура приложений — виды и особенности', href: '/arkhitektura/arkhitektura-prilozhenii-vidy-i-osobennosti' },
        { title: 'Domain-Driven Design', href: '/arkhitektura/domain-driven-design' },
        { title: 'Feature-Sliced Design', href: '/arkhitektura/feature-sliced-design' },
    ]"
/>
