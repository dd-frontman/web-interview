---
title: "Микросервисы фронтенде бэкенде"
description: "Фронтенд обычно работает как SPA/SSR-приложение и общается с микросервисами через API (REST/GraphQL/gRPC)."
tags:
  - "arkhitektura"
  - "mikroservisy"
updatedAt: "2026-02-16"
---
# Микросервисы фронтенде бэкенде

---

## 1) Что такое микросервисная архитектура

**Микросервисы** — это подход, при котором приложение разделяется на **набор независимых сервисов** (каждый отвечает за свою бизнес-область).  
Фронтенд обычно работает как SPA/SSR-приложение и общается с микросервисами через API (REST/GraphQL/gRPC).

---

## 2) Особенности

- Каждый сервис автономен: свой код, база данных, деплой.
- Сервисы общаются друг с другом по сети.
- Фронтенд — отдельное приложение, которое собирает данные с разных сервисов.

---

## 3) Пример структуры проекта

```
platform/
  services/
    auth-service/
      src/
        controllers/
        models/
        routes/
      Dockerfile
    orders-service/
      src/
        controllers/
        models/
        routes/
      Dockerfile
    catalog-service/
      src/
      Dockerfile
  frontend/
    apps/web/        # SPA/SSR (Vue, React, Nuxt, Next)
      src/
        components/
        pages/
        store/
        services/    # клиенты для микросервисов
    packages/ui/     # общие компоненты
    packages/utils/  # хелперы
  gateway/           # API-gateway (объединяет микросервисы для фронта)
    nginx.conf
  docker-compose.yml
```

---

## 4) Мини-пример (backend микросервис)

### Auth Service (Node + Express)

```ts
// services/auth-service/src/routes.ts
import express from "express";
const router = express.Router();

router.post("/login", (req, res) => {
    // проверка логина/пароля
    res.json({ token: "jwt-token" });
});

router.get("/me", (req, res) => {
    res.json({ id: 1, name: "Alice" });
});

export default router;
```

### Orders Service

```ts
// services/orders-service/src/routes.ts
import express from "express";
const router = express.Router();

router.get("/", (req, res) => {
    res.json([{ id: 100, product: "Phone", qty: 1 }]);
});

export default router;
```

---

## 5) API Gateway

Часто используют **API-шлюз** (nginx, Kong, GraphQL-gateway), чтобы фронт обращался в одну точку:

```
/api/auth/*  → auth-service:3001
/api/orders/* → orders-service:3002
/api/catalog/* → catalog-service:3003
```

---

## 6) Фронтенд (Vue/React) для микросервисов

### Vue пример

```ts
// frontend/apps/web/src/services/api.ts
export const api = {
    login: (data) => fetch("/api/auth/login", { method: "POST", body: JSON.stringify(data) }),
    me: () => fetch("/api/auth/me").then((r) => r.json()),
    orders: () => fetch("/api/orders").then((r) => r.json()),
};
```

### React пример

```tsx
// frontend/apps/web/src/pages/Orders.tsx
import { useEffect, useState } from "react";
import { api } from "../services/api";

export function Orders() {
    const [orders, setOrders] = useState<any[]>([]);

    useEffect(() => {
        api.orders().then(setOrders);
    }, []);

    return (
        <ul>
            {orders.map((o) => (
                <li key={o.id}>
                    {o.product} × {o.qty}
                </li>
            ))}
        </ul>
    );
}
```

---

## 7) Плюсы и минусы

**Плюсы:**

- Масштабируемость: сервисы развиваются независимо.
- Удобно для больших команд (каждая отвечает за свой сервис).
- Надёжность: сбой в одном сервисе ≠ падение всей системы.
- Можно использовать разные стеки (Node.js, Go, Java).

**Минусы:**

- Сложная инфраструктура (Docker, Kubernetes, CI/CD, мониторинг).
- Сложность коммуникации между сервисами.
- Требует API Gateway, сервис-дискавери.
- DevOps-нагрузка выше, чем у монолита.

---

## 8) Когда использовать

✅ Да:

- Крупные системы (e-commerce, маркетплейсы, SaaS).
- Когда есть несколько команд и бизнес-доменов.
- Если важна масштабируемость по нагрузке (отдельный сервис можно вынести в отдельный кластер).

❌ Нет:

- Малые/средние проекты, MVP → overhead будет выше пользы.

---

# 📑 Шпаргалка: Микросервисы + фронтенд

| Характеристика  | Монолит                      | Микросервисы                        |
| --------------- | ---------------------------- | ----------------------------------- |
| Кодовая база    | Одна                         | Несколько (каждый сервис отдельно)  |
| Масштабирование | Горизонтально всё приложение | Каждый сервис отдельно              |
| Деплой          | Один                         | Независимый                         |
| Надёжность      | Сбой ломает всё              | Сбой затрагивает только сервис      |
| Фронтенд        | Один SPA/SSR                 | SPA/SSR + общение через API Gateway |
| Подходит        | Малые/средние проекты        | Большие системы, много команд       |

---

<RelatedTopics
    :items="[
        { title: 'Архитектура приложений — виды и особенности', href: '/arkhitektura/arkhitektura-prilozhenii-vidy-i-osobennosti' },
        { title: 'Domain-Driven Design', href: '/arkhitektura/domain-driven-design' },
        { title: 'Feature-Sliced Design', href: '/arkhitektura/feature-sliced-design' },
    ]"
/>
