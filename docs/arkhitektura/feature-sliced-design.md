**FSD** — это архитектурная методология для фронтенд-приложений, которая помогает организовать код так, чтобы его было легко поддерживать, масштабировать и понимать. Основная идея — разделение приложения на изолированные части по **бизнес-логике** и **техническому назначению**.

FSD не привязан к конкретному фреймворку. Поэтому примеры ниже показаны и для Vue, и для Next.js как альтернативы. В одном реальном проекте обычно выбирают один стек, а не смешивают `App.vue` и `page.tsx` в одном дереве.

---

## 🏗️ Уровни (Слои) FSD

Слои идут сверху вниз — от самых близких к пользователю до общих низкоуровневых модулей. Важное правило: **модуль внутри слайса может зависеть только от модулей на слоях строго ниже**. Исключение: слои `app` и `shared` не делятся на слайсы, поэтому их сегменты могут импортировать друг друга.

### 1. `app` — Инициализация приложения

Здесь настраивается всё приложение: роутинг, провайдеры, глобальные стили.
**Пример для Vue (main.ts):**

```typescript
// main.ts
import { createApp } from "vue";
import App from "./app/App.vue";
import { router } from "./app/providers/router";

createApp(App).use(router).mount("#app");
```

---

### 2. `pages` — Страницы приложения

Каждый файл — отдельная страница. Страницы собирают виджеты и фичи.

**Пример для Next.js (App Router + re-export из FSD-слоя `pages`):**

```typescript
// app/catalog/page.tsx
export { CatalogPage as default, metadata } from "@/pages/catalog";

```

```typescript
// src/pages/catalog/index.ts
export { CatalogPage } from "./ui/catalog-page";
export { metadata } from "./config/metadata";
```

**Пример для Vue (pages/):**

```vue
<!-- pages/CatalogPage.vue -->
<template>
	<div>
		<ProductList />
		<Filters />
	</div>
</template>
```

---

### 3. `widgets` — Крупные составные блоки

Виджеты комбинируют несколько фич и entities. Например, шапка сайта или виджет корзины.

**Пример (widgets/Header):**

```vue
<!-- widgets/Header/Header.vue -->
<template>
	<header>
		<Logo />
		<Navigation />
		<UserProfile />
	</header>
</template>
```

```typescript
// widgets/Header/index.ts
export { default as Header } from "./Header.vue";
```

---

### 4. `features` — Пользовательские функции

Фичи реализуют конкретную бизнес-возможность. Например, "лайкнуть пост" или "добавить в корзину".

**Пример (features/AddToCart):**

```vue
<!-- features/AddToCart/AddToCart.vue -->
<template>
	<button @click="addToCart">Добавить в корзину</button>
</template>

<script setup>
const props = defineProps(["productId"]);

const addToCart = () => {
	// Логика добавления
};
</script>
```

---

### 5. `entities` — Бизнес-сущности

Сущности описывают основные понятия предметной области: `User`, `Product`, `Order`.

**Пример (entities/Product):**

```vue
<!-- entities/Product/ProductCard.vue -->
<template>
	<div class="product-card">
		<h3>{{ product.name }}</h3>
		<p>{{ product.price }}</p>
	</div>
</template>

<script setup>
defineProps(["product"]);
</script>
```

```typescript
// entities/Product/model/types.ts
export interface Product {
	id: string;
	name: string;
	price: number;
}
```

---

### 6. `shared` — Переиспользуемый код

Всё, что можно использовать в любом месте приложения: UI-кит, утилиты, API.

**Пример (shared/ui/):**

```vue
<!-- shared/ui/Button/Button.vue -->
<template>
	<button class="btn" :class="variant">
		<slot />
	</button>
</template>

<script setup>
defineProps({
	variant: { type: String, default: "primary" },
});
</script>
```

```typescript
// shared/api/base.ts
export const apiClient = axios.create({
	baseURL: "/api",
});
```

---

<a id="folder-structure-vue-next"></a>
## 📁 Пример структуры папок для Vue/Next.js

Короткий якорь для ссылки: `#folder-structure-vue-next`.

### Вариант 1: Vue (Vite + Vue Router)

```text
src/
├── app/                              # Инициализация приложения
│   ├── providers/                    # router, store, i18n
│   ├── styles/                       # Глобальные стили
│   ├── App.vue
│   └── main.ts
├── pages/
│   ├── catalog/
│   │   ├── ui/
│   │   │   └── catalog-page.vue
│   │   └── index.ts
│   └── product/
│       ├── ui/
│       │   └── product-page.vue
│       └── index.ts
├── widgets/
│   ├── header/
│   └── footer/
├── features/
│   ├── add-to-cart/
│   └── product-search/
├── entities/
│   ├── product/
│   └── user/
└── shared/
    ├── ui/
    ├── lib/
    └── api/
```

### Вариант 2: Next.js (App Router)

```text
app/                                  # Роутинг Next.js (root)
├── catalog/
│   └── page.tsx                      # re-export из src/pages/catalog
└── product/
    └── [id]/
        └── page.tsx

pages/                                # Пустая папка для App Router (см. FSD docs)
└── README.md

src/
├── app/
│   ├── providers/
│   └── styles/
├── pages/
│   ├── catalog/
│   │   ├── ui/
│   │   │   └── catalog-page.tsx
│   │   ├── config/
│   │   │   └── metadata.ts
│   │   └── index.ts
│   └── product/
│       ├── ui/
│       │   └── product-page.tsx
│       └── index.ts
├── widgets/
├── features/
├── entities/
└── shared/
```

---

## 🔄 Как работают зависимости между слоями

Правила FSD запрещают "горизонтальные" и "обратные" зависимости:

- **Можно**: `pages` → `widgets` → `features` → `entities` → `shared`
- **Нельзя**: импортировать соседний слайс на том же слое (например `features/a` → `features/b`)
- **Нельзя**: `shared` → `entities` → `features` (снизу вверх)

Это обеспечивает предсказуемость и предотвращает циклические зависимости.

---

## 💡 Преимущества FSD

1. **🍰 Чёткая структура** — Легко ориентироваться в проекте
2. **🚀 Масштабируемость** — Новые фичи добавляются без боли
3. **🛡️ Изоляция** — Изменения в одном слайсе не ломают другие
4. **♻️ Переиспользование** — `shared` и `entities` используются везде
5. **🧪 Тестируемость** — Каждый модуль можно тестировать отдельно

---

## 🛠️ Как внедрить FSD в существующий проект

1. Начните с выделения `shared` слоя (UI-кит, утилиты)
2. Выделите основные бизнес-`entities`
3. Перенесите функциональность в `features`
4. Соберите из этого `widgets`
5. Организуйте `pages` используя виджеты и фичи

FSD не требует рефакторинга всего проекта сразу — можно внедрять постепенно, слой за слоем.

Эта архитектура отлично подходит для средних и крупных проектов на Vue, Next.js или любом другом фреймворке!

## Официальные источники

- Overview: https://feature-sliced.design/docs/get-started/overview
- Layers: https://feature-sliced.design/docs/reference/layers
- Next.js (App Router): https://feature-sliced.design/docs/guides/tech/with-nextjs

---

> [!tip] Связанные темы
>
> - [Архитектура приложений — виды и особенности](/arkhitektura/arkhitektura-prilozhenii-vidy-i-osobennosti)
> - [Domain-Driven Design](/arkhitektura/domain-driven-design)
> - [Архитектура](/arkhitektura/index)

