# 🔐 Основные темы безопасности приложений

## 1. Аутентификация и авторизация
- **Аутентификация (AuthN)** — «кто ты?»: пароль, 2FA, OAuth, SSO.
- **Авторизация (AuthZ)** — «что тебе можно?»: RBAC, ABAC, ACL.
- Типичные ошибки:
  - Хранение паролей в открытом виде.
  - Долгоживущие или неподписанные JWT.
  - Проверка прав только на фронтенде (нет валидации на бэке).
---
## 2. Управление сессиями
- HttpOnly/Secure cookie для идентификатора сессии.
- Истечение срока действия, ротация токенов.
- Защита от угонов сессии (Session Hijacking).
- Logout → инвалидировать серверное состояние/refresh-токены.
---
## 3. Валидация и санитизация данных
- Двухсторонняя проверка: на фронте (UX) и на бэке (безопасность).
- Санитизация/экранирование входа, особенно для HTML/SQL/LDAP.
- Белые списки (разрешаем только допустимое), а не чёрные.
---
## 4. Типовые атаки и защита
- **XSS (Cross-Site Scripting)**  
  Внедрение JS-кода в страницу.  
  _Защита:_ экранирование, отказ от небезопасного рендера (`v-html`, `dangerouslySetInnerHTML`), CSP.
- **CSRF (Cross-Site Request Forgery)**  
  Заставляет пользователя сделать нежелательный запрос.  
  _Защита:_ SameSite cookies, CSRF-токены, проверка Origin/Referer.
- **SQL/NoSQL Injection**  
  Вставка кода в запрос к БД.  
  _Защита:_ подготовленные выражения, ORM, строгая валидация типов.
- **RCE (Remote Code Execution)**  
  Удалённый запуск кода на сервере.  
  _Защита:_ не выполнять пользовательский ввод, песочницы, изоляция.
- **Directory Traversal**  
  Неавторизованный доступ к файловой системе.  
  _Защита:_ нормализация путей, строгая карта разрешённых директорий.
---
## 5. Безопасность API
- Корректный **CORS** (минимальный список источников, запретов и заголовков).
- **Rate limiting**, защита от brute-force и DoS.
- Валидация схемы запросов/ответов (OpenAPI, zod, Joi).
- GraphQL: ограничение глубины/сложности запросов, persisted queries.
- Не публиковать секреты в Swagger/OpenAPI.
---
## 6. Безопасность передачи данных
- **HTTPS (TLS 1.2+)** везде, редиректы на HTTPS.
- **HSTS** для принудительного HTTPS.
- Шифрование конфиденциальных данных «в полёте» и «в покое» (at rest).
---
## 7. Логирование и мониторинг
- Логи доступа, ошибок, аутентификации (с примерной маскировкой).
- Не логировать секреты (пароли, токены, номера карт).
- Алертинги, дашборды, выявление аномалий (SIEM, IDS/IPS).
---
## 8. DevSecOps / безопасная разработка
- Сканы зависимостей и уязвимостей (npm audit, Snyk, Dependabot).
- Линтеры/правила безопасности (eslint-plugin-security).
- Встраивание проверок в CI/CD (SAST/DAST, контейнерные сканеры).
- Регулярные обновления и патчи, контроль версий секретов (Vault/Sealed Secrets).
---
## 9. Архитектурные вопросы
- Принцип наименьших привилегий (PoLP).
- Разделение и изоляция сервисов (микросервисы, sandboxing).
- Zero Trust: не доверять сетевым периметрам по умолчанию.
- Безопасные значения по умолчанию (fail-safe defaults), явные deny-листы.
---
## 10. Практические советы
- Никогда не доверяй пользовательскому вводу.
- Валидация и авторизация — **на сервере** (фронт — лишь UX).
- Периодические пентесты и сканирование (OWASP ZAP, Burp Suite).
- Изучи и отслеживай **OWASP Top 10**.
---
## Карта типовых вопросов на собеседованиях

| Тема | Что спрашивают |
|---|---|
| Аутентификация | Как хранить пароли? bcrypt vs argon2. Что такое 2FA/SSO/OAuth? |
| Авторизация | Разница RBAC/ABAC, где и как проверять права (бэк/фронт/БД)? |
| Сессии | Cookie vs JWT. Где хранить токены? HttpOnly, SameSite, refresh-логика. |
| XSS | Типы XSS, защита во Vue/React, CSP, экранирование. |
| CSRF | Как работает и как защититься в SPA/SSR, cookie+SameSite, CSRF-токены. |
| Инъекции | Подготовленные выражения, ORM, валидация входа. |
| HTTPS/HSTS | Зачем, как включить, какие уязвимости закрывает. |
| CORS | Механика preflight, минимальные разрешения, риски. |
| Зависимости | npm audit/Snyk, политика обновлений, lock-файлы. |
| CI/CD | SAST/DAST, секреты, сканы контейнеров, политика выпусков. |
