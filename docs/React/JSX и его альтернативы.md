В React **по умолчанию** рендер идёт через JSX, но он не единственный вариант — можно использовать и другие шаблонные подходы.  

---

## 🔹 1. **Чистый JavaScript (без JSX)**
React под капотом всё равно вызывает `React.createElement`, поэтому можно писать так:
```js
import React from "react";

export default function App() {
  return React.createElement(
    "div",
    { className: "wrapper" },
    React.createElement("h1", null, "Привет, мир!")
  );
}
```
📌 **Плюсы:** не нужен транспилятор (Babel).  
📌 **Минусы:** читать и писать тяжело — громоздко.

---

## 🔹 2. Синтаксис через функции / hyperscript**
Вместо JSX можно использовать библиотеки в стиле **hyperscript**:
```js
import { h } from "react-hyperscript";

export default function App() {
  return h("div.wrapper", [
    h("h1", "Привет"),
    h("button", { onClick: () => alert("Клик!") }, "Нажми")
  ]);
}
```
📌 **Плюсы:** можно писать без сборщика JSX, гибко.  
📌 **Минусы:** менее читаемо для больших компонентов.

---

## 🔹 3. **Template strings (react-template, lit-html)**
React сам не поддерживает template-строки как в Vue, но можно подключить сторонние решения:
```js
import html from "nanohtml";

export default function App() {
  return html`<div><h1>Привет</h1></div>`;
}
```
📌 **Плюсы:** ближе к привычным HTML-шаблонам.  
📌 **Минусы:** это уже сторонние библиотеки, не стандарт React.

---

## 🔹 4. **JSX-подобный синтаксис через TypeScript’s TSX**
Если проект на TypeScript, то расширение `.tsx` даёт ту же мощь JSX, но с типами.  
```tsx
type Props = { title: string };

const App = ({ title }: Props) => <h1>{title}</h1>;
```
📌 **Плюсы:** строгая типизация, защита от ошибок.  
📌 **Минусы:** нужен TypeScript.

---

## 🔹 5. **Рендер-функции без JSX**
Можно описывать разметку просто функциями и условиями:
```js
function App() {
  const isLoggedIn = true;
  return isLoggedIn
    ? React.createElement("h1", null, "Добро пожаловать")
    : React.createElement("h1", null, "Войдите");
}
```
📌 **Плюсы:** полный контроль логики рендера.  
📌 **Минусы:** теряется визуальная наглядность.

---

💡 **Вывод:**  
- В 90% случаев в React используют **JSX/TSX**.  
- Если нужна работа без сборщика (Babel/Webpack) или в средах вроде Node.js, можно рендерить **через чистый `React.createElement`** или hyperscript-подход.  
- Vue-шных шаблонов «по умолчанию» в React нет — это концепция фреймворка Vue, а React изначально заточен под декларативный JS.