## 1. Краткое объяснение

SSG — это когда страница вашего приложения **генерируется один раз на этапе билда** и сохраняется в виде **готового HTML-файла**. Пользователь сразу получает статический HTML, а затем подключается JavaScript для оживления интерфейса.

---

## 2. Подробное объяснение

### 🔹 Как работает:

1. На этапе **билда** (например, `npm run build`) фреймворк вызывает серверную функцию и подставляет данные.
2. Результат рендера — готовый HTML + JS.
3. На продакшене сервер просто **отдаёт статический файл** (нет обращения к базе или API на сервере при каждом запросе).
4. После загрузки браузер «гидрирует» страницу, превращая её в SPA.

### 🔹 Где применять:

- Маркетинговые сайты, блоги, документация, лендинги.
- Когда данные редко меняются.
- Когда важен **SEO** и **скорость первой загрузки**.

### 🔹 Отличия:

- **CSR (Client-Side Rendering):** данные подтягиваются только в браузере, до этого пустая страница или "Loading...".
- **SSR (Server-Side Rendering):** HTML генерируется на сервере при каждом запросе.
- **SSG:** HTML генерируется **заранее**, а потом кэшируется.

---

## 3. Примеры кода

### 🔸 Vue + Nuxt (SSG)

```ts
// nuxt.config.ts
export default defineNuxtConfig({
	ssr: true, // обязательно включён SSR
	nitro: {
		preset: "static", // указываем, что билдим в статике
	},
	generate: {
		routes: ["/blog/1", "/blog/2"], // статические маршруты
	},
});
```

```vue
<!-- pages/blog/[id].vue -->
<script setup lang="ts">
const route = useRoute();
const id = route.params.id;

// В Nuxt можно использовать useAsyncData для prefetch на этапе билда
const { data: post } = await useAsyncData(`post-${id}`, () => $fetch(`/api/posts/${id}`));
</script>

<template>
	<h1>{{ post.title }}</h1>
	<p>{{ post.content }}</p>
</template>
```

---

### 🔸 React + Next (SSG)

```tsx
// pages/blog/[id].tsx
import { GetStaticPaths, GetStaticProps } from "next";

type Post = { id: string; title: string; content: string };

export const getStaticPaths: GetStaticPaths = async () => {
	// Пути генерируются на этапе билда
	const posts = await fetch("https://jsonplaceholder.typicode.com/posts").then((res) => res.json());

	return {
		paths: posts.slice(0, 5).map((p: Post) => ({ params: { id: p.id.toString() } })),
		fallback: false, // false = 404 для несуществующих путей
	};
};

export const getStaticProps: GetStaticProps = async ({ params }) => {
	const post = await fetch(`https://jsonplaceholder.typicode.com/posts/${params?.id}`).then((res) =>
		res.json()
	);

	return { props: { post } };
};

export default function BlogPage({ post }: { post: Post }) {
	return (
		<>
			<h1>{post.title}</h1>
			<p>{post.content}</p>
		</>
	);
}
```

---

## 4. Сравнение Vue vs React (Nuxt vs Next)

|           | **Nuxt (Vue)**            | **Next (React)**                    |
| --------- | ------------------------- | ----------------------------------- |
| Настройка | `nitro.preset = 'static'` | `getStaticProps` / `getStaticPaths` |
| Данные    | `useAsyncData`            | `getStaticProps`                    |
| Роутинг   | Автоматический (pages/)   | Автоматический (pages/)             |
| Динамика  | `generate.routes`         | `getStaticPaths`                    |
| Fallback  | Нет, только вручную       | `fallback: true / blocking / false` |

---

## 5. Чит-щит

| Ключ                   | Объяснение                              | Пример                          |
| ---------------------- | --------------------------------------- | ------------------------------- |
| **SSG**                | Генерация HTML на этапе билда           | `next build` → готовые страницы |
| **Nuxt**               | `nitro.preset = 'static'`               | `generate.routes`               |
| **Next**               | `getStaticProps` + `getStaticPaths`     | блог, документация              |
| **Когда использовать** | SEO, скорость, редкие изменения         | блог, лендинг                   |
| **Fallback**           | Что делать с несуществующими страницами | `fallback: false/true/blocking` |

---

## 6. Подводные камни и ошибки

1. **Редкие обновления данных**
   - Данные устаревают, если не использовать **ISR (Incremental Static Regeneration)** в Next или `nuxt build` заново.
   - Решение: в Next → `revalidate`, в Nuxt → Nuxt Content или regenerate.

2. **Много страниц**
   - Если у вас 100k+ страниц, билд будет долгим.
   - В Next помогает ISR (`revalidate`), в Nuxt — `on-demand revalidation`.

3. **Динамические данные**
   - Если данные часто меняются (например, цены), SSG не подходит → лучше SSR или CSR.

4. **Fallback** (только в Next)
   - `false` → быстрый билд, но только заранее известные пути.
   - `true` → генерирует новые страницы на лету.
   - `blocking` → пользователь ждёт, пока страница соберётся.
