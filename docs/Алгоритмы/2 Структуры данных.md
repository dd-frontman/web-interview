# 🏗️ Структуры данных

> [!tip] Связанные темы
>
> - [[Программирование/JavaScript/JavaScript|JavaScript]]
> - [[Алгоритмы|Алгоритмы]]
> - [[blank/Эффективное обучение|Эффективное обучение]]
> - [[Подготовка к собеседованию|Подготовка к собеседованию]]

## 🎯 Что такое структуры данных

**Структура данных** — это способ организации и хранения информации в компьютере для эффективного доступа и изменения. Это как разные типы контейнеров для разных вещей.

## 🔤 Примитивные типы

### **Числа**

```javascript
// Целые числа
let integer = 42;

// Числа с плавающей точкой
let float = 3.14;

// BigInt для больших чисел
let bigInt = 9007199254740991n;

// Операции
let sum = 5 + 3; // 8
let product = 4 * 2; // 8
let power = 2 ** 3; // 8
```

### **Строки**

```javascript
let name = "John";
let message = "Hello World";
let template = `Hello, ${name}!`;

// Методы
let upper = name.toUpperCase(); // "JOHN"
let length = name.length; // 4
let substring = name.slice(0, 2); // "Jo"
```

### **Булевы значения**

```javascript
let isTrue = true;
let isFalse = false;

// Логические операции
let and = true && false; // false
let or = true || false; // true
let not = !true; // false
```

### **Специальные значения**

```javascript
let nothing = null; // отсутствие значения
let undefined; // неопределенное значение
let symbol = Symbol("id"); // уникальный идентификатор
```

## 📦 Составные структуры

### **Массивы (Arrays)**

```javascript
let fruits = ["Apple", "Banana", "Orange"];

// Доступ к элементам
let first = fruits[0]; // "Apple"
let last = fruits[fruits.length - 1]; // "Orange"

// Методы
fruits.push("Grape"); // добавить в конец
fruits.unshift("Lemon"); // добавить в начало
fruits.pop(); // удалить последний
fruits.shift(); // удалить первый

// Итерация
fruits.forEach((fruit) => console.log(fruit));
let upperFruits = fruits.map((fruit) => fruit.toUpperCase());
```

### **Объекты (Objects)**

```javascript
let person = {
	name: "John",
	age: 30,
	city: "New York",

	sayHello() {
		return `Hello, I'm ${this.name}`;
	},
};

// Доступ к свойствам
let name = person.name; // точечная нотация
let age = person["age"]; // скобочная нотация
let greeting = person.sayHello(); // вызов метода

// Добавление/изменение
person.job = "Developer";
person.age = 31;
```

### **Map**

```javascript
let userMap = new Map();

// Добавление элементов
userMap.set("name", "John");
userMap.set("age", 30);
userMap.set(1, "one");

// Получение значений
let name = userMap.get("name"); // "John"
let hasAge = userMap.has("age"); // true

// Итерация
userMap.forEach((value, key) => {
	console.log(`${key}: ${value}`);
});
```

### **Set**

```javascript
let uniqueNumbers = new Set();

// Добавление элементов
uniqueNumbers.add(1);
uniqueNumbers.add(2);
uniqueNumbers.add(2); // дубликат не добавится

// Проверка
let hasOne = uniqueNumbers.has(1); // true
let size = uniqueNumbers.size; // 2

// Преобразование в массив
let array = [...uniqueNumbers]; // [1, 2]
```

## 📚 Линейные структуры

### **Стек (Stack)**

```javascript
class Stack {
	constructor() {
		this.items = [];
	}

	push(element) {
		this.items.push(element);
	}

	pop() {
		if (this.isEmpty()) return "Underflow";
		return this.items.pop();
	}

	peek() {
		return this.items[this.items.length - 1];
	}

	isEmpty() {
		return this.items.length === 0;
	}

	size() {
		return this.items.length;
	}
}

// Использование
let stack = new Stack();
stack.push(1);
stack.push(2);
stack.push(3);
console.log(stack.pop()); // 3
console.log(stack.peek()); // 2
```

### **Очередь (Queue)**

```javascript
class Queue {
	constructor() {
		this.items = [];
	}

	enqueue(element) {
		this.items.push(element);
	}

	dequeue() {
		if (this.isEmpty()) return "Underflow";
		return this.items.shift();
	}

	front() {
		if (this.isEmpty()) return "Queue is empty";
		return this.items[0];
	}

	isEmpty() {
		return this.items.length === 0;
	}

	size() {
		return this.items.length;
	}
}

// Использование
let queue = new Queue();
queue.enqueue(1);
queue.enqueue(2);
queue.enqueue(3);
console.log(queue.dequeue()); // 1
console.log(queue.front()); // 2
```

## 🌳 Древовидные структуры

### **Бинарное дерево**

```javascript
class Node {
	constructor(data) {
		this.data = data;
		this.left = null;
		this.right = null;
	}
}

class BinaryTree {
	constructor() {
		this.root = null;
	}

	insert(data) {
		let newNode = new Node(data);

		if (this.root === null) {
			this.root = newNode;
		} else {
			this.insertNode(this.root, newNode);
		}
	}

	insertNode(node, newNode) {
		if (newNode.data < node.data) {
			if (node.left === null) {
				node.left = newNode;
			} else {
				this.insertNode(node.left, newNode);
			}
		} else {
			if (node.right === null) {
				node.right = newNode;
			} else {
				this.insertNode(node.right, newNode);
			}
		}
	}

	// Обход в порядке (inorder)
	inorder(node = this.root) {
		if (node !== null) {
			this.inorder(node.left);
			console.log(node.data);
			this.inorder(node.right);
		}
	}
}
```

## 🔗 Связанные структуры

### **Хеш-таблица**

```javascript
class HashTable {
	constructor(size = 53) {
		this.keyMap = new Array(size);
	}

	_hash(key) {
		let total = 0;
		let WEIRD_PRIME = 31;
		for (let i = 0; i < Math.min(key.length, 100); i++) {
			let char = key[i];
			let value = char.charCodeAt(0) - 96;
			total = (total * WEIRD_PRIME + value) % this.keyMap.length;
		}
		return total;
	}

	set(key, value) {
		let index = this._hash(key);
		if (!this.keyMap[index]) {
			this.keyMap[index] = [];
		}
		this.keyMap[index].push([key, value]);
	}

	get(key) {
		let index = this._hash(key);
		if (this.keyMap[index]) {
			for (let i = 0; i < this.keyMap[index].length; i++) {
				if (this.keyMap[index][i][0] === key) {
					return this.keyMap[index][i][1];
				}
			}
		}
		return undefined;
	}
}
```

### **Связанный список**

```javascript
class ListNode {
	constructor(data) {
		this.data = data;
		this.next = null;
	}
}

class LinkedList {
	constructor() {
		this.head = null;
	}

	append(data) {
		let newNode = new ListNode(data);

		if (this.head === null) {
			this.head = newNode;
			return;
		}

		let current = this.head;
		while (current.next !== null) {
			current = current.next;
		}
		current.next = newNode;
	}

	print() {
		let current = this.head;
		while (current !== null) {
			console.log(current.data);
			current = current.next;
		}
	}
}
```

## 🎯 Критерии выбора структуры данных

### **По типу операций**

- **Частый доступ**: массивы, объекты
- **Частые вставки/удаления**: связанные списки
- **Поиск**: хеш-таблицы, деревья
- **Сортировка**: массивы, деревья

### **По размеру данных**

- **Маленькие**: простые структуры (массивы, объекты)
- **Большие**: специализированные (деревья, хеш-таблицы)
- **Очень большие**: внешние структуры (файлы, БД)

### **По требованиям к памяти**

- **Экономия памяти**: связанные списки
- **Быстрый доступ**: массивы, хеш-таблицы
- **Баланс**: деревья

## 💡 Практические советы

- **Начинай с простого**: массивы и объекты для большинства задач
- **Изучай сложность**: понимай время выполнения операций
- **Практикуйся**: реализуй структуры данных с нуля
- **Анализируй**: выбирай структуру под конкретную задачу

> [!note] См. также
>
> - [[Программирование/JavaScript/JavaScript|JavaScript]]
> - [[Алгоритмы|Алгоритмы]]
> - [[Подготовка к собеседованию|Подготовка к собеседованию]]

#структуры-данных #алгоритмы #массивы #объекты #map #set #стек #очередь
