Ключевая цель: **как можно быстрее отобразить пользователю первый полезный контент (LCP) и сделать интерфейс интерактивным (INP/TTI)**.  
CRP — это набор шагов, которые выполняет браузер, чтобы превратить HTML/CSS/JS в пиксели на экране.

---

# Подробный пайплайн CRP (Critical Rendering Path)

Ниже — детальный разбор всех шагов, которые проходят в браузере от получения HTML до отрисовки пикселей.

---

## 1. Загрузка HTML

- **DNS Lookup**: преобразование доменного имени в IP-адрес.
- **TCP/TLS Handshake**: установка защищённого соединения (TLS).
- **TTFB (Time To First Byte)**: когда браузер получил первый байт ответа.
- HTML может ссылаться на внешние ресурсы (`<link>`, `<script>`, `<img>`).
- **Блокирующие ресурсы** (CSS, sync JS) могут задерживать дальнейший парсинг.

👉 Оптимизация: CDN, кеширование, сжатие (Brotli/Gzip), HTTP/2/3.

---

## 2. Парсинг HTML → DOM

- Браузер читает HTML **последовательно сверху вниз**.
- На основе тегов строится дерево объектов (**DOM**).
- Если встречается `<script>` без `defer` или `async`, парсинг **приостанавливается** до загрузки и выполнения скрипта.
- `<link rel="stylesheet">` также блокирует отрисовку (но не сам парсинг DOM).

👉 Оптимизация: использовать `defer`, выносить тяжелые JS в конец, минимизировать sync-скрипты.

---

## 3. Парсинг CSS → CSSOM

- Все CSS-файлы скачиваются и парсятся.
- На их основе строится **CSSOM (CSS Object Model)**.
- Пока CSSOM не готов, браузер не может начать отрисовку.
- Даже один «лишний» CSS-файл может задержать LCP.

👉 Оптимизация: критический CSS инлайнить, остальное грузить асинхронно (`media`, `preload`).

---

## 4. Render Tree = DOM + CSSOM

- DOM описывает содержимое.
- CSSOM — правила стилей.
- Вместе они формируют **Render Tree** — только те узлы, которые реально видны (например, `display: none` не попадает).

👉 Оптимизация: не нагружать DOM лишними элементами, избегать ненужных стилей.

---

## 5. Layout (Reflow)

- На основе Render Tree рассчитываются размеры и позиции элементов.
- Это «геометрия страницы».
- Всё, что связано с размерами (`width`, `height`, `offsetWidth`, `clientHeight` и т.д.), может вызвать **пересчёт Layout**.

👉 Проблема: «layout thrashing» (многократные измерения и изменения DOM).  
👉 Оптимизация: группировать DOM-операции, использовать `requestAnimationFrame`, `documentFragment`.

---

## 6. Paint

- После Layout браузер начинает отрисовку:
  - текст,
  - фон,
  - изображения,
  - тени и градиенты.
- Каждый визуальный слой (например, div с box-shadow) рисуется отдельно.

👉 Оптимизация: избегать тяжелых эффектов (большие тени, фильтры).

---

## 7. Composite

- Отдельные слои собираются на GPU.
- Сюда относятся:
  - анимации (`transform`, `opacity` → без layout/repaint),
  - наложение слоёв.
- Ошибки в компоновке → «jank» (рывки, подлагивания).

👉 Оптимизация: использовать `transform/opacity` для анимаций, не злоупотреблять `will-change`.

---

## 8. JavaScript и вмешательства

- **Синхронные скрипты** блокируют парсинг HTML.
- **Изменение DOM** → вызывает layout/reflow.
- **Чтение layout-свойств** (`getBoundingClientRect`, `offsetWidth`) → форсирует пересчёт Layout.
- Тяжёлый JS может задержать **TTI (Time to Interactive)**.

👉 Оптимизация:

- Делить код на чанки (**code splitting**).
- Использовать `async/defer`.
- Тяжёлые вычисления переносить в **Web Workers**.
- Минимизировать количество изменений DOM.

---

# 📝 Шпаргалка по Critical Rendering Path (CRP)

| Шаг                        | Что происходит                            | Что блокирует                        | Что вызывает проблемы                      | Как оптимизировать                                           |
| -------------------------- | ----------------------------------------- | ------------------------------------ | ------------------------------------------ | ------------------------------------------------------------ |
| **1. Загрузка HTML**       | Получение HTML через DNS → TCP/TLS → TTFB | Медленный сервер, большой HTML       | Высокий TTFB, тяжёлый HTML                 | CDN, кеш, HTTP/2/3, Brotli, уменьшить HTML                   |
| **2. Парсинг HTML → DOM**  | Чтение HTML и построение DOM              | `<script>` без `defer/async`         | Блокировка парсинга                        | `defer` для скриптов, выносить тяжёлый JS вниз               |
| **3. Парсинг CSS → CSSOM** | Загрузка и парсинг стилей                 | Все CSS-файлы блокируют отрисовку    | LCP задерживается                          | Inline critical CSS, `media`/`preload` для второстепенных    |
| **4. Render Tree**         | Объединение DOM + CSSOM                   | Нет                                  | Слишком много элементов и стилей           | Минимизировать DOM, избегать ненужных правил                 |
| **5. Layout (Reflow)**     | Расчёт размеров и позиций                 | Чтение layout-метрик (`offsetWidth`) | Layout thrashing (многократный пересчёт)   | Группировать DOM-операции, `requestAnimationFrame`           |
| **6. Paint**               | Рисование текста, картинок, фонов         | Нет                                  | Тяжёлые тени, фильтры, большие изображения | WebP/AVIF, уменьшить эффекты                                 |
| **7. Composite**           | Сборка слоёв на GPU                       | Нет                                  | Jank (рывки) при неправильных анимациях    | Использовать `transform/opacity`, аккуратнее с `will-change` |
| **8. JavaScript**          | Выполнение логики, манипуляции DOM        | Синхронные скрипты, тяжёлый JS       | Задержка TTI, reflow/repaint               | Code splitting, `defer/async`, Web Workers                   |

---

## ⚡ Vue / Nuxt / React особенности

| Фреймворк | Где тормозит CRP                | Приёмы оптимизации                                                                     |
| --------- | ------------------------------- | -------------------------------------------------------------------------------------- |
| **Vue 3** | Hydration, реактивные пересчёты | Suspense, lazy hydration, `Teleport`, следить за реактивностью                         |
| **Nuxt**  | SSR/SSG/ISR + hydration         | Hybrid rendering (часть страниц SSG), `nuxt/image`, Nuxt Islands (partial hydration)   |
| **React** | Hydration, тяжёлый JS-бандл     | React.lazy, Suspense + streaming SSR, RSC (React Server Components), ISR/SSG в Next.js |
|           |                                 |                                                                                        |

---

## 🎯 Краткий чеклист для ускорения CRP

- ✅ Минимизировать **CSS** (inline critical, остальное отложить).
- ✅ Использовать `defer/async` для скриптов.
- ✅ Делить код на чанки (**code splitting**).
- ✅ Оптимизировать картинки (форматы + `srcset`).
- ✅ Избегать лишних DOM-узлов и тяжёлых стилей.
- ✅ Для SPA — SSR/SSG + lazy hydration.

---

## 2) Ключевые метрики производительности

- **TTFB (Time To First Byte)** — скорость ответа сервера (важно при SSR/SSG).
- **FCP (First Contentful Paint)** — первый контент (текст/иконка).
- **LCP (Largest Contentful Paint)** — самый крупный элемент (часто картинка/заголовок). Должен быть ≤ 2.5s.
- **TBT (Total Blocking Time)** — время, когда главный поток заблокирован JS.
- **INP (Interaction to Next Paint)** — задержка отклика на действия. Должен быть ≤ 200ms.
- **CLS (Cumulative Layout Shift)** — сдвиги макета (должен быть ≤ 0.1).

---

## 3) Общие приёмы оптимизации CRP

### Сеть и загрузка

- Использовать HTTP/2 или HTTP/3 (мультиплексирование).
- Сжатие (Brotli > Gzip).
- CDN + геораспределение.
- `rel=preconnect`, `rel=preload`, `rel=prefetch`.
- Inline **critical CSS**, остальное — грузить отложенно.
- Минимизировать количество блокирующих файлов.

### JavaScript

- **Code splitting** (lazy load компонентов).
- `async`/`defer` для `<script>`.
- Tree-shaking и dead-code elimination.
- Перенос тяжёлых вычислений в **Web Workers**.
- Минимизировать hydration JS (SSR → интерактивность).

### CSS

- Сокращать количество правил.
- Inline для above-the-fold.
- Избегать CSS, вызывающего много reflow (например, глобальные `* {}` правила).
- Использовать `will-change` аккуратно (только для реально анимируемых свойств).

### Отрисовка

- Для анимаций использовать `transform`/`opacity`, а не `top/left`.
- Уменьшать количество GPU-слоёв.
- Оптимизировать картинки: `webp/avif`, `srcset`, `sizes`.

---

## 4) Специфика Vue / Nuxt / React

### Vue 3

- В `setup()` не делать тяжёлых операций — это блокирует hydration.
- **Suspense** для асинхронных компонентов → браузер может рендерить постепенно.
- **vue-lazy-hydration** или islands-архитектура: отложенная активация компонентов.
- `Teleport` для тяжёлых UI-частей (модалки, тултипы).
- DevTools показывают reactivity-trigger → помогает оптимизировать diff.

### Nuxt (2/3/4)

- **SSR и SSG** ускоряют LCP, так как HTML отдается сразу.
- **Hybrid rendering**: часть страниц рендерится статически (SSG), часть динамически (SSR).
- `nuxt/image`: автооптимизация картинок → прямое улучшение LCP.
- Code splitting встроен — каждая страница = отдельный чанк.
- Nuxt 3/4 используют **Nitro** и edge-функции → быстрее TTFB.
- **Nuxt Islands**: частичная гидратация (меньше JS → быстрее CRP).

### React

- **Concurrent Mode (React 18)**: рендер можно прервать, важные задачи идут первыми.
- **Suspense + streaming SSR**: HTML отдаётся кусками, быстрый first paint.
- **React.lazy + dynamic import**: загружается только нужный код.
- **RSC (React Server Components)**: меньше JS на клиенте, hydration проще.
- В Next.js — **ISR (Incremental Static Regeneration)**: часть сайта обновляется статикой → лучший баланс CRP.

---

## 5) Ключевые выводы для мидл/сеньор

- CRP = путь от **байта на сервере** до **пикселя в браузере**.
- Главные блокеры: **CSS, JS, hydration**.
- Vue/Nuxt/React различаются в подходах к SSR/SSG/ISR и гидратации.
- Чем меньше JS на клиенте → тем быстрее CRP.
- **Stream, partial hydration, оптимизация картинок** =

---

> [!tip] Связанные темы
>
> - [A11y (Accessibility)](/brauzery/a11y-accessibility)
> - [Forced Synchronous Layout и Long Tasks](/brauzery/forced-synchronous-layout-i-long-tasks)
> - [Браузеры](/brauzery/index)

