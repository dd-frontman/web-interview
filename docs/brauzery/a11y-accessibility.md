## 1. Краткое объяснение

**A11y** — это сокращение от _Accessibility_ (доступность).  
Цель — сделать сайт или приложение удобным для **всех пользователей**, включая людей с ограниченными возможностями (например, слабовидящих, незрячих, с нарушениями моторики).  
Это достигается за счёт правильной верстки, семантики, ARIA-атрибутов, контрастности и управления с клавиатуры.

---

## 2. Подробное объяснение

### 🔹 Что включает A11y:

1. **Семантическая разметка**
   - Использовать правильные HTML-теги (`<button>` вместо `<div onclick>`).
   - Заголовки `<h1>–<h6>` должны отражать иерархию.
   - Формы должны иметь `<label>`.

2. **Управление с клавиатуры**
   - Всё приложение должно быть доступно через **Tab/Shift+Tab/Enter/Space**.
   - Фокус должен быть видимым.
   - Нельзя блокировать стандартное поведение клавиш.

3. **ARIA (Accessible Rich Internet Applications)**
   - Атрибуты `aria-label`, `aria-hidden`, `role` помогают скринридерам.
   - Но: использовать **только если семантического HTML недостаточно**.

4. **Контрастность и визуал**
   - Цвет текста и фона должны иметь достаточный контраст (WCAG ≥ 4.5:1).
   - Не использовать только цвет для передачи информации (например, “красное = ошибка”).

5. **Альтернативные тексты**
   - Все изображения должны иметь `alt`.
   - Если картинка декоративная — `alt=""`.

6. **Динамический контент и SPA**
   - При смене страницы (в SPA) скринридеру нужно сообщать об изменении (`aria-live`, обновление `<title>`).
   - Уведомления, модальные окна, ошибки форм → должны быть доступны скринридерам.

7. **Тестирование доступности**
   - Скринридеры: NVDA, VoiceOver, JAWS.
   - Инструменты: [axe DevTools](https://www.deque.com/axe/), Lighthouse, Chrome Accessibility Tree.

---

## 3. Примеры кода

### ❌ Плохо

```html
<div onclick="submitForm()">Send</div>
```

✅ Хорошо

```HTML
<button type="submit">Send</button>

```

### ARIA пример

```html
<!-- Кнопка закрытия модалки -->
<button aria-label="Закрыть окно">✖</button>
```

Форма с label

```html
<label for="email">Email</label> <input id="email" type="email" />
```

## 5. Чит-щит шпаргалка

| Тема           | Делать ✅                       | Избегать ❌                     |
| -------------- | ------------------------------- | ------------------------------- |
| Семантика      | `<button>`, `<nav>`, `<header>` | `div`/`span` с JS-обработчиком  |
| Фокус          | Видимая рамка, Tab-навигация    | `outline: none;` без замены     |
| Контраст       | ≥ 4.5:1                         | Светло-серый текст на белом     |
| Альтернативы   | `alt="описание"`                | Пропуск `alt`                   |
| Формы          | `<label for>`                   | Placeholder вместо label        |
| ARIA           | Только при необходимости        | Избыточное использование        |
| SPA / динамика | `aria-live`, обновление title   | Тихие изменения без уведомления |
| Тестирование   | Lighthouse, axe, NVDA           | Игнорировать доступность        |

---

---

> [!tip] Связанные темы
>
> - [Critical Render Path](/brauzery/critical-render-path)
> - [Forced Synchronous Layout и Long Tasks](/brauzery/forced-synchronous-layout-i-long-tasks)
> - [Браузеры](/brauzery/index)

