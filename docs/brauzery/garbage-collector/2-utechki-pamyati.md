# 🧠 Что такое утечка памяти

**Утечка памяти** — это ситуация, когда **память не освобождается**, хотя объект уже **не нужен**.  
JavaScript умеет сам освобождать память через _Garbage Collector_, но если объект всё ещё **достижим через ссылку**, GC не может его удалить. Из-за этого память постепенно растёт, приложение замедляется и может даже падать. [DITDOT](https://www.ditdot.hr/en/causes-of-memory-leaks-in-javascript-and-how-to-avoid-them)

---

## 📉 Почему это происходит

JavaScript считает, что объект _нужен_, если:

- на него есть _ссылка_ из глобального или локального контекста
- он участвует в _замыканиях_
- на него ссылаются _timers / listeners_ и т.д.

Если такие ссылки остаются, то даже после того как объект перестаёт использоваться логически — он **не уничтожается**. [DITDOT](https://www.ditdot.hr/en/causes-of-memory-leaks-in-javascript-and-how-to-avoid-them)

---

# 🧪 Основные кейсы утечек памяти

## 1) 📌 Глобальные переменные

**Что это?**  
Переменные, которые определены без `let/const/var`, попадают в глобальный объект и живут до конца работы страницы. [Medium](https://saynaesmailzadeh.medium.com/memory-leaks-in-javascript-types-causes-and-how-to-fix-them-6e572fe11587)

### Пример

```js
function createLeak() {
	leakyVar = "global"; // становится глобальной переменной
}
createLeak();
// эта память уже не будет освобождена
```

👉 **Профит**: глобальные переменные никогда не удаляются, GC не может собрать их, даже если больше не используются. [Medium](https://saynaesmailzadeh.medium.com/memory-leaks-in-javascript-types-causes-and-how-to-fix-them-6e572fe11587)

---

## 2) 🤝 Замыкания (Closures), которые держат большие данные

**Почему это проблема?**  
Функции в JavaScript запоминают переменные из внешней области. Если замыкание захватывает большой объект, он остаётся в памяти, пока жива функция. [Medium](https://medium.com/%40rahul.jindal57/memory-leaks-in-dom-elements-and-closures-b3452f129dac)

### Пример

```js
function createClosure() {
	const bigArray = new Array(1e6).fill("data");
	return function inner() {
		console.log(bigArray.length);
	};
}

const leaky = createClosure(); // bigArray всё ещё в памяти
```

👉 Если `leaky` живёт долго, то и `bigArray` тоже не будет собран GC. [Medium](https://medium.com/%40rahul.jindal57/memory-leaks-in-dom-elements-and-closures-b3452f129dac)

---

## 3) 🧩 Неправильно очищенные **event listeners**

**Суть:** слушатель события держит ссылку на DOM-элемент или замыкания, поэтому даже если элемент удалён — память остаётся занята. [DEV Community](https://dev.to/alex_aslam/how-to-avoid-memory-leaks-in-javascript-event-listeners-4hna)

### Пример ошибки

```js
const button = document.createElement("button");
button.addEventListener("click", () => {
	console.log("clicked");
});
document.body.appendChild(button);

// позже
button.remove(); // слушатель остался → кнопка и слушатель не удалятся из памяти
```

Чтобы исправить:

`button.removeEventListener("click", handleClick); button.remove();`

👉 Без удаления слушателя объект DOM остаётся достижимым, поэтому GC не уберёт его. [DEV Community](https://dev.to/alex_aslam/how-to-avoid-memory-leaks-in-javascript-event-listeners-4hna)

---

## 4) 🕸 Detached DOM Nodes (отсоединённые узлы)

**Что это?**  
DOM-элемент удалён с страницы, но на него всё ещё есть ссылка в JS. Он отсоединён, но **не может быть собран GC**, потому что он ещё достижим. [Chrome for Developers](https://developer.chrome.com/docs/devtools/memory-problems)

### Пример

```js
let cachedDiv = document.getElementById("myDiv");
document.body.removeChild(cachedDiv);
// cachedDiv всё ещё хранит ссылку → узел не удалён из памяти
```

👉 Такое часто встречается в SPA-приложениях, где элементы удаляются динамически. [Chrome for Developers](https://developer.chrome.com/docs/devtools/memory-problems)

---

## 5) ⏱ Незакрытые Timers (setInterval / setTimeout)

**Почему это утечка?**  
Таймеры продолжают ссылаться на данные, которые они используют, даже если их логически больше не нужно. [Medium](https://medium.com/%40simplycodesmart/understanding-and-preventing-memory-leaks-in-javascript-1a6fc5d9f4f5)

### Пример

```js
const id = setInterval(() => {
	console.log("tick");
}, 1000);
// ничего не очищаем → функция и её окружение живут постоянно
```

Решение:

`clearInterval(id);`

👉 Без `clearInterval`, callback будет держать память постоянно. [Medium](https://medium.com/%40simplycodesmart/understanding-and-preventing-memory-leaks-in-javascript-1a6fc5d9f4f5)

---

## 6) 📦 Неразумное кеширование

Если ты хранишь данные в кеш без ограничения размера, кеш может расти бесконечно и не очищаться. [Habr](https://habr.com/ru/articles/309318/)

### Пример

```js
const cache = {};
function remember(key, value) {
	cache[key] = value; // без ограничения → бесконечный рост
}
```

👉 Такой кеш становится всегда достижимым глобально и не может быть собран GC. [Habr](https://habr.com/ru/articles/309318/)

---

## 7) 🔄 Циклические ссылки (в старых движках)

Реже в современных движках, но всё ещё может влиять: два объекта ссылаются друг на друга, а на них есть ссылки из локального окружения. [developer.mozilla.org](https://developer.mozilla.org/ru/docs/Web/JavaScript/Guide/Memory_management)

### Пример

```js
let a = {};
let b = {};
a.ref = b;
b.ref = a;
a = null;
b = null;
// Цикл может удерживать память в некоторых случаях
```

---

# 📋 Таблица-шпаргалка по утечкам памяти

| Тип утечки                | Причина                          | Основной симптом                           | Как избежать                                      |
| ------------------------- | -------------------------------- | ------------------------------------------ | ------------------------------------------------- |
| **Глобальные переменные** | Переменные без `let/const/var`   | Память не освобождается                    | Использовать строгий режим и локальные переменные |
| **Closures**              | Замыкания держат большие объекты | Данные живут слишком долго                 | Минимизировать захват переменных                  |
| **Event Listeners**       | Слушатели не удалены             | Слушатели + DOM элементы висят в памяти    | Удалять слушатели при cleanup                     |
| **Detached DOM**          | DOM удалён, но ссылка осталась   | Значки “detached” в профайле               | Обнулять ссылки и listeners                       |
| **Timers**                | Таймеры не очищены               | setInterval/Timeout продолжают выполняться | `clearInterval/clearTimeout`                      |
| **Caches**                | Кеш без лимитов                  | Память растёт постоянно                    | Ограничивать размер кеша                          |
| **Циклы ссылок**          | Объекты ссылаются друг на друга  | GC не может определить “мусор”             | Разрывать циклы, использовать Weak\*              |

---

# 🛠 Как диагностировать утечки

В браузерах есть инструменты:

📌 **Chrome DevTools — вкладка Memory**  
→ **Heap Snapshot**: можно делать снимки памяти и сравнивать их, чтобы увидеть, какие объекты не исчезают. [Chrome for Developers](https://developer.chrome.com/docs/devtools/memory-problems)

📌 **Performance / Allocation Timeline**: следит за ростом памяти во времени. [syncfusion.com](https://www.syncfusion.com/blogs/post/prevent-javascript-memory-leaks-guide)

---

# 📌 Итог

➡️ Утечки памяти возникают, когда **ссылки на объекты остаются, даже если они больше не нужны.**  
➡️ Garbage Collector может **удалять только недостижимые объекты**, поэтому всё ещё достижимое останется в памяти. [DITD](https://www.ditdot.hr/en/causes-of-memory-leaks-in-javascript-and-how-to-avoid-them)

---

<RelatedTopics
	:items="[
		{ title: 'Сборщик мусора', href: '/brauzery/garbage-collector/1-sborschik-musora' },
		{ title: 'Вопросы по Garbage Collector', href: '/brauzery/garbage-collector/voprosy-po-garbage-collector' },
		{ title: 'Полный путь загрузки сайта', href: '/brauzery/polnyi-put-zagruzki-saita' },
	]"
/>

