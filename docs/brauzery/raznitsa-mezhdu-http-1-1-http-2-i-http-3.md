## 1. Краткое объяснение

**HTTP (HyperText Transfer Protocol)** — это протокол, по которому браузер и сервер обмениваются данными.  
Сейчас активно используют **HTTP/2** и **HTTP/3**, так как они быстрее и безопаснее, чем **HTTP/1.1**.

---

## 2. Подробное объяснение

### 🔹 HTTP/1.1 (старый стандарт, 1999 год)

- Работает поверх TCP.
- **Ограничение:** за одно соединение — один запрос → много _лишних соединений_.
- Используется **Keep-Alive** и пайплайнинг, но есть проблема _head-of-line blocking_ (если один запрос тормозит, остальные ждут).

### 🔹 HTTP/2 (2015 год)

- Всё ещё TCP.
- Ключевые улучшения:
  1. **Мультиплексирование**: несколько запросов по одному соединению.
  2. **Бинарный протокол** (вместо текстового) → быстрее парсится.
  3. **Сжатие заголовков (HPACK)** → экономия трафика.
  4. **Server Push** (сервер может отправить CSS/JS до запроса клиента).
- Минус: из-за TCP всё ещё есть _head-of-line blocking_ (но уже внутри TCP, а не HTTP).

### 🔹 HTTP/3 (2022 год, самый новый)

- Работает поверх **QUIC** (UDP + встроенный TLS 1.3).
- Улучшения:
  1. Нет head-of-line blocking.
  2. Быстрее устанавливается соединение (нет отдельного TCP + TLS).
  3. Лучше работает в нестабильных сетях (мобильный интернет, Wi-Fi).
- Все современные браузеры поддерживают HTTP/3.

---

## 3. Как использовать актуальную версию

1. **На сервере**
   - Для HTTP/2/3 нужен HTTPS (TLS).
   - Настроить сервер (Nginx, Apache, Caddy, Cloudflare).
   - Пример (Nginx):

     ```nginx
     server {
       listen 443 ssl http2;
       ssl_protocols TLSv1.2 TLSv1.3;
       ...
     }
     ```

   - Для HTTP/3 (QUIC):
     ```nginx
     server {
       listen 443 ssl http2;
       listen 443 quic reuseport;   # HTTP/3
       ssl_protocols TLSv1.3;
       add_header Alt-Svc 'h3=":443"'; # сообщает браузеру о поддержке HTTP/3
       ...
     }
     ```

2. **На клиенте (браузере)**
   - Современные браузеры (Chrome, Firefox, Safari, Edge) поддерживают HTTP/2 и HTTP/3 автоматически.
   - Пользователь **ничего не делает** — браузер сам договаривается с сервером о версии.

3. **Проверка**
   - В DevTools → Network → Protocol (можно увидеть `h2` или `h3`).
   - Онлайн: [tools.keycdn.com/http2-test](https://tools.keycdn.com/http2-test).

---

## 4. Ссылки на документацию

- [MDN — Evolution of HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/Evolution_of_HTTP)
- [MDN — HTTP/2](https://developer.mozilla.org/en-US/docs/Web/HTTP/2)
- [MDN — HTTP/3](https://developer.mozilla.org/en-US/docs/Web/HTTP/3)
- [Cloudflare — What is HTTP/3?](https://www.cloudflare.com/learning/performance/what-is-http3/)

---

## 5. Чит-щит шпаргалка

| Версия   | Особенности                                      | Минусы                     | Когда использовать         |
| -------- | ------------------------------------------------ | -------------------------- | -------------------------- |
| HTTP/1.1 | Старый текстовый протокол, один запрос за раз    | Много соединений, тормоза  | Только для старых серверов |
| HTTP/2   | Мультиплексирование, бинарный, сжатие заголовков | Всё ещё TCP-задержки       | Использовать по умолчанию  |
| HTTP/3   | QUIC (UDP), быстрый, нет блокировок, TLS 1.3     | Сервер должен поддерживать | Самая актуальная версия    |

---

---

> [!tip] Связанные темы
>
> - [A11y (Accessibility)](/brauzery/a11y-accessibility)
> - [Critical Render Path](/brauzery/critical-render-path)
> - [Forced Synchronous Layout и Long Tasks](/brauzery/forced-synchronous-layout-i-long-tasks)

