## 🔹 LCP (Largest Contentful Paint)

**Что это:** момент, когда отрисован **самый крупный элемент на экране** (обычно картинка или заголовок).

- Показывает, насколько быстро пользователь видит **основное содержимое страницы**.
- Google считает хорошим результатом: **≤ 2.5 секунды**.

**Пример:**

- На лендинге крупная картинка + заголовок H1.
- LCP фиксируется, когда эта картинка полностью показана.

---

## 🔹 INP (Interaction to Next Paint)

**Что это:** время между действием пользователя (клик, ввод, скролл) и моментом, когда страница **отреагировала визуально**.

- Это более новый показатель, заменяет старый FID (First Input Delay).
- Меряет **реальную отзывчивость интерфейса**.
- Хорошо: **≤ 200ms**.

**Пример:**

- Пользователь кликает на кнопку.
- Если ответ (анимация, изменение UI) произошёл через 500ms → плохой INP.

---

## 🔹 TTI (Time to Interactive)

**Что это:** момент, когда страница становится **полностью интерактивной**:

- всё основное содержимое загружено,
- JS обработан,
- интерфейс готов реагировать на действия.

**Измеряется:** от загрузки до того момента, когда **главный поток свободен** и может обрабатывать события.

**Пример:**

- SPA загрузилась, текст уже виден, но кнопки ещё «мертвые», т.к. идёт hydration JS.
- TTI считается, когда кнопки начали реально работать.

---

## 🧩 В связке:

- **LCP** = насколько быстро пользователь увидел **главный контент**.
- **INP** = насколько быстро UI **отвечает на действия**.
- **TTI** = когда страница стала **полностью рабочей**.

---

## Для Vue / Nuxt / React

- **LCP**: зависит от SSR/SSG (чем быстрее HTML + картинки → тем лучше).
- **INP**: зависит от оптимизации JS, реактивности и числа ререндеров.
- **TTI**: зависит от размера бандла, сложности hydration, использования code splitting/lazy load.

---

> [!tip] Связанные темы
>
> - [A11y (Accessibility)](/brauzery/a11y-accessibility)
> - [Critical Render Path](/brauzery/critical-render-path)
> - [Forced Synchronous Layout и Long Tasks](/brauzery/forced-synchronous-layout-i-long-tasks)

