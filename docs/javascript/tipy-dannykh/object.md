## Методы объектов

1. **Создание свойств**:
   `let obj = {}; obj.newProperty = "value";`
2. **Чтение свойств**:
   `let value = obj.newProperty;`
3. **Изменение свойств**:
   `obj.newProperty = "newValue";`
4. **Удаление свойств**:
   `delete obj.newProperty;`
5. **Проверка наличия свойства**:
   `if ("newProperty" in obj) { // свойство существует }`
6. **Перебор свойств**:
   `for (let key in obj) { console.log(key, obj[key]); }`
7. **Получение списка ключей**:
   `let keys = Object.keys(obj);`
8. **Получение списка значений**:
   `let values = Object.values(obj);`
9. **Получение списка пар ключ-значение**:
   `let entries = Object.entries(obj);`
10. **Копирование объекта**:
    `let copy = Object.assign({}, obj);`
    Поверхностная копия объекта означает создание нового объекта, который копирует только верхний уровень исходного объекта. Если исходный объект содержит вложенные объекты или массивы, то в поверхностной копии будут скопированы только ссылки на эти вложенные объекты или массивы, а не их фактические значения.
    Для создания глубокой копии объекта, где каждый вложенный объект или массив также копируется, а не просто ссылается на исходный, вы можете использовать различные методы:
    • **Используя JSON:** Этот метод прост, но имеет ограничения (например, он не будет работать с функциями, `undefined`, `Symbol` или с циклическими ссылками в объектах).
    `let deepCopy = JSON.parse(JSON.stringify(original));`
    • **Используя рекурсию:** Вы можете написать рекурсивную функцию, которая будет проходить по каждому свойству объекта и копировать его.

```javascript
function deepClone(obj) {
	if (obj === null) return null;
	if (typeof obj !== "object") return obj;

	if (Array.isArray(obj)) {
		const arrCopy = [];
		for (let i = 0; i < obj.length; i++) {
			arrCopy[i] = deepClone(obj[i]);
		}
		return arrCopy;
	}

	const objCopy = {};
	for (let key in obj) {
		objCopy[key] = deepClone(obj[key]);
	}
	return objCopy;
}

let deepCopy = deepClone(original);
```

• **Используя Json.parse:** `const copy = JSON.parse(JSON.stringify(original));`
**Минусы:**

- Не копирует функции, `undefined`, символы, свойства с типом `Date`, `Map`, `Set` и т.д.
- Потеря прототипа.
  • **StructuredClone (современный способ)** `const copy = structuredClone();`
  **Минусы:**
- Не копирует функции.
- Работает только в современных браузерах и Node.js 17+.
  • **Используя библиотеки:** Существуют библиотеки, такие как Lodash, которые предоставляют функции для глубокого клонирования объектов. Например, с помощью Lodash:

```javascript
const _ = require("lodash");
let deepCopy = _.cloneDeep(original);
```

1. **Расширение объекта**: Вы можете добавить свойства из одного объекта в другой.
   `Object.assign(obj, anotherObj);`
2. **Проверка прототипа**: Вы можете проверить, является ли один объект прототипом другого.
   `obj instanceof ConstructorFunction;`
3. **Установка прототипа**: Вы можете установить прототип для объекта.
   `Object.setPrototypeOf(obj, newPrototype);`
4. **Получение прототипа**: Вы можете получить прототип объекта.
   `let prototype = Object.getPrototypeOf(obj);`

# 🧠 1) Объекты хранятся **по ссылке**, а не по значению

В JavaScript примитивы (числа, строки, булево) хранятся **как значения** — при копировании переменной значение дублируется. Но объекты (и массивы) — это **ссылочные типы**:

👉 переменная _не содержит сам объект_, а **ссылку на него** в памяти. При присвоении создаётся **копия ссылки**, а не объекта.

Пример:

```js
let user = { name: "John" };
let admin = user; // admin ссылается на тот же объект
admin.name = "Pete";
console.log(user.name); // 'Pete'
```

В этом примере изменение объекта через `admin` отражается и в `user`, потому что **оба указывают на один и тот же объект**.

---

## 🧠 2) Мутация объекта — прямое изменение его данных

Объекты в JavaScript по умолчанию **мутабельны** (mutable). Это значит, что ты можешь изменять их свойства:

```js
const obj = { x: 1 };
obj.x = 2; // изменяет объект
```

Мутация — это **изменение структуры или содержимого объекта**, а не его переопределение — и это может иметь неожиданные эффекты, если объект используется в нескольких местах через ссылки.

🔹 Это особенно важно в приложениях, где состояние часто передаётся по ссылке (например, в Vue/React), и **изменения одного объекта могут неожиданно затронуть другие части кода**.

---

# 🧠 3) Сравнение объектов

Объекты сравниваются **по ссылке**, а не по содержимому:

```js
{} === {} // false — разные объекты
let a = {};
let b = a;
a === b     // true — одна ссылка
```

То есть даже если два объекта _внешне одинаковы_ по свойствам, они считаются разными, если занимают разные места в памяти.

---

# 🧠 4) Поверхностное и глубокое копирование

Когда ты копируешь объект, JavaScript не делает полную копию всей структуры автоматически — он копирует **ссылки на вложенные объекты**.

### 📍 Поверхностное копирование (shallow copy)

```js
const original = {
	name: "John",
	hobbies: ["reading", "swimming"],
};
const copy = { ...original };
copy.hobbies.push("cooking");
console.log(original.hobbies); // ['reading', 'swimming', 'cooking']
```

Здесь внешний объект скопирован, **но массив `hobbies` остался по ссылке**, поэтому его изменение повлияло на оригинал.

Методы поверхностного копирования:

- `{ ...obj }` — spread
- `Object.assign({}, obj)`  
   Оба копируют только верхний уровень — _вложенные объекты остаются ссылками_.

---

### 📍 Глубокое копирование (deep copy)

Глубокое копирование создаёт **независимую копию всей структуры**, включая вложенные объекты:

```js
function deepCopy(obj) {
	return JSON.parse(JSON.stringify(obj));
}
```

Или более универсальные методы (рекурсивное клонирование, `structuredClone`, библиотеки типа lodash).  
Глубокая копия означает, что изменения в копии **не влияют на оригинал**.

⚠️ Обрати внимание, что `JSON.parse/stringify` не работает со всем (функции, `undefined`, даты и др.).

---

## 🧠 5) Мутация vs Иммутабельность

- **Мутация** — изменение существующих данных (объекта).
- **Иммутабельность** — объект остаётся неизменным; чтобы «изменить» его, создаётся новая копия.

В JavaScript примитивы по своей природе immutable, а большинство объектов mutable. В паттернах типа Redux/Flux/Pinia предпочтительно **не мутировать состояние напрямую**, а делать **новые копии при изменении** (pure functions).

Это помогает:  
✔ предсказуемости  
✔ отслеживанию изменений (например, сравнение по ссылке)  
✔ избежанию побочных эффектов

---

## 🧠 6) Почему это важно в приложениях

### 🔹 Состояние может неожиданно измениться

Если два модуля или компонента держат одну ссылку на объект, изменение в одном месте может **неявно изменить состояние где-то ещё**.

```js
const state = { user: { name: "Anna" } };
const ref = state.user;
ref.name = "Maria";
console.log(state.user.name); // Maria
```

Без глубокого копирования, такое поведение может быть источником багов.

---

### 🔹 Эффективность и производительность

Копирование по ссылке — очень быстро (просто дублируется указатель), но глубокое копирование может быть дорогостоящим, особенно для больших объектов. Понимание, когда и как копировать, помогает оптимизировать код.

---

## 📋 Краткая шпаргалка

| Понятие             | Примитивы                 | Объекты                          |
| ------------------- | ------------------------- | -------------------------------- |
| Сравнение           | по значению               | по ссылке                        |
| Копирование         | создаёт независимую копию | по умолчанию копирует **ссылку** |
| Поверхностная копия | не применимо              | копирует только первый уровень   |
| Глубокая копия      | не нужна                  | создаёт независимую структуру    |

---

## 📌 Итоги

✔ Объекты — **ссылочные типы** (изменение через одну переменную влияет на все, кто ссылается на этот объект).  
✔ Поверхностное копирование оставляет ссылки на вложенные объекты, глубокое — создаёт независимую структуру.  
✔ Понимание мутабельности и копирования важно для корректной работы с состоянием в приложениях.

---

> [!tip] Связанные темы
>
> - [Типы данных](/javascript/tipy-dannykh/tipy-dannykh)
> - [Типы функций](/javascript/tipy-funktsii)
> - [Методы массивов](/javascript/metody-massivov)

