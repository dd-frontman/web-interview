## 1. Что такое «приоритет» и «ассоциативность» — зачем это знать

- **Приоритет (precedence)** — чем выше число в таблице MDN, тем раньше оператор вычисляется. Например, `*` (ур.14) выполняется раньше `+` (ур.13). [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Operator_precedence)
- **Ассоциативность** — в какую сторону группируются операторы равного приоритета. У унарных `++`/`--` (право-ассоц.) выражение `i = ++a ** 2` сначала увеличит `a`, затем возведёт в степень. У присваивания `=` (право-ассоц.) цепочка `a = b = 3` присваивает справа → налево.
  💡 **На интервью** могут попросить объяснить, почему `3 + 4 * 5 / 2` даёт 13, а не 17 — демонстрируйте знание таблицы и умение ставить скобки вручную.

---

## 2. Арифметика и инкременты

| Операторы      | Особенности                                                                                                  |
| -------------- | ------------------------------------------------------------------------------------------------------------ |
| `+ - * / % **` | `+` конкатенирует строки, `**` — возведение в степень, с BigInt тоже работает, но **нельзя** смешивать типы. |
| `++ --`        | Префикс возвращает новое значение, постфикс — старое.                                                        |
| Унарные `+ -`  | `+` делает «быстрое Number()». `+'0x10'` → 16.                                                               |
|                |                                                                                                              |

**Подводный камень:** `0.1 + 0.2 !== 0.3` — не точность, а IEEE-754; не путать с ошибкой округления JS.
В JavaScript (как и в большинстве языков, основанных на IEEE-754 double) `0.1 + 0.2` даёт `0.30000000000000004`, потому что десятичная дробь 0.1 в двоичном виде бесконечна и хранится с округлением. Есть несколько практичных способов «получить ровно 0.3», и нужно понимать, какой именно результат вам важен — корректное математическое число для вычислений или красиво отформатированная строка для вывода.

| Подход                                               | Как написать                                                              | Что важно понимать                                                                                                                                                      |
| ---------------------------------------------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Округлить результат до нужного количества знаков** | `+(0.1 + 0.2).toFixed(2) // 0.3` <br>`parseFloat((0.1 + 0.2).toFixed(1))` | `toFixed` возвращает **строку** → ставим унарный `+`/`parseFloat`, если нужно число. Для финансовых расчётов храните «копейки» как целое, округляйте только для вывода. |
|                                                      |                                                                           |                                                                                                                                                                         |

Или использовать стороннюю библиотеку для сложений

---

## 3. Сравнения

- **Не-строгие** `== !=` приводят типы: `'' == 0` → `true`.
- **Строгие** `=== !==` сравнивают без приведения: safer-default.
- `Object.is()` похож на `===`, но `NaN` сравнивает как равный самому себе.
- **Совет для собеса:** отмечайте, что `>` / `<` при разных типах приводит оба к числу или к строке — зависит от случая.

---

## 4. Логические связки, короткое-замыкание и новые присваивания

| Оператор | Что делает                                                                                                                                                                                                                                 | «Грабли»                                                             |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------- |
| `&&`     | Возвращает **первый falsy** или последний truthy. Используется для условного рендера: `isReady && <List/>`. [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Logical_AND) | Не возвращает `true/false`, а реальное значение: `'' && 'x'` → `''`. |
| `        |                                                                                                                                                                                                                                            | `                                                                    |
| `??`     | «Nullish-coalescing». Срабатывает **только** на `null/undefined`.                                                                                                                                                                          | Решает проблему `0                                                   |
| `&&=     |                                                                                                                                                                                                                                            | = ??=`                                                               |

---

## 5. Условные конструкции

- **Тернарный `condition ? A : B`** — единственный истинно условный оператор (остальное — конструкции). Позволяет вернуть значение прямо в выражении.
- **`if/else`** остаётся читабельней, когда ветки сложные.
- **`switch … case`** полезен при множественных вариантах; fall-through без `break` — частый вопрос.

---

## 6. Optional chaining `?.`

- Безопасно обращается к вложенным полям (`user?.profile?.id`) и к вызовам (`cb?.()`).
- «Коротко-замыкается» и даёт `undefined`, вместо того чтобы бросить `TypeError`. [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Optional_chaining)
- В шаблонах Vue 3 работает «из коробки» в `<script setup>`; в старых версиях нужен Babel-плагин.

---

## 7. Побитовые и сдвиги

| Оператор    | Коротко                    | Что помнить                                        |
| ----------- | -------------------------- | -------------------------------------------------- |
| `&          | ^ ~`                       | AND, OR, XOR, NOT                                  |
| `<< >> >>>` | Сдвиги логические и арифм. | `>>>` заполняет нулями → всегда положит результат. |

Используются для флагов, хэш-функций, WebGL-шейдерных утилит.

---

## 8. Реляционные: `in` и `instanceof`

- `key in obj` проверяет наличие свойства **в цепочке прототипов**.
- `obj instanceof Ctor` ищет Ctor.prototype в цепочке `obj.__proto__`.  
   Частый вопрос: почему `[] instanceof Array` — true, но `[] instanceof Object` тоже true.

---

## 9. «Служебные» унарные

| Оператор          | Что делает                                                                                         | Кейс |
| ----------------- | -------------------------------------------------------------------------------------------------- | ---- |
| `typeof x`        | Строка-тип (`'object'`, `'number'` …). У `typeof null` баг наследия → `'object'`.                  |      |
| `delete obj.prop` | Удаляет ключ (и нарушает оптимизацию V8, будьте осторожны).                                        |      |
| `void expr`       | Выполняет и возвращает `undefined`. Используется в `<a href="javascript:void 0">`.                 |      |
| `await` / `yield` | Управляют асинхронностью/генераторами; оператор, потому что возвращает значение, а не конструкция. |      |
| `new Ctor()`      | Создаёт объект и связывает прототип.                                                               |      |

---

## 10. Запятая `,`

- Вычисляет слева → справа, возвращает последний результат: `i++, j = i, i * 2`.
- Встречается в `for (i=0, j=10; i<j; i++, j--)`.

---

## 11. BigInt-операции

- Все арифметические и побитовые операторы допускают BigInt, **но** нельзя смешивать с Number без явного преобразования: `1n + 1` → TypeError.

---

## 12. Экспериментальные Stage 2-3 (на 07 / 2025)

- **Pipeline `|>`** (Stage 2): более читаемые цепочки трансформаций. Пока Babel-флаг + суворые синтакс-варианты (topic-reference `%^`). [JavaScript in Plain English](https://javascript.plainenglish.io/whats-new-in-ecmascript-2025-for-javascript-developers-b7f8c98ca32e)[GitHub](https://github.com/tc39/proposal-pipeline-operator)
- **Do-expression `do { … }`** (Stage 1): блок-как-выражение, возвращающий последнее значение. Не стоит использовать в проде. [Medium](https://medium.com/%40ofir.zukerman/mastering-do-expressions-in-javascript-for-enhanced-code-clarity-3fa111a94bfc)

---

---

<RelatedTopics
	:items="[
		{ title: 'Что такое замыкание', href: '/javascript/chto-takoe-zamykanie' },
		{ title: 'Event Bubbling', href: '/javascript/event-bubbling' },
		{ title: 'Event Loop', href: '/javascript/event-loop' },
	]"
/>

