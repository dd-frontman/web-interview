# 🧠 Что такое **Map** в JavaScript

**`Map`** — это **структура данных (коллекция)**, которая хранит данные в виде **пар «ключ → значение»**, где:

✅ ключи могут быть **любого типа** (строки, числа, объекты, функции и т. д.)  
✅ значения тоже могут быть любого типа  
✅ порядок элементов **сохраняется** в том порядке, в котором пары были добавлены [MDN Web Docs+1](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map)

Это делает `Map` более гибким и мощным по сравнению с обычными объектами `{}`, которые раньше использовались как словари, но имеют ограничения. [learn.javascript.ru](https://learn.javascript.ru/map-set)

---

## 📌 Почему `Map` — не просто объект

Хотя `Map` и объект могут выглядеть похожими (оба хранят пары ключ-значение), у них есть **важные отличия**:

✔ **Любые типы ключей:**  
В `Map` ключи могут быть даже объектами или функциями.  
В объекте ключи ограничены строками и символами (символы символы тоже). [learn.javascript.ru](https://learn.javascript.ru/map-set)

✔ **Порядок вставки:**  
В `Map` порядок возвращаемых элементов (при переборе) совпадает с порядком добавления пар.  
Объекты не гарантируют порядок свойств так явно. [MDN Web Docs](https://developer.mozilla.org/ru/docs/Web/JavaScript/Guide/Keyed_collections)

✔ **Размер:**  
У `Map` есть свойство `.size`, которое возвращает количество пар.  
У объекта для этого нужно вручную вычислять `Object.keys(obj).length`. [DEV Community](https://dev.to/manojspace/javascript-maps-vs-objects-30-essential-questions-every-developer-should-know-49ga)

✔ **Итерация:**  
`Map` — итерируемый: можно использовать `for...of`, `forEach` и другие методы напрямую.  
Объект нельзя перебирать напрямую без дополнительных вызовов. [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map)

---

# 🔑 Методы и свойства `Map`

Вот самые важные:

| Метод / свойство      | Что делает                                   |
| --------------------- | -------------------------------------------- |
| `new Map()`           | создаёт новую пустую карту                   |
| `map.set(key, value)` | добавляет пару (или обновляет её)            |
| `map.get(key)`        | возвращает значение по ключу или `undefined` |
| `map.has(key)`        | проверяет, есть ли ключ                      |
| `map.delete(key)`     | удаляет пару по ключу                        |
| `map.clear()`         | очищает всю карту                            |
| `map.size`            | количество пар в карте                       |
| `map.keys()`          | возвращает итератор всех ключей              |
| `map.values()`        | возвращает итератор всех значений            |
| `map.entries()`       | итератор пар `[key, value]`                  |
| `forEach()`           | перебирает все пары                          |

---

## 📌 Простой пример

```js
const map = new Map();

map.set("name", "Alice");
map.set(1, "число");
map.set(true, "булево");

console.log(map.get("name")); // "Alice"
console.log(map.size); // 3

for (const [key, value] of map) {
	console.log(key, value);
}
```

Здесь:

- ключи — разного типа (строка, число, булево)
- вывод будет в порядке добавления
- `.size` прямо показывает, сколько элементов в коллекции [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map)

---

# 🧠 Почему `Map` полезен

### ✔ Использование объектов как ключей

В объектах `{}` объект не может быть ключом (приводится к строке).  
В `Map` объекты могут быть ключами напрямую:

`const objKey = {}; map.set(objKey, "value");`

И это работает корректно, потому что `Map` сравнивает ключи **по ссылке**, а не по строке. [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map)

---

### ✔ Порядок элементов важен

Допустим, ты храните записи логов, пользователи в порядке их регистрации или действия в хронологическом порядке. `Map` гарантирует, что итерация вернёт элементы в том же порядке, в котором они были добавлены. [MDN Web Docs](https://developer.mozilla.org/ru/docs/Web/JavaScript/Guide/Keyed_collections)

---

### ✔ Быстрые операции добавления и удаления

`Map` оптимизирован под частое добавление/удаление ключей — это может быть быстрее, чем работа с объектами, особенно когда ключей много. [Builder.io](https://www.builder.io/blog/maps)

---

## 🧠 Когда стоит использовать `Map` вместо объекта

👉 Когда ключи **не строковые**  
👉 Когда важно **сохранять порядок**  
👉 Когда нужно часто **удалять и добавлять** элементы  
👉 Когда хочется прямой доступ к `.size` и хорошей итерации  
👉 Когда объект наследует лишние свойства, которые мешают (`Object.prototype`) [Medium](https://medium.com/%40conboys111/when-to-use-a-map-instead-of-an-object-in-javascript-e396f1b27b19)

---

## 🧠 Что под капотом

`Map` — не просто обычный объект:

- Его реализация может быть основана на **хэш-таблице** (с быстрым доступом) или других более оптимизированных структурах.
- Он должен обеспечивать время доступа **лучшее, чем O(N)** (в среднем) для операций поиска. [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map)

Это означает, что он обычно **быстрее, чем перебор объекта**, особенно при больших объёмах данных.

---

## 🔁 Итерация и перебор

С `Map` легко перебирать данные:

```js
const objKey = {};
map.set(objKey, "value");
```

Такой функциональный итерируемый интерфейс — одно из больших преимуществ по сравнению с объектами. [Дока](https://doka.guide/js/map/)

---

## 📊 Сравнение `Map` vs `Object`

| Сравнение               | `Map`   | `Object`                          |
| ----------------------- | ------- | --------------------------------- |
| Типы ключей             | Любые   | Строки и символы                  |
| Порядок сохранения      | Да      | Нет (не гарантировано)            |
| Перебор                 | Прост   | Через `Object.keys` и ручные шаги |
| Размер                  | `.size` | `Object.keys(obj).length`         |
| Унаследованные свойства | Нет     | Да (`__proto__`, методы)          |

---

## 🧠 Реальные кейсы использования

### 📍 Кэширование данных

Иногда нужно хранить быстрый доступ к данным по сложным ключам (объекты, функции, данные API). `Map` подходит идеально.

### 📍 Отслеживание состояния объектов

Если ключом выступает объект (например, компонент, DOM-узел), `Map` позволяет привязать к нему дополнительные данные, не меняя сам объект.

### 📍 Частое добавление/удаление

В сценариях, где структура данных постоянно растёт и уменьшается (динамические коллекции, система подписок), `Map` показывает лучшие результаты.

---

## 📌 Итог

**`Map` — это мощная, гибкая и удобная коллекция «ключ-значение» в JavaScript**, которая:

✔ хранит данные любых типов  
✔ сохраняет порядок вставки  
✔ предоставляет удобные методы для работы  
✔ эффективнее справляется с частыми изменениями  
✔ имеет удобную итерацию и размер `.size` Summary.

---

<RelatedTopics
	:items="[
		{ title: 'Set', href: '/javascript/kollektsii-dannykh/set' },
		{ title: 'WeakMap', href: '/javascript/kollektsii-dannykh/weakmap' },
		{ title: 'WeakSet', href: '/javascript/kollektsii-dannykh/weakset' },
	]"
/>

