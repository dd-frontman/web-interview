# 🧠 Что такое **WeakMap**

**`WeakMap`** — это особая структура данных в JavaScript, похожая на `Map`, но **с «слабыми» ссылками на ключи**.  
Это значит: если объект-ключ больше нигде не используется, он **может быть автоматически удалён сборщиком мусора (GC)** вместе со связанным значением. [turn0search0][turn0search19]

🔹 Ключами **можно использовать только объекты или неконтролируемые символы** — примитивы вроде строк/чисел туда _не подходят_. [turn0search0]

🔹 Значения — любого типа: числа, строки, объекты, функции. [turn0search1]

---

## 🧠 Что значит “слабая ссылка”

Если ты добавляешь объект в `WeakMap`:

```js
const wm = new WeakMap();
let obj = { name: "Alice" };

wm.set(obj, "данные");

// сейчас доступ есть
console.log(wm.get(obj)); // 'данные'
```

📌 Но если **перестаёт существовать любая другая ссылка на `obj`**, кроме той, что внутри `WeakMap`, сборщик мусора **может удалить этот объект** (и соответствующее значение) автоматически:

`obj = null; // теперь объект и запись в WeakMap могут быть удалены GC`

То есть `WeakMap` **не мешает сбору мусора** — он хранит _слабую ссылку_ на ключ. [turn0search19][turn0search1]

---

## 📌 Главная причина существования WeakMap

В обычном `Map`, если ты используешь объект как ключ, **этот объект никогда не будет удалён GC**, пока он присутствует в карте — даже если больше нигде не используется. Это может привести к **утечкам памяти**. [turn0search11]

`WeakMap` решает эту проблему:  
✔ можно привязать данные к объекту (метаданные, кеш, подсчёт)  
✔ и не надо вручную удалять запись, когда объект больше не нужен  
✔ сборщик мусора сам очистит связанные данные. [turn0search0][turn0search19]

---

## 📋 Методы WeakMap

В `WeakMap` есть только 4 базовых метода:

| Метод                     | Что делает                                     |
| ------------------------- | ---------------------------------------------- |
| `weakMap.set(key, value)` | Добавляет или обновляет значение по ключу      |
| `weakMap.get(key)`        | Возвращает значение по ключу (или `undefined`) |
| `weakMap.has(key)`        | Проверяет наличие ключа                        |
| `weakMap.delete(key)`     | Удаляет запись для ключа                       |

📌 Обратите внимание:  
👉 тут **нет** методов `keys()`, `values()`, `entries()`, `size`, `forEach()`.  
👉 Это сделано специально: кол-во ключей может меняться из-за GC, и поэтому такие итерации неприменимы. [turn0search0][turn0search16]

---

## 🧠 Почему нельзя перебирать WeakMap

В `WeakMap` нет итераторов (`keys()`, `values()`) и `.size` — потому что **сборщик мусора может удалять ключи в любой момент**, и список ключей тогда был бы _неопределённым и непредсказуемым_. Поэтому механизмы получения всех ключей не предусмотрены. [turn0search13][turn0search16]

---

## 🧠 Сравнение `Map` и `WeakMap`

| Характеристика            | `Map`                        | `WeakMap`              |
| ------------------------- | ---------------------------- | ---------------------- |
| Ключи могут быть          | Любые типы                   | Только объекты/символы |
| Сильная или слабая ссылка | Сильная                      | Слабая                 |
| Поддержка перебора        | Есть (`keys/values/entries`) | Нет                    |
| Есть `size`               | Да                           | Нет                    |
| Препятствует GC?          | Да                           | **Нет**                |
| Когда очищается?          | Только вручную               | Автоматически по GC    |

---

## 🧠 Реальные кейсы использования

### 🔹 1) Приватные данные объектов

Когда ты хочешь **связать дополнительные данные с объектом**, но не изменять сам объект, можно сделать это безопасно:

```js
const privateStorage = new WeakMap();

class User {
	constructor(name) {
		privateStorage.set(this, { name });
	}
	getName() {
		return privateStorage.get(this).name;
	}
}

let u = new User("Bob");
console.log(u.getName()); // 'Bob'

u = null; // объект и privateStorage запись могут быть удалены GC
```

📌 Это позволяет скрывать внутренние данные и **не заботиться об очистке памяти**. [turn0search1]

---

### 🔹 2) Кэш временных данных

Если у тебя есть объекты, для которых ты хочешь хранить вспомогательные результаты (например, результаты тяжёлых вычислений):

- используя `Map`, ты _держишь эти объекты в памяти навсегда_
- используя `WeakMap`, они будут автоматически удалены, когда объект больше не нужен. [turn0search19]

---

## 🧠 Когда НЕ использовать WeakMap

❌ Если нужно **перебирать все записи** (например, узнать `.size`, получить ключи) — это невозможно с WeakMap.  
❌ Если ключи — не объекты (строки, числа) — они не подходят для WeakMap. В таких случаях лучше `Map`. [turn0search16]

---

## ✅ Итог простыми словами

**`WeakMap`** — это коллекция ключ-значение, где:

✔ ключи — только объекты  
✔ записи _не препятствуют сбору мусора_  
✔ значения очищаются вместе с ключами автоматически  
✔ нет итерации или `.size`, потому что содержимое может исчезнуть в любой момент

Это делает `WeakMap` отличным инструментом для **сопровождения объектов вспомогательной информацией**, _без риска утечек памяти_. [turn0search0][turn0search1]

---

## 📌 Когда использовать WeakMap

💡 хранение **приватных/вспомогательных данных** для объектов  
💡 кэш на основе объектов  
💡 библиотеки, которые хотят привязать метаданные к объектам без их модификации  
💡 сценарии, где ожидаешь, что ключи будут удаляться по GC

---

> [!tip] Связанные темы
>
> - [Map](/javascript/kollektsii-dannykh/map)
> - [Set](/javascript/kollektsii-dannykh/set)
> - [WeakSet](/javascript/kollektsii-dannykh/weakset)

