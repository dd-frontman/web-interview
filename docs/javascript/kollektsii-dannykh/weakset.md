# 🧠 Что такое `WeakSet` в JavaScript

**`WeakSet`** — это специальная коллекция для хранения **слабых ссылок** на значения.
Чаще всего используют объекты: если объект больше нигде не нужен, сборщик мусора может удалить его автоматически, даже если он был добавлен в `WeakSet`.

Идея простая:

- `Set` держит значения "сильно" (они остаются, пока ты их не удалишь).
- `WeakSet` не мешает GC очищать память.

---

## 📌 Основные особенности

| Свойство | `WeakSet` |
| --- | --- |
| Что хранит | объекты (и non-registered символы) |
| Уникальность | да, дубликаты не добавляются |
| Порядок | не гарантируется, перебора нет |
| Размер | нет `.size` |
| Итерация | нет `for...of`, `forEach`, `values`, `keys` |
| GC | элементы могут удаляться автоматически |

---

## ⚙ Основные методы

У `WeakSet` всего 3 метода:

| Метод | Что делает |
| --- | --- |
| `add(value)` | добавляет значение в коллекцию |
| `has(value)` | проверяет наличие |
| `delete(value)` | удаляет значение |

Пример:

```js
const ws = new WeakSet();

const user = { id: 1 };
ws.add(user);

console.log(ws.has(user)); // true
ws.delete(user);
console.log(ws.has(user)); // false
```

---

## 🔍 Почему нет `.size` и перебора

У `WeakSet` нельзя безопасно перечислить все элементы, потому что часть из них может исчезнуть в любой момент из-за garbage collector.

Именно поэтому у `WeakSet` нет:

- `size`
- `forEach`
- `keys`/`values`/`entries`
- `for...of`

---

## 🧠 Когда `WeakSet` полезен

### 1) Отмечать "уже обработанные" объекты

```js
const processed = new WeakSet();

function processNode(node) {
	if (processed.has(node)) {
		return;
	}

	processed.add(node);
	// тяжелая логика
}
```

Полезно, когда объект живет только пока нужен приложению. Не нужно вручную чистить коллекцию.

### 2) Временные метки для DOM-узлов

```js
const bound = new WeakSet();

function bindClick(el) {
	if (bound.has(el)) {
		return;
	}

	el.addEventListener("click", () => {
		console.log("clicked");
	});

	bound.add(el);
}
```

Когда элемент удалится из DOM и на него не останется ссылок, GC сможет очистить и запись в `WeakSet`.

---

## 📊 Сравнение `Set` и `WeakSet`

| Критерий | `Set` | `WeakSet` |
| --- | --- | --- |
| Тип значений | любые | только объекты/спец. символы |
| Итерация | есть | нет |
| `.size` | есть | нет |
| Контроль удаления | вручную | автоматически через GC |
| Типичный кейс | список уникальных значений | маркеры для объектов без утечек памяти |

---

## ❌ Когда `WeakSet` не подходит

- нужно перебрать все элементы;
- нужно знать точный размер коллекции;
- нужно хранить примитивы (`string`, `number`, `boolean`, `null`, `undefined`, `bigint`).

В этих случаях лучше использовать обычный `Set`.

---

## ✅ Итог

`WeakSet` — это коллекция для **временных связей с объектами**, где важно не держать память лишний раз.

Используй `WeakSet`, когда нужно:

- помечать объекты как "видел/обработал";
- избежать ручной очистки коллекции;
- снизить риск утечек памяти в long-running приложениях.

---

## Официальные источники

- MDN: `WeakSet`  
  https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/WeakSet
- MDN: Keyed collections  
  https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Keyed_collections
- ECMAScript specification: WeakSet Objects  
  https://tc39.es/ecma262/#sec-weakset-objects

---

<RelatedTopics
	:items="[
		{ title: 'Map', href: '/javascript/kollektsii-dannykh/map' },
		{ title: 'Set', href: '/javascript/kollektsii-dannykh/set' },
		{ title: 'WeakMap', href: '/javascript/kollektsii-dannykh/weakmap' },
	]"
/>

