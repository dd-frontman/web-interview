# 🌍 Что происходит, когда пользователь вводит адрес сайта в браузере?

## 1. Краткое объяснение

Когда ты вводишь URL в адресную строку и нажимаешь Enter, браузер проходит через **цепочку шагов**: от проверки кэша и DNS-запроса до загрузки HTML, CSS, JS и финальной отрисовки страницы. Этот процесс называется **Critical Rendering Path (CRP)**.

---

## 2. Подробное объяснение (пошагово)

### 🔹 1. Ввод URL и проверка

- Браузер проверяет:
  1. **Browser cache** — нет ли страницы в кеше.
  2. **Service Worker** — если он установлен, то запрос может перехватиться.
  3. **DNS cache** — знает ли он IP адрес.

### 🔹 2. DNS-резолвинг

- Если IP неизвестен: браузер обращается к **DNS-серверу**.
- DNS ищет: `example.com` → IP-адрес сервера (например, `93.184.216.34`).

### 🔹 3. Установление соединения

1. **TCP handshake** (3-way handshake): клиент ↔ сервер договариваются о соединении.
2. **TLS handshake** (если HTTPS): обмен сертификатами, шифрование, ключи.

### 🔹 4. Отправка HTTP-запроса

- Браузер отправляет HTTP-запрос:  
  GET / HTTP/1.1  
  Host: example.com

```
- Сервер обрабатывает и отвечает.

### 🔹 5. Ответ от сервера
- Сервер возвращает **HTTP-ответ** (обычно HTML).
```

200 OK  
Content-Type: text/html

````
- В теле ответа — HTML-документ.

### 🔹 6. Парсинг HTML → DOM
- Браузер начинает читать HTML сверху вниз.
- Строит **DOM-дерево**.
- Если встречает:
- `<link rel="stylesheet">` → делает запрос за CSS.
- `<script>` без `defer/async` → блокирует парсинг и выполняет JS.

### 🔹 7. Загрузка и парсинг CSS → CSSOM
- Браузер загружает все CSS-файлы.
- Создаёт **CSSOM** (дерево стилей).
- ⚠️ CSS **блокирует отрисовку** (без стилей нельзя построить render tree).

### 🔹 8. DOM + CSSOM = Render Tree
- Объединяются в **Render Tree** (структура: что рисовать и как).
- Невидимые элементы (`display: none`) в Render Tree не попадают.

### 🔹 9. Layout (Reflow)
- Браузер вычисляет размеры, позиции каждого элемента.
- Например: ширина блока = 100%, кнопка внутри = 200px.

### 🔹 10. Paint и Composite
- **Paint**: отрисовка пикселей (цвета, фоны, текст).
- **Compositing**: объединение слоёв (например, видео, canvas, transform).

### 🔹 11. JavaScript и динамика
- JS может изменить DOM → снова запускается reflow/repaint.
- Поэтому тяжёлые операции в JS могут замедлять отрисовку.

---

## 3. Пример (схематично)

```text
URL → DNS → TCP/TLS → HTTP-запрос → HTML → DOM
                        ↓
                    CSS → CSSOM
                        ↓
                DOM + CSSOM → Render Tree → Layout → Paint → Composite

````

## 5. Чит-щит шпаргалка

| Шаг             | Что происходит                   |
| --------------- | -------------------------------- |
| 1. Ввод URL     | Проверка кеша, сервис-воркеров   |
| 2. DNS          | Домен → IP                       |
| 3. TCP/TLS      | Установление соединения          |
| 4. HTTP-запрос  | `GET /`                          |
| 5. Ответ        | Сервер возвращает HTML           |
| 6. Парсинг HTML | Строится DOM, загружается CSS/JS |
| 7. Парсинг CSS  | Строится CSSOM                   |
| 8. Render Tree  | DOM + CSSOM                      |
| 9. Layout       | Расчёт размеров и позиций        |
| 10. Paint       | Отрисовка пикселей               |
| 11. Composite   | Сборка финальной картинки        |
