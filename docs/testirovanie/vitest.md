## 🧠 Что такое **Vitest**

**Vitest** — это современный **тест-раннер и тест-фреймворк** для JavaScript/TypeScript, прямо из экосистемы **Vite**.  
Он создан как **следующее поколение утилит для тестирования**, оптимизированное под быстрые сборки и современные проекты. [Vitest](https://vitest.dev/guide/)

👉 Его цель — дать **удобный, быстрый и простой способ писать и запускать тесты**, особенно если проект уже использует **Vite/vue**. [Vue.js](https://vuejs.org/guide/scaling-up/testing)

---

## ⚡ Почему выбирают Vitest

### 1) **Скорость**

Vitest очень быстрый за счёт tight-интеграции с Vite и использованием технологий, которые ускоряют запуск и трансформацию модулей. Это даёт более быстрый feedback при разработке тестов. [Sauce Labs](https://saucelabs.com/resources/blog/vitest-vs-jest-comparison)

---

### 2) **Интеграция с Vite**

Он использует существующую Vite-конфигурацию (плагины, трансформеры и т. д.), поэтому:

✔ не нужно отдельно настраивать транспиляцию/плагины  
✔ твой компилятор для тестов — тот же, что и для разработки  
✔ поддерживаются ESM, TypeScript, JSX сразу из коробки. [Vitest](https://vitest.dev/guide/)

---

### 3) **Совместимость с Jest-API**

Vitest старается быть **совместимым с API Jest**, поэтому если ты знаком с Jest, то писать тесты в Vitest будет очень похоже: `test()`, `expect()`, `describe()` и т. д. [Sauce Labs](https://saucelabs.com/resources/blog/vitest-vs-jest-comparison)

---

### 4) **Поддержка современных возможностей**

Vitest из коробки поддерживает:

- TypeScript
- ESM-модули
- асинхронные тесты
- фильтрацию тестов
- запуск в параллельных потоках
- режим браузера  
   — и многое другое без сложной настройки. [Vitest](https://vitest.dev/guide/features)

---

## 🧪 Основные возможности

### 🔹 Тесты и описание

```js
import { test, expect } from "vitest";

test("sum works", () => {
	expect(1 + 2).toBe(3);
});
```

✔ `test` или псевдоним `it` — описывает тест  
✔ `expect` — делает утверждения  
✔ можно использовать `test.skip`, `test.only` и др. для управления запуском. [Vitest](https://vitest.dev/api/)

---

### 🔹 Расширенные API

Vitest поддерживает такие хуки, как:

- `beforeAll` — перед всеми тестами
- `afterAll` — после всех
- `beforeEach` — перед каждым
- `afterEach` — после каждого

Это помогает подготовить/очистить окружение теста. [Vitest](https://vitest.dev/api/)

---

### 🔹 Параллельные и условные тесты

Можно запускать несколько тестов параллельно:

```js
test.concurrent('parallel 1', async () => {...})
test.concurrent('parallel 2', async () => {...})
```

А также условно пропускать/выполнять:

```js
test.skip('skip this test', () => {...})
test.runIf(condition)('only if condition', () => {...})
```

Такой контроль удобен для сложных тестовых сетапов. [Vitest](https://vitest.dev/api/)

---

## 🧠 Почему он особенно хорош для Vue/Vite

🔸 **Vue официально рекомендует Vitest** для тестирования компонентов, composables и других модулей, если проект создан на основе Vite. [Vue.js](https://vuejs.org/guide/scaling-up/testing)

🔸 Он работает с тем же конфигом Vite (тот же плагин Vue, те же трансформы), поэтому конфигурация тестов почти не требует усилий. [Vue.js](https://vuejs.org/guide/scaling-up/testing)

🔸 Тесты можно писать рядом с компонентами или в отдельной папке — это гибко для команд. [Vue FAQ](https://vue-faq.org/ru/book/Chapter_9__Testing_and_Source_Control.html)

---

## 🧠 Интеграция с компонентными тестами

Для тестирования Vue-компонентов Vitest часто используют **вместе с Vue Test Utils** — утилитой для монтирования компонентов и взаимодействия с ними в тестах. [test-utils.vuejs.org](https://test-utils.vuejs.org/guide/)

Пример простого теста компонента:

```js
test.skip('skip this test', () => {...})
test.runIf(condition)('only if condition', () => {...})
```

Здесь:  
✔ `mount()` — рендерит компонент виртуально  
✔ тест проверяет, что UI рендерится правильно  
✔ Vitest запускает и проверяет утверждения. [test-utils.vuejs.org](https://test-utils.vuejs.org/guide/)

---

## 📌 Дополнительные фичи

### 📍 Тестирование типов

Vitest поддерживает тесты для TypeScript-типов (например, `*.test-d.ts`), что помогает убедиться в корректности интерфейсов/типов. [Vitest](https://vitest.dev/guide/testing-types)

---

## 📦 Конфигурация

Конфигурация Vitest обычно живёт в `vite.config.js` или `vitest.config.js`. Это означает, что ты можешь использовать сотни настроек Vite (алиасы, плагины, трансформаторы) без дублирования настроек. [Vitest](https://vitest.dev/config/)

---

## 🧠 Пример полного рабочего цикла

1. Устанавливаешь:

`npm install -D vitest`

или

`yarn add -D vitest`

✔ интегрируется с Vite, использует ту же экосистему  
✔ не требует отдельного билда тестов (как в Jest)  
✔ тесты запускаются очень быстро  
✔ есть watch-режим + мгновенная обратная связь. [Vitest](https://vitest.dev/guide/)

---

## 📊 Кому подходит

🔹 Проекты на **Vite / Vue / React / Svelte**  
🔹 TypeScript-проектам  
🔹 Командам, которые хотят **быстрее запускать тесты без лишней конфигурации**  
🔹 Тестировать UI, утилиты, composables, логику или даже типы. [Vitest](https://vitest.dev/guide/)

---

## 🧠 Короткое резюме

Vitest — это:

✅ современный тест-раннер (аналог Jest)  
✅ интегрированный с Vite  
✅ быстрый и удобный  
✅ хорошо работает с Vue и TypeScript  
✅ поддерживает продвинутые возможности (параллель, фильтрация, типы) [Vitest](https://vitest.dev/guide/)

---

> [!tip] Связанные темы
>
> - [Vitest + Vue Test Utils + Playwright](/testirovanie/vitest-plus-vue-test-utils-plus-playwright)
> - [Vue](/vue)
> - [Реактивность во Vue3](/vue/ref-and-reactive/reaktivnost-vo-vue3)

