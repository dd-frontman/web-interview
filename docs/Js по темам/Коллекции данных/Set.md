# 🧠 Что такое `Set` в JavaScript

**`Set`** — это **структура данных (коллекция) для хранения уникальных значений любого типа**: примитивов (чисел, строк, булевых) и ссылок на объекты.  
То есть каждое значение в `Set` **может появляться только один раз** — дубликаты автоматически игнорируются. [MDN Web Docs](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Set?utm_source=chatgpt.com)

📌 Уникальность в `Set` определяется алгоритмом `SameValueZero`:  
✔ `NaN` считается равным `NaN` (даже если `NaN !== NaN`)  
✔ `0` и `-0` считаются одинаковыми  
✔ для объектов сравнение — по ссылке (тоже самое, что и `===`, но с особым `NaN`). [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Set?utm_source=chatgpt.com)

---

## 📌 Основные особенности

| Свойство | Значение                                               |
| -------- | ------------------------------------------------------ |
| Тип      | Коллекция уникальных значений                          |
| Порядок  | Сохраняет порядок вставки                              |
| Индекс   | В `Set` **нет** индексов — нельзя обращаться по номеру |
| Итерация | Можно перебрать в порядке вставки                      |
| Значения | Любые типы (числа, строки, объекты и т. д.)            |

---

## 🛠 Как создать и использовать

### 📍 Создание пустого Set

`const mySet = new Set();`

Или сразу с элементами:

`const unique = new Set([1, 1, 2, 3]); // unique содержит: 1, 2, 3`

Это удобно, например, чтобы **удалить дубликаты из массива**. [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Set/Set?utm_source=chatgpt.com)

---

## ⚙️ Основные методы

| Метод                 | Что делает                                        |
| --------------------- | ------------------------------------------------- |
| `add(value)`          | Добавляет значение (если его еще нет)             |
| `delete(value)`       | Удаляет значение, возвращает `true` если было     |
| `has(value)`          | Проверяет, есть ли значение                       |
| `clear()`             | Удаляет все элементы                              |
| `size`                | Количество элементов в Set                        |
| `values()` / `keys()` | Итератор значений (для совместимости с `Map`)     |
| `entries()`           | Итератор пар `[value, value]` (для совместимости) |
| `forEach()`           | Перебирает элементы                               |

---

## 📌 Примеры

### 🔹 Добавление и проверка

```js
const set = new Set();

set.add(1);
set.add(5);
set.add(5); // не добавит второй раз

console.log(set.has(1)); // true
console.log(set.has(3)); // false
console.log(set.size); // 2
```

Здесь даже `set.add(5)` два раза **не создаёт дубликата**. [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Set?utm_source=chatgpt.com)

---

### 🔹 Object как значение

```js
const obj = { name: "Alice" };
set.add(obj);
console.log(set.has(obj)); // true
```

Примет объекты как значения и хранит по ссылке (другие похожие объекты — другие значения). [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Set?utm_source=chatgpt.com)

---

### 🔹 Итерация

```js
for (const val of set) {
	console.log(val);
}

set.forEach((v) => console.log(v));
```

Элементы будут в том порядке, в котором добавлялись. [MDN Web Docs](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Set?utm_source=chatgpt.com)

---

## 🧠 Почему `Set` полезен

### ✨ 1) Удаление дубликатов

Частый кейс: фильтрация массива, чтобы остались только уникальные элементы.

```js
const arr = [1, 1, 2, 2, 3];
const uniqueArr = [...new Set(arr)]; // [1, 2, 3]
```

Это короткий и эффективный способ убрать дубликаты. [MDN Web Docs](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Set?utm_source=chatgpt.com)

---

### ⚡ 2) Быстрое наличие элемента

Метод `.has()` работает значительно быстрее, чем `Array.prototype.includes`, особенно в больших наборах данных. [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Set?utm_source=chatgpt.com)

---

### 🧠 3) Математические операции над множествами _(новые методы)_

Современные версии JavaScript добавляют методы вроде `union`, `intersection`, `difference`, `isSupersetOf` и др.  
Они полезны, если нужно сравнивать и комбинировать множества, как в математике. [MDN Web Docs](https://developer.mozilla.org/en-US/blog/javascript-set-methods/?utm_source=chatgpt.com)

---

## 🧠 Отличие `Set` от `Array`

| Свойство      | `Array`                      | `Set`                   |
| ------------- | ---------------------------- | ----------------------- |
| Дубликаты     | Да                           | ❌ Нет (уникальность)   |
| Индексы       | Да                           | ❌ Нет (только перебор) |
| Порядок       | Да                           | Да (порядок вставки)    |
| Методы        | Многие (map, filter, reduce) | Базовые                 |
| Быстрый поиск | `includes`                   | `has` (быстрее)         |

---

## 📌 Отличие `Set` от `Map`

- `Set` хранит **только значения**,
- `Map` хранит **пары ключ → значение**.  
   По сути, `Set` похож на `Map`, где ключ и значение **одно и то же**. [purpleschool.ru](https://purpleschool.ru/blog/JS-Map_Set?utm_source=chatgpt.com)

---

## 🧠 Чем `Set` не является

❌ Он **не хранит индексы** → нельзя обратиться по номеру элемента  
❌ Он **не гарантирует автоматическое получение “лучшего” элемента** → только перебор  
❌ Он **не сохраняет методы `get(key)`** → есть только `has(value)`  
Это часть его дизайна: `Set` — не комбинация массива и объекта, а именно _множество уникальных значений_. [purpleschool.ru](https://purpleschool.ru/blog/JS-Map_Set?utm_source=chatgpt.com)

---

## 🧠 Когда использовать `Set`

✔ Список уникальных значений  
✔ Быстрый доступ проверки наличия  
✔ Фильтрация больших массивов  
✔ Алгоритмы на множественных коллекциях (пересечения/объединения/разности)

---

## 📋 Полный пример

```js
// создаём Set
const uniqueUsers = new Set();

// добавляем
uniqueUsers.add("Alice");
uniqueUsers.add("Bob");
uniqueUsers.add("Alice"); // не добавится второй раз

// проверяем
console.log(uniqueUsers.has("Bob")); // true

// перебираем
uniqueUsers.forEach((name) => {
	console.log(name);
});

// удаляем
uniqueUsers.delete("Bob");

// количество
console.log(uniqueUsers.size); // 1
```

---

## 🔍 Итог

**`Set` — это коллекция уникальных значений любого типа, построенная по принципу математического множества**:  
✔ сохраняет порядок вставки  
✔ не допускает дубликатов  
✔ имеет оптимизированное API  
✔ работает быстрее для проверок наличия, чем массивы [MDN Web Docs](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Set?utm_source=chatgpt.com)
