# Web Interview Documentation

Документация по веб-разработке, собранная в одном месте.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Локальная разработка

```bash
npm run dev
```

Документация будет доступна по адресу `http://localhost:5173/web-interview/`

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `.vitepress/dist`

### Просмотр собранной версии

```bash
npm run preview
```

## 📚 Структура документации

- **Vue по темам** - материалы по Vue.js
- **React** - материалы по React
- **Архитектура** - архитектурные паттерны и подходы
- **Js по темам** - основы JavaScript
- **Ts по темам** - TypeScript
- **Nuxt** - фреймворк Nuxt.js
- **Браузеры** - работа браузеров, HTTP, производительность
- **Безопасность** - безопасность веб-приложений
- **Алгоритмы** - алгоритмы и структуры данных
- **ООП** - объектно-ориентированное программирование

## 🌐 Деплой на GitHub Pages

Проект уже настроен для автоматического деплоя на GitHub Pages.

### Настройка деплоя

1. **Включите GitHub Pages в настройках репозитория:**
   - Перейдите в `Settings` → `Pages`
   - В разделе `Source` выберите `GitHub Actions`

2. **Настройте base URL (если нужно):**
   - Если репозиторий называется не `web-interview`, откройте `.vitepress/config.ts`
   - Раскомментируйте и измените строку `base: '/'` на `base: '/имя-репозитория/'`

3. **Автоматический деплой:**
   - После каждого push в ветку `main` или `master` документация будет автоматически собираться и деплоиться
   - Workflow файл находится в `.github/workflows/deploy.yml`

### Альтернативные платформы для деплоя

- **Vercel:** Просто подключите репозиторий, Vercel автоматически определит VitePress
- **Netlify:** Добавьте команду сборки `npm run build` и папку публикации `.vitepress/dist`
- **Cloudflare Pages:** Аналогично Netlify

## 📝 Добавление новых материалов

1. Добавьте markdown файл в соответствующую папку
2. Обновите навигацию в `.vitepress/config.ts` в секции `sidebar`
3. При необходимости добавьте ссылку в `nav` для быстрого доступа

## 🛠 Технологии

- [VitePress](https://vitepress.dev/) - генератор статических сайтов на основе Vite и Vue
